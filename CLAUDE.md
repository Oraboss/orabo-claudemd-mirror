# Orabo (JapaConnect) — Claude Code Guide

## Project Overview
Orabo is a platform empowering Africans with verified pathways to study, work, and migrate legally to the USA, Canada, UK, and Europe.

**Tagline:** "Japa is a dream. We help you plan it, verify it, and live it."

**Target users:** African aspirants (Nigeria, Ghana, Kenya, South Africa, Ethiopia, Tanzania, Uganda, Cameroon, and other African countries)

---

## Current State — Phase 8 + Phase 5A/5B + Phase EB Tier 1 + Phase EB Tier 2 + Phase EB Tier 3 + Phase O-1A + Phase UKGT Tier 1 + Phase UKGT Tier 2 + Phase UKGT Tier 3 + Phase UKGT Peer Review Tier 1 + Phase UKGT Peer Review Tier 2 + Phase UKGT Peer Review Tier 3 + Email Pipeline Hardening + Review Screen Polish + Back Buttons & Cancel Return + Payment Integrity (Live) + Pay Calculator 18 Countries + Save & Resume Draft (14 Tools) + Free Preview Verdict (8 Tools) + Migration Worth It Calculator + Dashboard Phase 5 (Preferences Modal + Retake Quiz) + Dashboard Phase 6 (Pro Retention: Usage Analytics + Monthly Value Report) + Sources Part 1 (Numeric Data Citation) + Sources Part 2 (AI Document Citation Footers) + GA4 Analytics (Site-wide) + Inline Script Extraction (CSP) + Phase Petition Audit (O-1A) + Phase Petition Audit Phase 2 (EB-1A + EB-2 NIW, build+calibrate) + EB-2 Letter R-01 (Prong 2 Filing Risk Advisory) + Sources Footer Leak Fix (2026-05-21) + Compare Destinations (pathway-compare.html) + Compare Destinations Hotfix (pathway-level overview card) + Compare Destinations Content Fix (us_green_card DV/EB-2/NIW facts + navbar logo) + Checklist Knowledge Base Content Fix (us_green_card DV/EB-2/NIW backend facts + CHECKLIST_PROMPT_VERSION v2.2) + Checklist Accuracy Sweep (African-eligibility claims across 13 pathways — Congo DRC E-2 treaty added, UK ENIC rename, Nuffic rename + CHECKLIST_PROMPT_VERSION v2.3) + Compare Destinations Re-sync (aligned with backend CHECKLIST_PROMPT_VERSION v2.3: DRC E-2 treaty added to investor_business_visa×US, UK ENIC/NARIC→UK ENIC, EP-Nuffic→Nuffic; 24h soak clock reset 2026-05-21) + Compare Destinations Phase B SEO (validation batch: 12 programmatic comparison pages + generator + manifest, 2026-05-21) + Pathway Match Phase C Part 1 (backend route + prompts + Stripe wiring, 2026-05-21) + Pathway Match Phase C Part 2 (frontend tool — 5-section form, review, Stripe return, DOCX download, 2026-05-21) + Compare SEO Quality Pass (a11y + grammar + emailjs guard, 2026-05-21) + EB Waitlist CSP Fix (eb-immigration.html inline script externalised to js/eb-immigration.js, 2026-05-21) + Phase Petition Audit Phase 4 (EB-1A + EB-2 NIW frontend funnel — Stripe SKU wiring, tool pages, discoverability cards, 2026-05-22) + Phase Petition Audit Phase 5 (UKGT Endorsement Audit — single SKU `ukgt_audit` $59, Tech Nation + Peer Review routes, 2026-05-22) + Reset Password Page (dedicated reset-password.html + js/reset-password.js, CSP-compliant external script, 2026-05-23) + Compare SEO Navbar Fix (/compare/ index + 12 child pages — navbar-solid inline style block added to generator buildPage() + buildManifest(); all pages regenerated, 2026-05-23) + EB Hero Gold Family-Match (eb-immigration.html hero CTAs changed to gold .btn-hero-primary matching o1a/ukgt convention; dead .lp-hero .btn-primary glow override removed; button role system documented, 2026-05-23) + CV Tool Client-side PDF + DOCX Extraction (js/cv-file-extract.js new ES module using vendored pdfjs-dist@4.10.38 + mammoth@1.8.0; supports .pdf/.docx/.txt on Section 5 file input; scanned/image PDFs detected via MIN_TEXT_CHARS=20; 10 MB size guard; worker-src 'self' added to _headers — only CSP change; no unsafe-eval; MIME check PASS text/javascript on Cloudflare Pages, 2026-05-23) + Homepage Reframe Phase 0 (First-party funnel analytics — analytics_events table on Orabo Prod; POST /api/track route with dedicated trackLimiter 600/min; js/funnel-track.js ES module on index.html tracking page_view, scroll_depth, section_view, tool_start, eligibility_complete; checkout_start deferred — no inline Stripe on index.html; tool_start is interim conversion-intent proxy, 2026-05-23) + Homepage Reframe Phase 1 (Destination-led hero — new H1 "Verified legal pathways to the USA, Canada, UK and Europe.", tagline "Plan it, verify it, live it.", single primary CTA #hero-eligibility-cta opens eligibility checker in one click via js/hero-cta.js IIFE, trust strip with EB-1A · EB-2 NIW · O-1A · UK Global Talent, removed: countries strip/stats/social proof; sticky mobile CTA re-pointed to #eligibility; hero-trust-strip + hero-explore-link CSS added to css/hero.css, 2026-05-23) + Homepage Reframe Phase 2 (Plan → Verify → Live band reorganization — homepage body restructured into three labelled bands: PLAN IT (5 members: Readiness Quiz, Visa Tracker, Cost of Living, Pay Calculator, Compare Destinations promo + Worth It callout + Live Today #features intro), VERIFY IT (most visual weight: 4px gold top rule, larger gold eyebrow, heavier description text; #eligibility + new minimal Pathway Match card — no price per Phase 3 ladder), LIVE IT (.us-tools-section, #cv-tool, #sop-tool, #doc-checklist, #consultation, relocated .eb-waitlist-section); below-the-spine zone: blog teaser + #universities, #scholarships, #visa, #timeline + community + #newsletter + #waitlist; all 20+ in-page anchors preserved; funnel-track.js landmarks unchanged; no IDs renamed; band CSS added to css/features.css; 24h soak before Phase 3, 2026-05-23) + Homepage Reframe Phase 3 (Pricing ladder + attorney anchor, 2026-05-23): Compact `.rung-ladder` chip strip added to LIVE IT band header (Free → From $9 → From $12 → From $240, styled for light `var(--bg)` background); US Immigration cards retain headline `From $39` and gain per-card `.attorney-anchor` value line (EB: full package $240, O-1A: $250, UKGT: $250 — all canonical-verified; attorney range $4,000–$15,000); CV Review and SOP Writer sections replace six scattered `.tool-price` elements with shared `.rung-label` + `.rung-tag--draft` (CV From $12, SOP From $28); Doc Checklist note gains `.rung-tag--assess`; Pathway Match card gains `Assess · $19` `.rung-label` via `.pm-rung-label` spacing class; `css/pricing.css` (new, ~75 lines, no raw hex, no inline `style=`) added to `styles.css` before `mobile.css`; all `data-type`/`data-plan`/`href`/`id` Stripe hooks byte-for-byte unchanged; 24h soak before Phase 4. + Homepage Reframe Phase 4 (Reveal-at-value + eligibility relabel, 2026-05-24): After free eligibility result renders, a `.elig-reveal` upsell block is injected into `#eligResultsView` via `js/eligibility-reveal.js` (new non-module IIFE, ~100 lines). MutationObserver on `#eligResultsView` watches `attributeFilter: ['style', 'hidden']` — fires when `showResultsView()` sets `style.display='block'`; resets `_revealed=false` when display goes to `'none'` (retake path). Remove-before-inject (`rv.querySelector('.elig-reveal')?.remove()`) runs before every injection for correctness. PATHWAY_REVEAL_MAP routes `'O-1 Extraordinary Ability'` → `/o1a-visa.html` ($39/$250) and `'Global Talent Visa'` → `/uk-global-talent.html` ($39/$250); all other 10 visa matches fall through to DEFAULT_PATHWAY (`/pathway-match-tool.html`, $19). Pro detection: `localStorage.getItem('orabo_pro_status') === 'true'` — Pro + tooled pathway shows neutral explore link; Pro + non-tooled suppresses entirely; free users see full pricing + attorney anchor (`$4,000–$15,000`) + gold CTA. Analytics: `window.oraboTrack = track` added to `funnel-track.js` (1 line) exposes `track()` for non-module scripts; reveal fires `reveal_shown` + CTA fires `reveal_cta_click` both via fail-open `window.oraboTrack`. `eligibility.js` is byte-for-byte unchanged. `css/pricing.css` extended with `.elig-reveal`, `.elig-reveal__headline`, `.elig-reveal__pricing`, `.elig-reveal__attorney`, `.elig-reveal__cta`, `.elig-reveal__neutral-link` + mobile media query. Eyebrow relabel: `#eligibility` section-tag changed from `Pro Tools` → `FREE · 3 CHECKS` (3 cumulative total, never resets). No Stripe/backend changes; no raw hex; no inline `style=`; CSP-safe external script. + Eligibility Checker EB Phase A (EB-2 NIW as 13th visa, 2026-05-24): EB-2 National Interest Waiver added as 13th entry to VISAS array in `js/eligibility.js`. Field is the dominant discriminator: Q6 STEM/Healthcare=20, Arts/Sports/Trades=0, Finance=5, Education=4; all other signals small (Q2 edu max=10, Q3 exp max=7, Q10 USA=7, Q1 age=1, Q4 Advanced=1, Q5 not-required=2; Q7/Q8/Q9=0). MAX_SCORE=48; STEM/Healthcare profile ~96%; Arts/Sports/Trades profile ~54% (≤55% standalone requirement met, not top match). `'EB-2 National Interest Waiver'` added to PATHWAY_REVEAL_MAP in `js/eligibility-reveal.js` → `/eb-immigration.html` ($35 assess / $240 pkg, verified against ITEM_PRICES). `index.html` "10 visa pathways scored" corrected to "13" (was stale when array already had 12 entries). 7 hardcoded "12" strings in `visa-eligibility.html` → "13". Dynamic counts (VISAS.length) auto-update to 13. **Known limitation:** Q6 `'Arts/Research/Sports/Other'` bundles research (genuine NIW national-importance field) with arts/sports (generally don't qualify) — legitimate researchers are under-matched by the 0 score on that option. Fixing requires splitting Q6 into distinct options; deferred to Phase B. 24-hour soak in progress. + Petition Audit Landing Page (2026-05-24): `petition-audit.html` — new standalone router/landing page explaining the adjudicator-style audit product and routing users to the correct per-type tool. Hero explains this is for users who already have a drafted petition. 2×2 `.audit-router-grid` routes: O-1A → `/o1a-audit-tool.html`, EB-1A → `/eb1a-audit-tool.html`, EB-2 NIW → `/eb2-audit-tool.html`, UK Global Talent → `/ukgt-audit-tool.html`. Includes "How it works" 3-step section, output checklist, Audit vs Generate distinction band, disclaimer. Mirrors `o1a-visa.html` template (`.lp-nav`, `.lp-hero`, `.lp-section`, `.lp-footer`; inline `<style>` in `<head>`; no JS required — static links only; no inline `style=`/`onclick=`; CSP-safe). Full OG block + canonical + twitter:card. `index.html` VERIFY band: `us-tools-grid--single` modifier removed; Petition Audit card added as sibling to Pathway Match (`.us-tool-card`, `data-tool="petition_audit"`, `Assess · $59`, no attorney anchor). Nav Tools dropdown: `🔍 Petition Audit` entry added after UK Global Talent Tools in both `index.html` and `pathway-compare.html` (the only two pages with the full Tools dropdown). `sitemap.xml`: entry added (priority 0.8, lastmod 2026-05-24). Icon 🔍 consistent in both nav and VERIFY card. No changes to four audit tool pages, eligibility.js, eligibility-reveal.js, PATHWAY_REVEAL_MAP, or ITEM_PRICES.

> **Email pipeline last reviewed: 2026-05-22.** Active `[brevo-<context>]` contexts: `checklist`, `report`, `cv`, `cv-attr`, `sop`, `sop-attr`, `eb1a`, `eb1a-attr`, `eb2`, `eb2-attr`, `eb1a-letter`, `eb1a-letter-attr`, `eb2-letter`, `eb2-letter-attr`, `eb1a-package`, `eb1a-package-attr`, `eb2-package`, `eb2-package-attr`, `o1a`, `o1a-attr`, `o1a-letter`, `o1a-letter-attr`, `o1a-package`, `o1a-package-attr`, `ukgt-tech`, `ukgt-tech-attr`, `ukgt-tech-statement`, `ukgt-tech-statement-attr`, `ukgt-tech-package`, `ukgt-tech-package-attr`, `ukgt-research`, `ukgt-research-attr`, `ukgt-research-statement`, `ukgt-research-statement-attr`, `ukgt-research-package`, `ukgt-research-package-attr`, `preview`, `worthit`, `consultation`, `waitlist-welcome`, `rejection`, `story-approved`, `story-rejected`, `payout`, `opp-ack`, `publish`, `o1a-audit`, `o1a-audit-attr`, `eb1a-audit`, `eb1a-audit-attr`, `eb2-audit`, `eb2-audit-attr`, `ukgt-audit`, `ukgt-audit-attr`, `pathway-match`, `pathway-match-attr`.

- **Frontend:** Static site — source code in private GitHub repo, deployed to Cloudflare Pages at `https://orabo.app`. Cloudflare Pages builds from the `main` branch on push; `_headers` in the repo root sets server-level HTTP headers (including CSP) at the edge.
- **Frontend GitHub repo:** https://github.com/Oraboss/japaconnect (private)
- **Backend:** Node/Express on Railway — health check: `GET /health` returns `{"status":"ok"}`
- **Database:** Supabase (two projects — Orabo Prod for users/payments/AI cache; Orabo Main for content/CMS). See Supabase section below.
- **Phase 7 (SEO):** 4 SEO landing pages + blog directory with 3 articles added to root. No JS logic changes, no backend.
- **Phase 7C (Homepage discoverability):** Blog teaser section (3 cards) + EB waitlist banner added to `index.html` above footer. Nav Tools dropdown extended with Blog and EB Green Card Tools links. New `css/eb-waitlist.css` + `js/eb-index-waitlist.js`.
- **Phase 8 (Admin panel):** `admin.html` + `css/admin.css` + `js/admin.js` (frontend); `routes/admin.js` (backend). Token-gated at `x-admin-token` header. Manages opportunities, stories, scholarships, universities, Post & Earn rewards, users, and one-off purchases. Opportunity phone field added to form + backend. `post_earn_rewards` table added.
- **Phase 4 (Nav restructure):** Nav restructured with Tools dropdown grouping CV Review, SOP Writer, Visa Tracker, Cost of Living, Pay Calculator. Sticky mobile CTA bar added at bottom of viewport on `index.html`.
- **Phase 5A (Post-purchase email sequences):** CV and SOP delivery emails now include the specific output type purchased, upsell paragraph (CV→SOP Writer; SOP→Consultation), and fire-and-forget Brevo contact attribute update (`PURCHASE_DATE`, `LAST_TOOL`) after every successful generation.
- **Phase 5B (Quiz score email capture):** Free quiz on `index.html` shows an email capture block after score reveal (MutationObserver on `#quizResultsScreen`). `js/quiz-email-capture.js` (non-module) injects the UI, POSTs to `/api/newsletter/subscribe` with `quiz_score`/`quiz_date`/`source`. Backend `/api/newsletter/subscribe` extended: `destination` now optional; quiz attributes (`QUIZ_SCORE`, `QUIZ_DATE`, `QUIZ_LEAD`) set when `quiz_score` is present. **Note:** `quiz-email-capture.js` is currently a disabled stub (`// Disabled — pending rebuild`) — Phase 5B email capture behaviour is dormant until the rebuild is complete.
- **Dashboard Rebuild Phase 3 (Quiz Score Auto-Save, 2026-05-16):** When a logged-in user completes the free readiness quiz on `index.html`, their score + dimensions are saved fire-and-forget to `user_profiles.quiz_results` on Orabo Prod. Backend: `routes/quiz.js` (NEW — mounted at `/api/quiz`), single POST `/save-results` handler; JWT-gated via `resolveAuthUser`; feature-flagged behind `DASHBOARD_V2_ENABLED`; rate-limited 10/min per IP; validates score (0–100), dimensions (4-key object or null), country_of_origin/destination strings (max 80 chars); overwrites previous quiz_results on retake. Frontend: `js/quiz-results-saver.js` (NEW non-module IIFE) — capture-phase click listener on `#quizOptions` builds `trackedScores` per question (mirrors static OPTION_SCORES + QUESTION_ORDER from quiz.js); MutationObserver on `#quizResultsScreen` watches `style` attribute (`quiz.js` uses `style.display`, not `.active` class); on results visible: attemptSave() dispatches immediately if trackedScores has entries, else after 2s delay (payment-return path). Primary save uses computeScoreFromClicks(trackedScores) for score + computeFromTracked() for dimensions (both require all 15). Fallback (payment-return, no tracked answers) uses getQuizScoreFromDom() after 2s delay. Coupling: computeScoreFromClicks() must stay in sync with quiz.js scoring formula. Aborts rather than persisting null/NaN score. Anonymous users skip silently (no session → no POST). sessionStorage flag `orabo_quiz_saved` dedupes within session; cleanup-first wipe on page load; cleared on network/save error to allow retry-on-retake. Loaded after `free-quiz-origin.js` on `index.html` only. `quiz.js` not modified.
- **Phase EB Tier 1 (EB immigration tools — Tier 1 only):** `eb1a-tool.html` + `eb2-tool.html` live. Routes: `routes/eb1a.js` + `routes/eb2.js` (NO ai_cache reads — every assessment generated fresh). Frontend modules: `js/eb1atool.js` + `js/eb1atool-questions.js` + `js/eb2tool.js` + `js/eb2tool-questions.js`. CSS: `css/ibtool.css` (imported in `styles.css` before `mobile.css`). Stripe items: `eb1a_assessment` ($39), `eb2_assessment` ($35). Tooltip pattern: `.ib-hint-icon` + `.ib-hint-tooltip` — CSS hover only, no JS. Legal disclaimer appended to every EB output before docgen. Nav "Soon" badge removed from EB entry. Tool links added to `eb-immigration.html` above the tools breakdown section. **Payment return fixed:** `js/stripe.js` `checkPaymentReturn` IIFE now bypasses the generic banner for EB items, preserving the URL for the EB tool module to handle — identical to the CV/SOP pattern. Tier 1 now captures a final supporting-evidence and recommendation-letter inventory section (EB-1A Section E, EB-2 NIW Section D), and the AI weighs this when rating criteria/prongs and produces a new 'Evidence Position Summary' output block.
- **Phase EB Tier 2 (EB immigration tools — Tier 2):** `eb1a-letter-tool.html` + `eb2-letter-tool.html` live. Routes: `routes/eb1a.js` + `routes/eb2.js` each extended with an `eb1a_letter` / `eb2_letter` outputType branch (still NO ai_cache reads — every petition letter generated fresh). Frontend modules: `js/eb1a-letter-tool.js` + `js/eb1a-letter-questions.js` + `js/eb2-letter-tool.js` + `js/eb2-letter-questions.js`. Stripe items: `eb1a_letter` ($150), `eb2_letter` ($140). sessionStorage keys: `orabo_eb1a_letter_answers` / `orabo_eb1a_letter_output_type` and `orabo_eb2_letter_answers` / `orabo_eb2_letter_output_type`. Module exports `window.eb1aLetterTool` / `window.eb2LetterTool = { init, start }`. Each generates a 1,500–2,000 word attorney-voice petition letter; no caching; attorney disclaimer appended; DOCX-only download; Brevo delivery + attribute update with `[brevo-eb1a-letter]` / `[brevo-eb2-letter]` / `[brevo-eb1a-letter-attr]` / `[brevo-eb2-letter-attr]` log prefixes; `one_off_purchases` audit row written. Mandatory email validation at section 0 (mirrors the May 11 fix). AI outputs HTML (`<h1>`, `<h2>`, `<h3>`, `<p>`, `<strong>`); code-fence stripping applied before DOCX generation. `css/ibtool.css` extended with Tier 2 modal/screen IDs. Tier 2 cards added to `eb-immigration.html`.
- **Phase EB Tier 3 (EB immigration tools — Tier 3):** `eb1a-package-tool.html` + `eb2-package-tool.html` live. Routes: `routes/eb1a.js` + `routes/eb2.js` each extended with an `eb1a_package` / `eb2_package` outputType branch (NO ai_cache reads — every package generated fresh). Frontend modules: `js/eb1a-package-tool.js` + `js/eb1a-package-questions.js` + `js/eb2-package-tool.js` + `js/eb2-package-questions.js`. Stripe items: `eb1a_package` ($250), `eb2_package` ($240). sessionStorage keys: `orabo_eb1a_package_answers` / `orabo_eb1a_package_output_type` and `orabo_eb2_package_answers` / `orabo_eb2_package_output_type`. Module exports `window.eb1aPackageTool` / `window.eb2PackageTool = { init, start }`. EB-1A package delivers 6 HTML sections: petition letter (1,500–2,000w), criterion scorecard table (all 10 criteria), evidence checklist by exhibit label, expert letter template, filing strategy brief with 5 action items, attorney disclaimer. EB-2 NIW package delivers 7 HTML sections: petition letter, Dhanasar three-prong scorecard, Standalone Proposed Endeavor Statement, evidence checklist by prong (Exhibit A–D), expert letter guidance, filing notes with RFE scenarios, attorney disclaimer. `max_tokens: 10000` for both (up from 8000 for Tier 2). `res.setTimeout(180000)` on both handlers. Code-fence stripping before `generateDocx()`. Brevo delivery + attribute update with `[brevo-eb1a-package]` / `[brevo-eb2-package]` / `[brevo-eb1a-package-attr]` / `[brevo-eb2-package-attr]` log prefixes. `one_off_purchases` audit row written. Question ID prefix isolation: `eb1a_package_q<n>_<slug>` / `eb2_package_q<n>_<slug>` (distinct from Tier 2 prefix). `css/ibtool.css` extended with Tier 3 modal/screen IDs. Tier 3 cards added to `eb-immigration.html`. Customer email safety net in `routes/stripe.js` validates email regex before passing to Stripe (strips invalid, never 500s). `js/stripe.js` `checkPaymentReturn` IIFE bypass list extended to 12 item keys (added `eb1a_package`, `eb2_package`). Subsequent O-1A phase extended the bypass list to 15 item keys.
- **CV + SOP Adjudicator-First Rewrite:** Both tools rewritten with two-pass gatekeeper-then-drafter system prompts, per-country format rules (9 countries + DEFAULT), and SOP-track-specific adjudicator personas + output scaffolds (`utils/document-norms.js`). CV adds an explicit no-fabrication rule: the AI inserts `[INSERT METRIC — e.g. team size, $ figure, % improvement]` placeholders rather than inventing numbers, and flags every placeholder in the Top 5 Priority Actions. SOP retains its existing Rule 1 (no invented experiences) and adds a tightened Orabo Notes scorecard with a verdict line (YES/MAYBE/NO pile). `max_tokens` bumped to 6000 for both routes. `ai_cache` reads and writes **bypassed** in both routes — every CV/SOP is generated fresh. SOP form gains a required `sopTrack` dropdown (nine tracks: Master's admissions, PhD admissions, Chevening, Commonwealth, Fulbright, MasterCard Foundation, DAAD, Other named scholarship, Visa SOP) with a conditional `otherScholarshipName` follow-up textarea. `destinationCountry` dropdown in both CV and SOP forms updated from grouped "Europe" option to nine individual countries (USA, Canada, United Kingdom, Germany, Netherlands, Ireland, Australia, New Zealand, Portugal, Other). Chevening track produces four labelled essays (Leadership and Influence, Networking, Studying in the UK, Career Plan), not one document. MCF track structures the entire essay around the give-back arc. Backend track normalisation maps display strings to internal keys via `normalizeTrack()` in `routes/sop.js`.
- **Email Pipeline Hardening (May 11 2026):** Opportunity submission email flow rebuilt — acknowledgement email fires from `routes/opportunities.js` with submission details table + phone-conditional Post & Earn paragraph; `scheduler.js` publish-notification rewritten with warmer copy and LEFT JOIN to `post_earn_rewards` to surface `approved_count`. Newsletter welcome gate hardened: now suppresses `opportunity_submission` source + only fires on first-time contacts (Brevo 201 status check). Every Brevo fire-and-forget call now logs gate decisions + `[brevo-<context>]` success/error. All transactional email templates updated to use `https://orabo.app/orabo-icon-256x256.png` as hosted logo. PWA icons replaced (`icons/icon-192.png` + `icons/icon-512.png` now correct navy/gold logo). `site.webmanifest` entries corrected to point at `/icons/` paths.
- **Checkout diagnosability (2026-05-11):** `purchaseItem()` catch block in `js/stripe.js` now logs `console.error('[checkout]', err)` before the alert so future failures are immediately identifiable in DevTools. Backend `/api/create-checkout-session` verified working for all standalone tool items (cv_review, cv_cover_letter, cover_letter_only, sop_scratch, sop_review, sop_rewrite, eb1a_assessment, eb2_assessment) — CORS passes, Stripe sessions created, ITEM_PRICES map complete. If "Could not start checkout" reappears: check `[checkout]` in DevTools console → most likely causes are (1) Stripe key rotation needed in Railway, (2) browser ad-blocker blocking fetch to Railway, (3) Railway cold-start timeout.
- **Mandatory email validation at section 0 (2026-05-11):** CV, SOP, EB-1A, EB-2 tool forms now block advancement past section 0 unless `user_email` passes `isValidEmail()` regex (`/^[^\s@]+@[^\s@]+\.[^\s@]+$/` + length bounds 5–254). Inline `.error-text.visible` (existing mechanism) surfaces the message; `.tool-question input.input-error` (added to `css/cvtool.css`) turns the field border red. Error clears on input. Fixes Stripe `email_invalid` 500s previously seen at 18:42 / 18:44 / 18:46 on 11 May where partial values like `"ade"` and `"cc"` bypassed `<input type="email">` soft validation on mobile.
- **Review Screen Polish (all 8 tools):** A review-before-payment screen has been inserted between the last form section and the payment screen on all 8 tools (CV, SOP, EB-1A Tier 1, EB-2 Tier 1, EB-1A Tier 2, EB-2 Tier 2, EB-1A Tier 3, EB-2 Tier 3). Screen IDs: `cv-review-screen`, `sop-review-screen`, `eb1a-review-screen`, `eb2-review-screen`, `eb1a-letter-review-screen`, `eb2-letter-review-screen`, `eb1a-package-review-screen`, `eb2-package-review-screen`. Shared renderer in `js/tool-review.js` (exported `renderReview()`); each module calls a thin wrapper. Last form section's Next button now reads "Review your answers →" (was "Continue to payment →"); payment screen back button returns to review screen (not last form section). Stripe cancel URL for these 12 items (`cv_review`, `cv_cover_letter`, `cover_letter_only`, `sop_scratch`, `sop_review`, `sop_rewrite`, `eb1a_assessment`, `eb2_assessment`, `eb1a_letter`, `eb2_letter`, `eb1a_package`, `eb2_package`) now returns to the tool's own page with `?payment_cancelled=1&item=<key>` — backend `TOOL_CANCEL_PAGES` map in `routes/stripe.js`. `js/stripe.js` `checkPaymentReturn` IIFE extended to preserve URL for `payment_cancelled=1` for those 12 keys. Each tool's `init()` now preserves sessionStorage when either `payment_success=1` or `payment_cancelled=1` is in the URL; on cancel return, answers are restored from sessionStorage and the user lands on the review screen.
- **EB pricing v2 (2026-05-12):** All six EB items repriced ahead of production launch. eb1a_assessment $35→$39, eb2_assessment $29→$35, eb1a_letter $75→$150, eb2_letter $65→$140, eb1a_package $120→$250, eb2_package $99→$240. Tier 1 kept low as funnel-entry; Tier 2 and Tier 3 anchored to attorney-fee-relative value ($4k–$15k market). CV/SOP/consultation pricing unchanged.
- **Phase O-1A (O-1A Extraordinary Ability Visa suite — all 3 tiers):** `o1a-tool.html` + `o1a-letter-tool.html` + `o1a-package-tool.html` live, plus `o1a-visa.html` landing page. Route: `routes/o1a.js` (NEW — mounted at `/api/o1a`); branches on `outputType`: `o1a_assessment` → Tier 1 (plain text/markdown, 8-criterion scorecard), `o1a_letter` → Tier 2 (HTML, petition letter + mandatory peer consultation letter), `o1a_package` → Tier 3 (HTML, 7-section full petition package including Petitioner Support Brief). Petitioner-voice enforcement in Tier 2/3 system prompts bans first-person pronouns referring to the beneficiary. Non-immigrant framing throughout (3-year renewable work visa, not a green card). Frontend modules: `js/o1atool.js` + `js/o1atool-questions.js` + `js/o1a-letter-tool.js` + `js/o1a-letter-questions.js` + `js/o1a-package-tool.js` + `js/o1a-package-questions.js`. Stripe items: `o1a_assessment` ($39), `o1a_letter` ($150), `o1a_package` ($250). sessionStorage keys: `orabo_o1a_answers`/`orabo_o1a_output_type`, `orabo_o1a_letter_answers`/`orabo_o1a_letter_output_type`, `orabo_o1a_package_answers`/`orabo_o1a_package_output_type`. Question ID prefixes: `o1a_q<n>_<slug>`, `o1a_letter_q<n>_<slug>`, `o1a_package_q<n>_<slug>`. Download filenames: `o1a-assessment.docx`, `o1a-petition-letter.docx`, `o1a-full-petition-package.docx`. Brevo delivery + attribute update with `[brevo-o1a]`/`[brevo-o1a-attr]`, `[brevo-o1a-letter]`/`[brevo-o1a-letter-attr]`, `[brevo-o1a-package]`/`[brevo-o1a-package-attr]` prefixes. `css/ibtool.css` extended with all 15 new O-1A screen IDs. `js/stripe.js` `checkPaymentReturn` bypass list extended from 12 to 15 item keys (added `o1a_assessment`, `o1a_letter`, `o1a_package`). `TOOL_CANCEL_PAGES` map in `routes/stripe.js` extended with 3 new O-1A cancel URLs. Nav Tools dropdown + homepage US Immigration tools section updated. Back button on Section 0 of all 3 O-1A tools routes to `/o1a-visa.html`.
- **Back buttons + cancel return for free tools (2026-05-12):** In-tool back buttons added to Document Checklist wizard (Stages 2 and 3 only — Stage 1 has NO back button, only the X close, to avoid blank-page navigation on the bare standalone page) and Readiness Report quiz (dynamically injected `.tool-back-btn#quizTopBack` in `renderQuestion()` on Questions 2–15 only — Question 1 has NO back button, only the X close, same reason). Stage 2 → Stage 1; Stage 3 → Stage 2. Q2–Q15 go to previous question. A `#quizResultsBack` `.tool-back-btn` is also injected as first child of `#quizResultsScreen` by `showResults()` — clicking it hides results, shows `#quizQuestionScreen`, and re-renders Question 15 with the saved answer preserved. `TOOL_CANCEL_PAGES` in `routes/stripe.js` extended with 6 new entries (`document_checklist`, `full_doc_checklist`, `readiness_report`, `consultation_express`, `consultation_full`, `consultation_doc_review`) — now 21 total keys. `js/stripe.js` `checkPaymentReturn` bypass list extended to 21 keys matching. Cancel return state restoration: `payment-return-checklist.js` handles `payment_cancelled=1` (preserves sessionStorage, intercepts autoopen, calls `window.oraboChecklistRestore`, shows amber cancel notice on Stage 3); `payment-return-quiz.js` handles `payment_cancelled=1` (calls `window.oraboQuizRestoreScore`, shows cancel notice above pay button); new `js/payment-return-booking.js` handles consultation cancel return (restores step 1 fields, advances to step 2, shows cancel notice). `js/booking.js` saves `jc_booking_state` to sessionStorage before Stripe redirect. `css/quiz.css` adds `.payment-cancel-notice` (amber/gold, shared by all 3 tools).
- **Payment Integrity (P1-01 + P1-02, 2026-05-12):** New `utils/verify-payment.js` gates all 7 AI generation routes (`cv`, `sop`, `eb1a`, `eb2`, `o1a`, `checklist`, `report`). Verification order: (1) Stripe session — `session.payment_status === 'paid'` AND `metadata.itemType === outputType` AND `one_off_purchases.stripe_session_id` not consumed; (2) Pro subscription (`user_profiles.pro_status === true` AND `pro_expires_at > NOW()`, checklist/report only); (3) Pre-existing `purchases` row with `consumed_at IS NULL` (consumed atomically on first use). New schema: partial unique index `one_off_purchases_session_id_unique` (where `stripe_session_id IS NOT NULL`); new `purchases.consumed_at TIMESTAMPTZ` column. Stripe success URLs now append `&session_id={CHECKOUT_SESSION_ID}` (Stripe-substituted template variable, client cannot forge). All 11 tool modules + 2 payment-return scripts read `session_id` from `URLSearchParams` and forward as `stripe_session_id` in the POST body. Recipient email for Brevo delivery now derived from trusted source (`session.customer_email` or `supabase.auth.admin.getUserById(userId).email`), never from `formAnswers.user_email`. Kill switch: `AI_ROUTES_REQUIRE_STRIPE_SESSION=true` env var on Railway (currently ON; `false` = shadow-mode logging only). Validated end-to-end with real $9 doc_checklist purchase + Stripe sandbox refund.
- **Webhook Hardening (P1-03 + P1-04 + P1-05, 2026-05-13):** `routes/stripe.js` now idempotent via `processed_webhook_events(event_id TEXT PRIMARY KEY, event_type TEXT, processed_at TIMESTAMPTZ)` ledger. After signature verification and before event-type branching, insert `event.id`; on duplicate-key error (`23505`) return 200 immediately. All internal errors inside the webhook handler now return 200 (only signature verification can return 4xx) — prevents Stripe retry storms. `invoice.payment_failed` revokes `pro_status = false` when `attempt_count >= 2` (existing `customer.subscription.deleted` remains as final fallback at end of dunning cycle). New `charge.refunded` handler looks up Checkout Session via `payment_intent`, reads `metadata.userId` + `metadata.itemType`, deletes the matching `purchases` row where `consumed_at IS NULL`; consumed rows left intact. Anonymous flows auto-mitigated by `one_off_purchases.stripe_session_id` single-use ledger from Phase 1. Stripe Dashboard webhook endpoint subscribed to `charge.refunded` (added manually post-deploy). Validated with real Stripe sandbox refund of the $9 doc_checklist purchase — `[stripe-webhook] charge.refunded` log line confirmed. All 5 findings closed — audit recorded in `audit/pass-1-payment-integrity.md` in the backend repo.
- **Phase UKGT Tier 1 (UK Global Talent — Tech Nation Tier 1, 2026-05-13):** `ukgt-tech-tool.html` live, plus `uk-global-talent.html` landing page. Route: `routes/ukgt-tech.js` (NEW — mounted at `/api/ukgt-tech`); single `outputType` branch: `ukgt_tech_assessment` → plain text/markdown, Tech Nation criteria scorecard. `sanitizeBrand()` defined inline. UK English rules enforced in system prompt. `max_tokens: 8000`. `res.setTimeout(180000)` + `req.setTimeout(180000)`. NO ai_cache reads — every generation is fresh. verifyPayment + gate before Claude call. Brevo delivery + attribute update fire-and-forget. `one_off_purchases` audit row written. `[brevo-ukgt-tech]`/`[brevo-ukgt-tech-attr]` log prefixes. Frontend module: `js/ukgt-tech-tool.js` + `js/ukgt-tech-assessment-questions.js`. Stripe item: `ukgt_tech_assessment` ($39). sessionStorage keys: `orabo_ukgt_tech_answers`/`orabo_ukgt_tech_output_type`. Question ID prefix: `ukgt_tech_q<n>_<slug>`. Download filename: `ukgt-tech-nation-eligibility-assessment.docx`. Download file key: `ukgt_tech_assessment_docx`. Section 0 back button → `/uk-global-talent.html`. Review screen between last form section and payment. DOCX-only download. `js/stripe.js` bypass list extended from 21 to 22 keys (added `ukgt_tech_assessment`). `TOOL_CANCEL_PAGES` in `routes/stripe.js` extended with `ukgt_tech_assessment` → `ukgt-tech-tool.html`. `routes/newsletter.js` SUPPRESSED_SOURCES extended with `ukgt_waitlist`. `css/ibtool.css` extended with `#ukgt-tech-tool-modal` + 5 screen IDs. Nav Tools dropdown + homepage Immigration tools section updated; section tag renamed "Immigration Tools".
- **Phase UKGT Tier 2 (UK Global Talent — Tech Nation Tier 2, 2026-05-13):** `ukgt-tech-statement-tool.html` live. Route: `routes/ukgt-tech.js` extended with a `ukgt_tech_statement` outputType branch (HTML output — first-person personal statement + 3 distinct-voice recommendation letter templates + Plan for the UK one-pager, all in one DOCX). System prompts extracted to `routes/ukgt-tech-prompts.js` (module exports `UKGT_TECH_SYSTEM_PROMPT` + `UKGT_TECH_STATEMENT_SYSTEM_PROMPT`) to keep `ukgt-tech.js` under 600 lines. Two-pass adjudicator + applicant-voice output. Word-count guardrail: `_countPersonalStatementWords()` counts personal statement section words; if >1,200 words, one retry with stricter instruction appended; accepted even if still over limit after retry; logs `[ukgt-tech-statement] word-count retry`. Code-fence stripping before `generateDocx()`. `max_tokens: 8000`. `res.setTimeout(180000)` + `req.setTimeout(180000)`. NO ai_cache reads. verifyPayment gate before Claude call. `one_off_purchases` audit row with `stripe_session_id`. Brevo delivery (4-component list + Tier 3 upsell) + attribute update (`LAST_TOOL: 'ukgt_tech_statement'`) fire-and-forget. `[brevo-ukgt-tech-statement]`/`[brevo-ukgt-tech-statement-attr]` log prefixes. Response: `{ success, result, files: { ukgt_tech_statement_docx }, firstName }`. Tier 1 email upsell updated to link `/ukgt-tech-statement-tool.html`. Frontend modules: `js/ukgt-tech-statement-tool.js` + `js/ukgt-tech-statement-questions.js`. Stripe item: `ukgt_tech_statement` ($150). sessionStorage keys: `orabo_ukgt_tech_statement_answers`/`orabo_ukgt_tech_statement_output_type`. Question ID prefix: `ukgt_tech_statement_q<n>_<slug>` (isolated from Tier 1 prefix). 6 sections: Section 0 "Your details" + Identity & Career Snapshot + Recognition Evidence + Founder/Employee Innovation & Sector Contribution + Plan for the UK + Recommenders & Referees (22 questions total). Q5 track uses `{ value, label }` option objects — `_renderSelect` handles both formats. Download filename: `ukgt-tech-personal-statement-package.docx`. Download file key: `ukgt_tech_statement_docx`. Section 0 back button → `/uk-global-talent.html`. Review screen between last form section and payment. `js/stripe.js` bypass list extended from 22 to 23 keys (added `ukgt_tech_statement`). `TOOL_CANCEL_PAGES` in `routes/stripe.js` extended with `ukgt_tech_statement` → `ukgt-tech-statement-tool.html` (now 23 keys total). `css/ibtool.css` extended with `#ukgt-tech-statement-tool-modal` + 5 screen IDs + review/payment/download layout selectors. `uk-global-talent.html`: Tier 2 card changed from "Soon" to "LIVE"; hero CTA updated; waitlist heading updated to reflect only Tier 3 pending; FAQ updated to link directly to Tier 2 tool.
- **Save & Resume Draft (all 14 paid tools, 2026-05-15):** Users can leave any of the 14 paid tool forms mid-way, close the tab, and resume from where they left off on next visit. Two new shared ES modules: `js/draft-store.js` (exports `saveDraft`, `loadDraft`, `clearDraft`, `hasDraft`, `listAllDrafts` — localStorage, 30-day TTL, version field `DRAFT_VERSION = 1`, key prefix `orabo_draft_<toolKey>`, all calls wrapped in try/catch for Safari private mode safety) and `js/draft-banner.js` (exports `showResumeBanner({ container, toolLabel, savedAt, onResume, onStartFresh })` — inserts `.draft-resume-banner` as `container.firstChild`, relative time display, both buttons via `addEventListener`, CSP-compliant). Banner CSS in `css/cvtool.css` (`.draft-resume-banner`, `.draft-resume-banner-title`, `.draft-resume-banner-meta`, `.draft-resume-banner-actions`, `.draft-resume-btn`, `.draft-start-fresh-btn`). All 14 tool modules import both new modules and define `TOOL_KEY` + `TOOL_LABEL` constants. `saveCurrentAnswers()` in each module calls `saveDraft()` on every section navigation. `showDownloadScreen()` calls `clearDraft()` on successful generation. `init()` shows the resume banner when a valid draft exists and no payment URL param is present. Tools scope: CV (`cv`), SOP (`sop`), EB-1A Tier 1 (`eb1a`), EB-2 Tier 1 (`eb2`), EB-1A Tier 2 (`eb1a_letter`), EB-2 Tier 2 (`eb2_letter`), EB-1A Tier 3 (`eb1a_package`), EB-2 Tier 3 (`eb2_package`), O-1A Tier 1 (`o1a`), O-1A Tier 2 (`o1a_letter`), O-1A Tier 3 (`o1a_package`), UKGT Tier 1 (`ukgt_tech`), UKGT Tier 2 (`ukgt_tech_statement`), UKGT Tier 3 (`ukgt_tech_package`). Excluded: Document Checklist, Readiness Quiz, Consultation Booking. Draft auto-cleared on successful generation (`showDownloadScreen`). sessionStorage payment-return bridge unchanged — localStorage draft is a separate layer on top.
- **Free Preview Verdict (8 high-priced tools, 2026-05-15):** A free email-gated preview screen inserted between the review screen and the payment screen on all 8 paid tools priced $140–$250 (EB-1A Letter, EB-2 Letter, EB-1A Package, EB-2 Package, O-1A Letter, O-1A Package, UKGT Tech Statement, UKGT Tech Package). Backend: `routes/preview.js` (NEW — mounted at `/api/preview`, own `previewLimiter` — 3 per IP per 24h, `standardHeaders: true`, env-overridable via `PREVIEW_RATE_LIMIT_WINDOW_HOURS` + `PREVIEW_RATE_LIMIT_MAX`). Prompt files split by visa family: `routes/preview-prompts-eb.js` (4 prompts), `routes/preview-prompts-o1a.js` (2 prompts), `routes/preview-prompts-ukgt.js` (2 prompts). Each prompt enforces a CRITICAL GUARDRAILS block + 4-block output structure (`**Verdict**` / `**Strongest evidence**` / `**Critical weakness**` / `**What the full document delivers**`). Claude call: `claude-sonnet-4-6`, `max_tokens: 400`, `res.setTimeout(60000)`. NO `verifyPayment()` — preview is free. NO `ai_cache` writes. Brevo fire-and-forget sets `LAST_PREVIEW_TOOL` (text) + `LAST_PREVIEW_DATE` (date) on the contact; `[brevo-preview]` log prefix. Frontend: `js/preview-screen.js` (NEW shared ES module — exports `showPreviewScreen(opts)`; renders 5 states: A email-capture, B loading, C preview result, D rate-limited, E error). `js/draft-store.js` DRAFT_VERSION bumped 1→2; `saveDraft`/`loadDraft` extended with `lastPreview` + `lastPreviewEmail` fields (v1 drafts silently cleared on version mismatch). `css/ibtool.css` appended with 8 preview screen `.active` rules + ~70 lines preview block CSS. All 8 tool modules: import `showPreviewScreen` from `./preview-screen.js`; `SCREENS` constant extended with `<prefix>-preview-screen` between review and payment; `SOFT_SELL_TEXT` constant; `showPreviewScreen_()` wrapper; `renderXxxReview` onContinue now calls `showPreviewScreen_()`; payment back button now goes to preview screen (not review screen); `saveCurrentAnswers()` preserves `lastPreview`/`lastPreviewEmail` via `loadDraft`. Skip button logs `[preview-skip] <outputType>` then proceeds to payment. Cached preview (draft.lastPreview) skips the Claude call entirely on re-visit.
- **Migration Worth It Calculator (2026-05-15):** `migration-worth-it.html` live — free, public, SEO-indexed tool at `/migration-worth-it.html`. No auth, no payment. Rate-limited 5/IP/24h (env-overridable via `WORTHIT_RATE_LIMIT_WINDOW_HOURS` + `WORTHIT_RATE_LIMIT_MAX`). Single-page form: origin country, current salary (auto-currency by country), destination country, current role, target role, age range, family situation, optional email. Backend: `routes/migration-worth-it.js` (NEW, route-scoped `worthitLimiter`) + `utils/worthit-data.js` (NEW, all data constants) + `routes/migration-worth-it-prompts.js` (NEW). Math: 5-year net gain = (target disposable − current disposable) × 5 − migration cost. Verdict: `yes_clear` / `yes_marginal` / `maybe` / `marginal_no` / `no_dont`. AI commentary: `claude-haiku-4-5-20251001`, `max_tokens: 250`, `res.setTimeout(30000)` — first Haiku use in Orabo. Tool recommendation: deterministic lookup based on (targetCountry, targetRole) — USA+HIGH_ACH→EB-1A, USA+TECH→O-1A, USA+other→EB-2, UK+TECH→UKGT, TRADES→document_checklist, else→CV Review. Brevo fire-and-forget sets `WORTHIT_TARGET` + `WORTHIT_VERDICT` on contact; `[brevo-worthit]` log prefix. Shareable result URLs via `?result=<base64>` param; Twitter/X, WhatsApp, clipboard share buttons. `css/worthit.css` (NEW), `js/worthit.js` (NEW ES module — imports ROLES/ROLE_GROUPS from paycalc-data.js, API_URL from config.js). NO `verifyPayment()`, NO `ai_cache`. Homepage callout band (`.worthit-callout-band`) added between hero and features strip. "Migration Worth It Calculator" added as first item in nav Tools dropdown. `sitemap.xml` entry added (priority 0.9). Brevo custom attributes required: `WORTHIT_TARGET` (text), `WORTHIT_VERDICT` (text).
- **Dashboard Phase 5 (Preferences Modal + Retake Quiz, 2026-05-16):** Users can now set their migration preferences (destination, pathway, field, optional target_date) from the journey-home panel. Backend: `POST /api/dashboard/preferences` added to `routes/dashboard.js` — JWT-gated via `resolveAuthUser`, feature-flagged behind `DASHBOARD_V2_ENABLED`, validates against `ALLOWED_DESTINATIONS` (10), `ALLOWED_PATHWAYS` (13), `ALLOWED_FIELDS` (8), writes to `user_profiles.user_preferences`, fire-and-forget Brevo sync sets `DESTINATION`, `PREF_PATHWAY`, `PREF_FIELD` attributes. Frontend: `#prefs-modal` added to `dashboard.html` — visibility via `body.preferences-modal-open .prefs-modal { display: flex }` CSS class toggle (CSP-compliant, no inline styles). New functions in dashboard.html inline script: `openPreferencesModal(prefill)`, `closePreferencesModal()`, `handlePreferencesSubmit(e)` (JWT read via localStorage scan — same pattern as dashboard-journey.js `readJwt()`), `handleRetakeQuiz()` (sets `sessionStorage.orabo_retaking_quiz='1'` then `window.location.href='/#readiness'` — top-frame navigation preserves sessionStorage). `wireJourneyCtas()` extended to wire `data-cta="open-preferences"` button and add delegated listener on `#dash-journey-panel` for `data-action="open-prefs-edit"` and `data-action="retake-quiz"`. `css/dashboard-preferences.css` (NEW) — modal styles, 480px mobile breakpoint, `.dj-pathway-edit-btn` gear icon. `js/dashboard-journey.js` changes: `window._oraboLastJourneyState = state` stored after successful fetch; `window.loadJourneyState` canonical alias added; `renderPathway` ready state gear button uses `data-action="open-prefs-edit"`; `renderQuizScore` ready state retake button uses `data-action="retake-quiz"` (direct wiring removed, handled by delegated listener). Retake flow: `js/quiz-retake-handler.js` (NEW non-module IIFE, loaded in `index.html` BEFORE `<script type="module">`) — cleanup-first wipes `orabo_quiz_saved`/`jc_quiz_state`/`jc_quiz_origin`, detects `orabo_retaking_quiz='1'`, consumes flag, scrolls to `#readiness`, clicks `#startQuizBtn` after 200ms to allow ES module quiz.js to finish binding. `PREF_PATHWAY` and `PREF_FIELD` are new Brevo text attributes — create in Brevo dashboard before first save.
- **Phase UKGT Peer Review Tier 1 (UK Global Talent — Peer Review Tier 1, 2026-05-17):** `ukgt-research-tool.html` live. Route: `routes/ukgt-research.js` (NEW — mounted at `/api/ukgt-research`); single outputType branch: `ukgt_research_assessment` → plain text/markdown, Peer Review eligibility assessment. System prompt in `routes/ukgt-research-prompts.js` (exports `UKGT_RESEARCH_SYSTEM_PROMPT`). Endorsing body routing: RS (natural/medical/research-CS), RAEng (all engineering), BA (humanities/social sciences). CS/SE disambiguation in system prompt with verbatim worked example (fintech SE → Tech Nation; Computational Biology faculty → RS). Mandatory gates: PhD (or equivalent 5-year industrial research) + UK-resident eminent recommender (FRS/FREng/FBA/FMedSci). Track election (Talent vs Promise) from career stage + Q6–Q9. Gap-disclosure no-fabrication rule (no [INSERT:] placeholders — state gaps explicitly in verdict). `sanitizeBrand()` defined inline. UK English mandatory. `max_tokens: 8000`. `res.setTimeout(180000)` + `req.setTimeout(180000)`. NO ai_cache reads — every generation is fresh. verifyPayment + gate before Claude call. Brevo delivery `_sendUkgtResearchEmail(userEmail, userName, docxBase64, resultText)` — upsell paragraph fires ONLY when `resultText.includes('STRONG — READY TO APPLY')`, links to `/ukgt-research-statement-tool.html`; UK English throughout email. `[brevo-ukgt-research]`/`[brevo-ukgt-research-attr]` log prefixes. `one_off_purchases` audit row with `stripe_session_id: null`. Response: `{ success, result, files: { ukgt_research_assessment_docx }, firstName }`. Frontend module: `js/ukgt-research-tool.js` + `js/ukgt-research-assessment-questions.js`. Stripe item: `ukgt_research_assessment` ($39). sessionStorage keys: `orabo_ukgt_research_answers`/`orabo_ukgt_research_output_type`. TOOL_KEY: `ukgt_research`. `const EMAIL_Q_ID = 'ukgt_research_q0_user_email'` used throughout (cleaner than hardcoded strings). Question ID prefix: `ukgt_research_q<n>_<slug>`. 5 sections (Section 0 + A–D): S0 name+email; A discipline/career-stage/PhD; B UK recommender availability + path; C research outputs/metrics/funding/fellowships; D UK research plan/contribution/affiliation. 14 questions total. Download filename: `ukgt-peer-review-eligibility-assessment.docx`. Download file key: `ukgt_research_assessment_docx`. Section 0 back button → `/uk-global-talent.html`. Review screen between last form section and payment. Save & Resume Draft with TOOL_KEY `ukgt_research`. `js/stripe.js` bypass list extended from 24 to 25 keys (added `ukgt_research_assessment`). `TOOL_CANCEL_PAGES` in `routes/stripe.js` extended with `ukgt_research_assessment` → `ukgt-research-tool.html` (now 25 keys total). `css/ibtool.css` extended with `#ukgt-research-tool-modal` + 5 screen IDs in all 11 selector blocks. `uk-global-talent.html`: Peer Review section tag updated (removed "— Coming Soon"); Tier 1 card changed from COMING SOON to LIVE with `.tier-card.live`, `.tier-live-badge`, link to `/ukgt-research-tool.html`; Tiers 2 and 3 remain COMING SOON.
- **Phase UKGT Peer Review Tier 2 (UK Global Talent — Peer Review Tier 2, 2026-05-17):** `ukgt-research-statement-tool.html` live. Route: `routes/ukgt-research.js` extended with `ukgt_research_statement` outputType branch (HTML output — personal statement ~1,000 words first-person + UK eminent recommender letter template + two international peer letter templates + Plan-for-the-UK research statement, all in one DOCX). System prompt `UKGT_RESEARCH_STATEMENT_SYSTEM_PROMPT` added to `routes/ukgt-research-prompts.js` (module now exports both prompts). Two-pass adjudicator + applicant-voice output. Eligibility Advisory Header Rule: header inserted ONLY when Q5 unambiguously states Tier 1 returned NOT YET ELIGIBLE (user's amendment verbatim). Word-count guardrail: `_countPersonalStatementWords()` (copied from `ukgt-tech.js`); if >1,200 words one retry with stricter instruction; logs `[ukgt-research-statement] word-count retry`. Code-fence stripping before `generateDocx()`. `max_tokens: 8000`. NO ai_cache reads. verifyPayment gate before Claude call. Brevo delivery `_sendUkgtResearchStatementEmail()` — subject "Your UK Global Talent Peer Review Personal Statement Package from Orabo", 4-component list, Tier 3 upsell → `/ukgt-research-package-tool.html`. `[brevo-ukgt-research-statement]`/`[brevo-ukgt-research-statement-attr]` log prefixes. `one_off_purchases` audit row. Response: `{ success, result, files: { ukgt_research_statement_docx }, firstName }`. Tier 1 `_sendUkgtResearchEmail()` upsell updated: links to `/ukgt-research-statement-tool.html`. Frontend modules: `js/ukgt-research-statement-tool.js` + `js/ukgt-research-statement-questions.js`. Stripe item: `ukgt_research_statement` ($150). sessionStorage keys: `orabo_ukgt_research_statement_answers`/`orabo_ukgt_research_statement_output_type`. TOOL_KEY: `ukgt_research_statement`. Question ID prefix: `ukgt_research_statement_q<n>_<slug>` (isolated from Tier 1). 6 sections (Section 0 + A–E): S0 name+email; A research identity + career narrative + PhD + track election + optional Tier 1 verdict (Q5); B key publications + grants + metrics + prizes; C endorsing body + UK recommender identity + relationship + specific knowledge; D peer recommender 1 + peer recommender 2; E UK plan + contribution + affiliation. 24 questions total (Q5 optional). NO preview screen — review screen → payment directly. Download filename: `ukgt-peer-review-personal-statement-package.docx`. Download file key: `ukgt_research_statement_docx`. Section 0 back button → `/uk-global-talent.html`. `js/stripe.js` bypass list extended from 25 to 26 keys (added `ukgt_research_statement`). `TOOL_CANCEL_PAGES` in `routes/stripe.js` extended with `ukgt_research_statement` → `ukgt-research-statement-tool.html` (now 26 keys total). `css/ibtool.css` extended with `#ukgt-research-statement-tool-modal` + 5 screen IDs in all 11 selector blocks. `uk-global-talent.html`: Peer Review Tier 2 card changed from COMING SOON to LIVE; waitlist heading updated to "Peer Review Tier 3 coming soon — join the waitlist".
- **Phase UKGT Peer Review Tier 3 (UK Global Talent — Peer Review Tier 3, 2026-05-18):** `ukgt-research-package-tool.html` live. Completes the 6-tool UKGT suite (Tech Nation × 3 + Peer Review × 3). Route: `routes/ukgt-research.js` extended with `ukgt_research_package` outputType branch (HTML output — 7-component full endorsement package delivered as one DOCX). System prompt `UKGT_RESEARCH_PACKAGE_SYSTEM_PROMPT` added to `routes/ukgt-research-prompts.js` (module now exports all 3 prompts). Two-pass adjudicator + drafter output. **7 output sections:** (1) Endorsement Readiness Scorecard (HTML table — PhD, UK recommender, research output, UK plan, overall verdict); (2) Personal Statement (~1,000 words, first-person, UK academic English, ABSOLUTE MAX 1,200 words); (3) Three Recommendation Letter Templates — UK eminent letter (600–800 words, ≥3 named outputs, drafted with extra care) + two international peer letters (450–600 words each, distinct voices and evidence dimensions); (4) Recommender Briefing Strategy — sample request emails + briefing packs for all 3 recommenders; (5) Evidence Portfolio Map — up to 10 documents from Q23 inventory with obtain-by guidance and `[TO OBTAIN]` flags; (6) Plan for the UK Expanded — named institution/collaborators, field-specific UK contribution argument; (7) Stage 2 Visa Application Checklist — post-endorsement administrative steps. **Eligibility Advisory Header** (inline `style=` div, renders in DOCX only — no CSP concern): triggers when (a) Q5 explicitly states NOT YET ELIGIBLE, (b) Q3 indicates no PhD and no 5-year industrial equivalent, or (c) Q11 indicates no UK recommender AND Q24 has no credible strategy — generation never blocked, all 7 sections produced regardless. Word-count guardrail: `_countPersonalStatementWords()` (regex stops at next `<h1>`, verified correct for 7-section output); if >1,200 words, one retry; logs `[ukgt-research-package] word-count retry`. Code-fence stripping before `generateDocx()`. `max_tokens: 10000` (up from 8000 for T2 — largest output in the suite). `res.setTimeout(180000)` + `req.setTimeout(180000)`. NO ai_cache reads. verifyPayment gate before Claude call. Added `_sendUkgtResearchPackageEmail(userEmail, userName, docxBase64)` — subject "Your UK Global Talent Peer Review Full Endorsement Package from Orabo", 7-bullet HTML list matching 7 output sections, consultation upsell to `/consult-booking.html`. T2 email upsell updated from "Tier 3 — Coming Soon" to live link. `[brevo-ukgt-research-package]`/`[brevo-ukgt-research-package-attr]` log prefixes. `one_off_purchases` audit row with `stripe_session_id`. Response: `{ success, result, files: { ukgt_research_package_docx }, firstName }`. Stripe item: `ukgt_research_package` ($250). `ITEM_PRICES`: 25000. `TOOL_CANCEL_PAGES`: `ukgt_research_package` → `ukgt-research-package-tool.html` (now 27 keys total). Frontend modules: `js/ukgt-research-package-tool.js` + `js/ukgt-research-package-questions.js`. sessionStorage keys: `orabo_ukgt_research_package_answers`/`orabo_ukgt_research_package_output_type`. TOOL_KEY: `ukgt_research_package`. Question ID prefix: `ukgt_research_package_q<n>_<slug>`. **7 sections, 27 form inputs (25 content + 2 detail):** S0 name+email; S1 Research Identity & Career Narrative (Q1–Q5, Q5 optional Tier 1 verdict); S2 Research Achievements & Impact (Q6–Q10); S3 UK Eminent Recommender (Q11–Q14); S4 International Peer Recommenders (Q15–Q18); S5 UK Research Plan & Application Readiness (Q19–Q21 verbatim from T2 + NEW Q22 preemptive_weakness, required: false); S6 Evidence Portfolio, Recommender Logistics & Timeline (Q23 evidence_inventory required: true, Q24 uk_recommender_strategy required: false, Q25 target_timeline type: textarea required: false). 8 loading messages. Payment screen: "Peer Review Tier 3 — Full Endorsement Package", $250, 8-item features list. Download filename: `ukgt-peer-review-full-endorsement-package.docx`. Download file key: `ukgt_research_package_docx`. Section 0 back button → `/uk-global-talent.html`. Review screen between last form section and payment. Consultation upsell on download screen. Window export: `window.ukgtResearchPackageTool = { init, start }`. Autoopen: `js/autoopen-ukgt-research-package.js`. `js/stripe.js` bypass list extended from 26 to 27 keys (added `ukgt_research_package`). `css/ibtool.css` extended with `#ukgt-research-package-tool-modal` + 5 screen IDs in all 9 selector blocks (575 lines, under 600). `uk-global-talent.html`: Peer Review T3 card changed from COMING SOON to LIVE with link to `/ukgt-research-package-tool.html`; hero paragraph updated (Peer Review track no longer "coming soon"); track card CTA updated to link `#peer-review-tools`; Peer Review section description updated; waitlist heading changed to "More endorsement routes coming soon"; FAQ "What happens after the eligibility assessment?" updated with Peer Review T2/T3 links.
- **Phase Sources Part 1 (numeric data tools, 2026-05-17):** Shared `js/sources.js` exports `SOURCES` map (87 entries: 36 paycalc + 33 costliving + 18 visatracker) and sets `window.OraboSources` for the non-module IIFE. Data split across `js/sources-paycalc.js` (18 salary entries `orabo_estimate` + 18 tax entries `public_domain`), `js/sources-costliving.js` (33 city entries `orabo_estimate`), `js/sources-visatracker.js` (18 visa entries `public_domain`). Each entry has `source_name`, `source_url`, `retrieved_on`, `verified: '2026-05-17'`, `methodology`, `license_note`, and `granularity`. Non-module `js/sources-modal.js` IIFE registers click delegation on `.source-tag[data-source-key]`; accesses `window.OraboSources` lazily at click time (not at init) to avoid module-deferred-execution race. Renders `#sourceModalOverlay` with full citation detail. CSS in `css/sources.css` imported after `mobile.css` in `styles.css`. Source tags rendered by: `paycalc.js renderResult()` (2 tags — salary + tax); `costliving.js renderSingleCity()` (1 tag) and `renderCompareModal()` (1 tag per city column); `visatracker.js renderDetail()` (1 tag). `compare-cities.html` and `all-countries-pay.html` also render source tags inline. `index.html` loads `sources-modal.js` with `defer` after `main.js`; `main.js` imports `sources.js` first. `all-countries-pay.html` inline module imports `./js/sources.js`. AI document citation footers shipped as Part 2 (see below). Verification rule: no `SOURCES` entry committed without an opened authoritative URL and an ISO `verified` date in the same commit. **Frontend sources infrastructure lives at `js/sources.js` (not `utils/sources.js`) — frontend repo convention is `js/`.**
- **Phase Sources Part 2 (AI document citation footers, 2026-05-18):** Every paid AI-generated document now ends with a `## Sources & Authorities` section citing verified legal/regulatory authorities. Backend only — no frontend changes. Routes updated: `routes/cv.js` (career-service authority per `destinationCountry`, 9-country map + fallback); `routes/sop.js` (scholarship authority from `track.citation_authority` in `utils/document-norms.js`, or methodology line for non-scholarship tracks); `routes/eb1a.js` (8 CFR §204.5(h), USCIS PM Vol 6 Part F Ch 2, Kazarian v. USCIS — all 3 tiers); `routes/eb2.js` (8 CFR §204.5(k), INA §203(b)(2)(B), Matter of Dhanasar, USCIS PM Vol 6 Part F Ch 5 — all 3 tiers); `routes/o1a.js` + new `routes/o1a-prompts.js` (INA §101(a)(15)(O), 8 CFR §214.2(o), USCIS PM Vol 2 Part M Ch 4 — all 3 tiers; o1a.js was 684 lines, extracted to o1a-prompts.js to comply with 600-line rule); `routes/ukgt-tech-prompts.js` + `routes/ukgt-tech.js` (gov.uk/global-talent, technation.io — all 3 tiers); `utils/checklist-knowledge.js` (`CHECKLIST_PROMPT_VERSION` bumped to `v2.1-2026-05-18`, `buildKnowledgeBlock()` extended with item 9 appending HTML Sources section using `source_url` from pathway data + HCCH Apostille status for origin); `utils/checklist-pathway-data.js` (`source_url` field added to all 13 `PATHWAY_KNOWLEDGE_BASE` entries); `utils/document-norms.js` (`citation_authority` field added to all 9 `SOP_TRACK_SCAFFOLDS` entries — chevening/commonwealth/fulbright/mcf/daad get authority objects; masters_admissions/phd_admissions/other_scholarship/visa_sop get null); `routes/report.js` (item 15 added to `REPORT GENERATION INSTRUCTIONS` instructing Claude to append destination-filtered Sources & Authorities HTML). USCIS PM chapter URLs (Vol 6 Part F Ch 2, Vol 6 Part F Ch 5, Vol 2 Part M Ch 4) are canonical-pattern-confirmed; content not web-fetched due to Cloudflare bot detection. Post-deploy: `DELETE FROM ai_cache WHERE cache_key LIKE 'report|%'` to clear stale report cache (checklist auto-invalidated by version bump).
- **Phase UKGT Tier 3 (UK Global Talent — Tech Nation Tier 3, 2026-05-13):** `ukgt-tech-package-tool.html` live. Route: `routes/ukgt-tech.js` extended with a `ukgt_tech_package` outputType branch (HTML output — 7-component full endorsement package: Endorsement Readiness Scorecard, Personal Statement ~1,000 words, 3 Recommendation Letter Templates, Recommender Briefing Strategy with sample request emails + briefing packs, Evidence Portfolio Map for up to 10 documents with [TO OBTAIN] flags, Plan for the UK Expanded, Stage 2 Visa Application Checklist). `UKGT_TECH_PACKAGE_SYSTEM_PROMPT` added to `routes/ukgt-tech-prompts.js` (module now exports all 3 prompts) to keep `ukgt-tech.js` under 600 lines. Two-pass adjudicator + drafter output. Word-count guardrail: same `_countPersonalStatementWords()` pattern; if >1,200 words, one retry with stricter instruction; logs `[ukgt-tech-package] word-count retry`. Code-fence stripping before `generateDocx()`. `max_tokens: 10000` (up from 8000 for Tier 2 — Tier 3 is the largest output). `res.setTimeout(180000)` + `req.setTimeout(180000)`. NO ai_cache reads. verifyPayment gate before Claude call. `one_off_purchases` audit row with `stripe_session_id`. Brevo delivery (7-component list + Peer Review upsell) + attribute update (`LAST_TOOL: 'ukgt_tech_package'`) fire-and-forget. `[brevo-ukgt-tech-package]`/`[brevo-ukgt-tech-package-attr]` log prefixes. Response: `{ success, result, files: { ukgt_tech_package_docx }, firstName }`. Frontend modules: `js/ukgt-tech-package-tool.js` + `js/ukgt-tech-package-questions.js`. Stripe item: `ukgt_tech_package` ($250). sessionStorage keys: `orabo_ukgt_tech_package_answers`/`orabo_ukgt_tech_package_output_type`. Question ID prefix: `ukgt_tech_package_q<n>_<slug>` (isolated from Tier 1 and Tier 2 prefixes). 7 sections: Section 0 "Your details" + Identity & Career Snapshot + Recognition Evidence + Founder/Employee Innovation & Sector Contribution + Plan for the UK + Recommenders & Referees + Evidence Portfolio, Recommender Logistics & Timeline (25 questions total). Download filename: `ukgt-tech-full-endorsement-package.docx`. Download file key: `ukgt_tech_package_docx`. Section 0 back button → `/uk-global-talent.html`. Review screen between last form section and payment. `js/stripe.js` bypass list extended from 23 to 24 keys (added `ukgt_tech_package`). `TOOL_CANCEL_PAGES` in `routes/stripe.js` extended with `ukgt_tech_package` → `ukgt-tech-package-tool.html` (now 24 keys total). **Phase UKGT Peer Review Tier 1** then extended bypass list to 25 keys and TOOL_CANCEL_PAGES to 25 keys. `css/ibtool.css` extended with `#ukgt-tech-package-tool-modal` + 5 screen IDs + review/payment/download layout selectors. `uk-global-talent.html`: Tier 3 card changed from "Coming Soon" to "LIVE"; hero updated to celebrate complete Tech Nation track; waitlist heading updated to Peer Review only; FAQ updated to link Tier 3.
- **Inline Script Extraction (CSP, 2026-05-18):** All 17 standalone tool pages migrated from inline `<script>` boot blocks to external `js/autoopen-<tool>.js` files. Triggered by production fire where Cloudflare-enforced strict CSP (`_headers` line 2, no `'unsafe-inline'`) blocked every inline polling script that auto-opens modal on tool pages — local dev server doesn't apply `_headers` so the regression was invisible until live. Commit `62b94fd`: 17 new `js/autoopen-*.js` files (cv, sop, consult, eb1a, eb2, eb1a-letter, eb2-letter, eb1a-package, eb2-package, o1a, o1a-letter, o1a-package, ukgt-tech, ukgt-tech-statement, ukgt-tech-package, ukgt-research, ukgt-research-statement) + 17 HTML edits replacing inline blocks with `<script src="js/autoopen-<tool>.js"></script>` at end of `<body>`. Canonical patterns: `js/autoopen-cv.js` (DOMContentLoaded wrapper), `js/autoopen-eb1a.js` (bare IIFE polling), `js/autoopen-consult.js` (complex IIFE with emailjs.init + modalClose listener + 300ms `.consult-btn` auto-click). `_headers` CSP unchanged — code conformed to policy, not the reverse. New Coding Rule added (see `**Standalone tool page boot scripts MUST be external files (CSP)**` below). The 2 UKGT Research pages were not in the original task scope but had identical inline blocks; included in the same commit to prevent follow-up hotfix.
- **Phase Petition Audit (O-1A, 2026-05-20 — code-complete; pending live e2e validation):** `o1a-audit-tool.html` + backend `/api/audit` (commit adcfcda) + frontend (commit a1414c8). This is a **REVIEWER tool**, not a generator: the buyer already has a draft O-1A petition (from a lawyer or from Orabo Tier 2/3 tools) and uploads it for risk review. Distinct buyer persona and product position from the three O-1A generate tools. **Adversarial dual-persona prompt:** USCIS Adjudicating Officer (RFE-writing, 7-year NSSC veteran) + Senior Reviewing Immigration Attorney (second-opinion remediation) — NOT the generate-tool adjudicator→drafter pair; the officer reviews the petition as-submitted to find its weakest points, the attorney then rebuts each flag and prioritises fixes. **Backend:** shared route `routes/audit.js` mounted at `/api/audit` with its own `express.json({ limit: '8mb' })` before the global 20 kb parser (base64 payloads require the higher limit); dispatches by `family` field (`'o1a'`, `'eb1a'`, `'eb2'` all live; `'ukgt'` is a future port). System prompts in `routes/audit-prompts.js` keyed by family; `runAudit({ family, text })` exported for offline calibration. **Ingestion:** `utils/document-ingest.js` — `parseUploadedDocument` handles mammoth (DOCX), pdf-parse v2 (PDF, pinned `pdf-parse@^2.4.5` — v2 uses `new PDFParse({data}).getText()` class API), and paste (text); `sanitizeForPrompt` strips `<uploaded_petition>` delimiter tags (case-insensitive) before passing untrusted text into the prompt; `IngestError` thrown on encrypted/scanned PDFs; `MAX_WORDS = 25000` hard cap. New backend deps: `mammoth`, `pdf-parse@^2.4.5`. **Output:** 9-section HTML — (1) Overall Posture band (never a numeric %: FILING-READY / FILING-READY WITH VULNERABILITIES / HIGH RFE RISK / LIKELY INSUFFICIENT), (2) Criteria Compliance table (all 8 O-1A criteria), (3) Threshold Check, (4) Final Merits Read, (5) Flagged Passages, (6) Structural Compliance, (7) Ranked Remediation Plan (P1/P2/P3), (8) Recommended Next Step, (9) Scope & Disclaimer. HTML → code-fence strip → `generateDocx()`. Static audit disclaimer appended before docgen. NO `ai_cache` reads/writes. **verifyPayment** gate (Stripe item `o1a_audit`); replay protection owned by `verify-payment.js` single-use `stripe_session_id` ledger; trusted recipient email = `session.customer_email`, `body.user_email` fallback only. Brevo fire-and-forget delivery + attribute update (`LAST_TOOL: 'o1a_audit'`) with `[brevo-o1a-audit]`/`[brevo-o1a-audit-attr]` log prefixes. `one_off_purchases` audit row written. **Frontend:** `o1a-audit-tool.html` (standalone, no navbar/footer, no Supabase UMD; 6 screen divs: form / review / payment / loading / download / error); `js/o1a-audit-tool.js` (exports `window.o1aAuditTool = { init, start }`); `js/autoopen-o1a-audit.js` (CSP-compliant polling IIFE). 3 MB raw file cap (base64 inflates ~33% → ~4 MB, safely under sessionStorage ceiling). `QuotaExceededError` guard in `_doCheckout()` — catches sessionStorage write failure, shows inline error, aborts before calling `purchaseItem`. Post-payment ingest failure (encrypted/scanned PDF after session is consumed): shows "Your payment went through" + `mailto:support@orabo.app` link — no retry button, no re-checkout (session is burned). Cancel-return re-hydrates `renderForm()` DOM from restored `answers`/`selectedFile` state (no silent UI/state mismatch). sessionStorage keys: `orabo_o1a_audit_answers` / `orabo_o1a_audit_output_type`. Download filename: `o1a-petition-audit-report.docx`. `js/stripe.js` bypass list: 28 keys (added `o1a_audit`). `css/ibtool-audit.css` (NEW split — `ibtool.css` was at 575 lines, adding audit CSS would exceed 600-line cap); `@import` added to `styles.css`. Audit card (`.tool-card--audit`, gold border variant) added to `o1a-visa.html`. **PENDING VALIDATION:** live e2e smoke (upload real .docx → pay $59 → confirm DOCX download + Brevo delivery email → issue Stripe refund); confirm base64 payload survives sessionStorage through the Stripe redirect at real file size (3 MB DOCX). Offline PDF smoke (pdfkit proxy): SMOKE-1 wordCount=980, posture="FILING-READY WITH VULNERABILITIES" PASS; SMOKE-2 wordCount=980, posture="FILING-READY WITH VULNERABILITIES" PASS. CAL-1–CAL-4 calibration harness: 4/4 PASS. **Deferred:** pre-payment client-side file-readability precheck (encrypted/scanned PDF currently caught only post-payment; a free `/api/audit/precheck` or client-side readability probe would catch this before Stripe redirect).
- **Phase Petition Audit Phase 2 (EB-1A + EB-2 NIW, 2026-05-21 — build+calibrate complete, go-live pending):** Ports the adversarial dual-persona audit engine to EB-1A and EB-2 NIW families. Backend-only changes (no frontend, no Stripe item, no landing card, no nav). **`routes/audit-prompts.js`** extended with `EB1A_AUDIT_BLOCK1` + `EB1A_AUDIT_OUTPUT_CONTRACT` + `EB2_AUDIT_BLOCK1` + `EB2_AUDIT_OUTPUT_CONTRACT`; both registered in `AUDIT_PROMPTS` — `runAudit({family:'eb1a'})` / `{family:'eb2'}` now functional. Key EB-1A distinctions: 10-criterion Kazarian two-step (vs 8 criteria for O-1A); **first-person self-petitioner voice is CORRECT and NOT flagged** (inverted from O-1A petitioner-third-person requirement); Structural Compliance removes O-1A-specific checks (employer support letter, advisory opinion, contract, agent); Threshold requires ≥3 of 10 + FMD. Key EB-2 distinctions: Dhanasar three-prong test (not a criteria count); **ALL THREE prongs required** — failure on any single prong is a threshold failure; Section 2 is "Prong Compliance" (3 rows, not 10); Structural Compliance checks for Proposed Endeavor Statement coherence. **`routes/audit.js`** minimal updates: `AUDIT_HTML_DISCLAIMER` (single constant) replaced by `AUDIT_HTML_DISCLAIMERS` map (eb1a cites §204.5(h) + Kazarian; eb2 cites §204.5(k) + Dhanasar); error message updated to "Supported: o1a, eb1a, eb2". **`audit/calibration-eb.js`** (new, ~290 lines): 12 primary EB test cases; `runAudit({ family: tc.family })` dispatches to correct officer; `extractBand` called from in-memory `fullHtml` only — never reads pre-existing `o1a_audit_calib_eb*.html` files from disk; startup collision guard logs informational warning about near-identical O-1A cross-family files; mtime logged after each file write confirms freshness. **Calibration results (2026-05-21):** EB-1A NEEDS-TUNING (high=1: letter_A STRONG profile → HIGH RFE RISK, too harsh; packages 4/4 PASS with correct monotonic ordering); EB-2 CALIBRATED (6/6 PASS; Prong Compliance table has 3 rows; Threshold Check explicitly states all-three requirement). Tuning pass deferred. Findings at `audit/eb-audit-calibration-findings.md`. Outputs at `audit/outputs/eb_audit_calib_*.html` (12 files). **Acceptance criteria confirmed:** EB-1A does NOT flag first-person voice (Structural Compliance section explicitly states no employer letter, job offer, or advisory opinion required); EB-2 Threshold Check states "All three Dhanasar prongs must be satisfied — failure on any single prong is a threshold failure"; both `*_C` files emit LIKELY INSUFFICIENT without being confused by refusal-framing input; zero PARSE-MISS; zero numeric-% leak. `git diff` touches only `audit/**` + `routes/audit-prompts.js` + `routes/audit.js`.
- **EB-2 Letter R-01 — Prong 2 Filing Risk Advisory (2026-05-21):** `EB2_LETTER_SYSTEM_PROMPT` in `routes/eb2.js` extended with a **conditional Filing Risk Advisory** inserted between the Prong 2 and Prong 3 arguments. Fires when ALL THREE of the following are absent from the petitioner's answers: (a) `eb2_letter_q15_us_collaborations` is `"(none confirmed)"` or substantively equivalent — no named US institutional collaborations; (b) `eb2_letter_q12_publications_projects` contains no peer-reviewed publications (commercial products, operational platforms, and conference presentations do NOT qualify); (c) `eb2_letter_q14_expert_letters` is `"(none identified)"` or substantively equivalent — no confirmed expert letter authors. When triggered, inserts `<h3>Filing Risk Advisory</h3>` + a paragraph stating the petition lacks (a), (b), and (c) and at least one gap must be closed before submitting. When the petitioner HAS adequate Prong 2 validation (named US collaborations, peer-reviewed publications, or confirmed expert letter authors), the advisory is NOT inserted — a well-evidenced petition must not carry a risk warning that does not apply. **Layer 2 guard (log-only observability):** after code-fence stripping, the route handler checks `hasInsertGaps = /\[INSERT/i.test(aiOutput)` and `hasAdvisory = /Filing Risk Advisory/i.test(aiOutput)`; if `hasInsertGaps && !hasAdvisory`, logs `[eb2-letter] prong2-advisory-missing` to Railway via `console.warn` — detection only, no force-append. Guard runs on pre-footer `aiOutput` so footer edits cannot mask a missing advisory. `max_tokens` unchanged at 8000. No `ai_cache` change (EB routes never read cache). No frontend changes.
- **Sources Footer Leak Fix (2026-05-21):** The Phase Sources Part 2 citation footer (commit `0f8f2e3`) had leaked a debug/provenance sentence — `Last verified: … canonical-pattern-confirmed; content not web-fetched due to Cloudflare bot detection.` — into the customer-facing `## Sources & Authorities` section of every AI-generated deliverable. Removed across 8 backend source files: `routes/cv.js`, `routes/eb1a.js`, `routes/eb2.js`, `routes/o1a-prompts.js`, `routes/report.js`, `routes/sop.js`, `routes/ukgt-tech-prompts.js`, `utils/checklist-knowledge.js`. `routes/ukgt-research-prompts.js` was inspected and confirmed clean (never contained the sentence). Citation lists, legal disclaimers, and the O-1A nonimmigrant clause were fully preserved. **Docgen guard:** a log-only absence guard was added at the top of `generateDocx()` in `routes/docgen.js` (lines 228–231): `const LEAK_TOKENS = /Cloudflare|web-fetched|canonical-pattern|Last verified/i; if (LEAK_TOKENS.test(content)) { console.error('[footer] provenance-leak-detected', { route: 'docgen' }); }` — this fires `console.error` on Railway if any of the four banned fragments appear in any future document, without stripping or blocking the output. Defect was caught by the Petition Audit officer persona (the audit product flagging a structural integrity defect in a generator deliverable).
- **Pathway Match Phase C Part 1 — backend route + prompts + Stripe wiring (2026-05-21):** Backend-only. `routes/pathway-match.js` (176 lines, mounted at `/api/pathway-match` with `checklistLimiter`) + `routes/pathway-match-prompts.js`. Stripe item: `pathway_match_report` ($19 = 1900 cents). Frontend page `pathway-match-tool.html` ships in Part 2. **System prompt**: two-block pattern (checklist.js style) — Block 1 cached (`PATHWAY_MATCH_SYSTEM_PROMPT`, built at require-time from `utils/checklist-pathway-data.js` PATHWAY_KNOWLEDGE_BASE + `utils/checklist-knowledge.js` ORIGIN_AUTHENTICATION_CONTEXT + INFEASIBLE_COMBINATIONS), Block 2 per-request (formAnswers JSON + today's date + local currency + TOOL_MANIFEST). `max_tokens: 6000`. `TOOL_MANIFEST` defined locally in `pathway-match-prompts.js` — not imported from stripe.js (circular). Accuracy rails baked into Block 1: (1) INFEASIBLE_COMBINATIONS applied without exception; (2) DV Lottery — Nigeria excluded; (3) EB-2 ROW — no multi-decade India/China-style backlogs for African applicants, NIW removes PERM not the EB-2 priority date queue; (4) success bands: high/moderate/low only (no %); (5) profile gaps flagged with `[PROFILE GAP — please provide: X]`; (6) strategic guidance not legal advice; (7) Section 7 must reference ≥1 real Orabo tool. **Output**: 7 `<h1>` HTML sections: Executive Summary → Profile Analysis → Top Pathway Recommendation → Alternative Pathways → Country-Specific Risks → 30/60/90-Day Action Plan → Orabo Tool Recommendations. Sources footer appended (`PATHWAY_MATCH_SOURCES_FOOTER`). `sanitizeBrand()` applied before footer. Code-fence stripping before `generateDocx()`. `verifyPayment` + `gate` (HTTP 403 on failure) — same pattern as ukgt-tech.js. `stripe_session_id: null` in `one_off_purchases` insert (verify-payment.js already inserts the real session row). Brevo fire-and-forget: SMTP delivery + contact attribute update (`PURCHASE_DATE`, `LAST_TOOL: 'pathway_match_report'`) with `[brevo-pathway-match]`/`[brevo-pathway-match-attr]` log prefixes. Currency miss logs `console.warn('[pathway-match] currency miss for origin:', originCountry)` before USD fallback. Response: `{ success, result, files: { pathway_match_docx }, firstName }`. Stripe: `ITEM_PRICES.pathway_match_report = 1900`, `ITEM_NAMES` + `ITEM_SUCCESS_PAGES` (local inside handler) + `TOOL_CANCEL_PAGES` all updated. Frontend bypass list update shipped in Part 2.
- **Pathway Match Phase C Part 2 — frontend tool (2026-05-21):** `pathway-match-tool.html` live — standalone paid tool at $19. Bare page (no navbar, no footer, no Supabase UMD). 5-section form: Section 0 "Your details" (name + email, required) + Section A "Background" (origin country, age, citizenship, dependents, spouse status — age/dependents/spouse optional) + Section B "Profession" (role, experience, qualification, field of study, English level — field optional) + Section C "Migration goals" (primary motivation, urgency, budget band, risk appetite) + Section D "Preferences" (target countries multiselect cap 4, deal breakers, prior application history — last two optional). **19 fields total: 13 required / 6 optional** — AI uses `[PROFILE GAP]` for blanks. Frontend modules: `js/pathway-match-tool.js` + `js/pathway-match-questions.js`. Autoopen: `js/autoopen-pathway-match.js` (CSP-compliant polling IIFE — external file, follows existing autoopen pattern). sessionStorage keys: `orabo_pathway_match_answers`/`orabo_pathway_match_output_type`. TOOL_KEY: `pathway_match`. TOOL_LABEL: `'Pathway Match Report'`. ITEM_TYPE: `pathway_match_report`. EMAIL_Q_ID: `pathway_match_q0_user_email`. Question ID prefix: `pathway_match_q<n>_<slug>`. SCREENS: `['pm-form-screen','pm-review-screen','pm-payment-screen','pm-loading-screen','pm-download-screen']`. No preview screen — $19 is below the $140 preview threshold. Section 0 back button → `/pathway-compare.html` (NOT `/`). Multiselect `q15_target_countries` has a 4-country cap: when 4 boxes checked, all unchecked boxes are disabled; deselect one to re-enable; value stored as pipe-delimited string `|`; validation requires ≥1 selected. Payment screen "Select & Pay" sets both sessionStorage keys then calls `purchaseItem('pathway_match_report', btn, { customer_email })`. POST body: `{ outputType, formAnswers, stripe_session_id }`. `handlePaymentReturn()` reads `session_id` from URLSearchParams and forwards as `stripe_session_id`. Cancel return restores answers from sessionStorage and shows review screen + amber `.payment-cancel-notice`. `showDownloadScreen()` calls `clearDraft(TOOL_KEY)` first; downloads from `data.files.pathway_match_docx` as `pathway-match-report.docx`. Save & Resume Draft with TOOL_KEY `pathway_match` (localStorage key `orabo_draft_pathway_match`). 8 rotating loading messages every 10s. `window.pathwayMatchTool = { init, start }` export. CSS: `css/pathway-match.css` — modal/screen `.active` visibility, multiselect styles; imported in `styles.css` between `pathway-compare-overview.css` and `mobile.css`. Nav dropdown entry: `🎯 Pathway Match Report` with `.nav-paid-badge` `$19` badge — inserted after "Compare Destinations" `<li>` in Tools dropdown. `.nav-paid-badge` CSS class added to `css/nav.css`. `js/stripe.js` bypass list: **29 keys** (added `pathway_match_report`). `TOOL_CANCEL_PAGES` in `routes/stripe.js`: **29 keys** (added `pathway_match_report` → `pathway-match-tool.html`). `sitemap.xml` entry added (priority 0.7, lastmod 2026-05-21).
- **Phase Petition Audit Phase 5 — UKGT Endorsement Audit (2026-05-22):** Single SKU `ukgt_audit` ($59) covering both endorsement routes — Tech Nation (digital technology) and Peer Review (Royal Society / RAEng / British Academy / UKRI) — via a `route` field selected at runtime on the tool page. **Backend (`japaconnect-backend`):** `routes/audit-prompts-ukgt.js` (NEW, ~550 lines — sibling file to keep audit-prompts.js under 600-line cap) exports `UKGT_AUDIT_PROMPTS` with 4 constants: `UKGT_TECH_NATION_AUDIT_BLOCK1` + `UKGT_TECH_NATION_AUDIT_OUTPUT_CONTRACT` + `UKGT_PEER_REVIEW_AUDIT_BLOCK1` + `UKGT_PEER_REVIEW_AUDIT_OUTPUT_CONTRACT`; both rubrics derived from shipped generation prompts in `ukgt-tech-prompts.js` / `ukgt-research-prompts.js` — generation prompts untouched. `routes/audit-prompts.js`: requires and spreads `UKGT_AUDIT_PROMPTS` into `AUDIT_PROMPTS`. `routes/audit.js`: (1) `ALLOWED_FAMILIES` Set `{'o1a','eb1a','eb2','ukgt'}` replaces `!AUDIT_PROMPTS[family]` check since `'ukgt'` has no direct key; (2) `route` validated ∈ `{tech_nation, peer_review}` when `family==='ukgt'`, then `promptKey = 'ukgt_'+route` constructed; (3) `runAudit({ family, promptKey, text })` uses `promptKey || family` for the AUDIT_PROMPTS lookup; (4) `_sendAuditEmail` gains 5th `route` param — `ukgt` branch added to `FAMILY_META` with route-conditional subjects (Tech Nation vs Peer Review), UK English body, OISC disclaimer, route-conditional upsell links; (5) `AUDIT_HTML_DISCLAIMERS` gains `ukgt` key with UK/gov.uk/global-talent framing, no USCIS references. `routes/stripe.js`: `ITEM_PRICES.ukgt_audit = 5900`, `ITEM_NAMES.ukgt_audit = 'Orabo UK Global Talent Petition Audit'`, `ITEM_SUCCESS_PAGES.ukgt_audit = 'ukgt-audit-tool.html'`, `TOOL_CANCEL_PAGES.ukgt_audit = 'ukgt-audit-tool.html'`. **Frontend (`japaconnect`):** `ukgt-audit-tool.html` (NEW — bare page, 6 screen divs, `noindex nofollow`); `js/ukgt-audit-tool.js` (NEW, ~580 lines — mirrors o1a-audit-tool.js; key differences: route `<select>` in form with `tech_nation`/`peer_review` options + validation; `answers.route` carried through sessionStorage; route-conditional payment screen title; `family:'ukgt', route: parsed.route` in backend POST body; file key `ukgt_audit_docx`; download `ukgt-petition-audit-report.docx`; back → `/uk-global-talent.html`; UK English throughout); `js/autoopen-ukgt-audit.js` (NEW — CSP-compliant polling IIFE); `css/ibtool-audit.css` appended with 6-screen modal visibility block + `.tool-cards--single { max-width: 480px }`; `js/stripe.js` bypass list extended from 31 to **32 keys** (`ukgt_audit` added to both `payment_cancelled` and `payment_success` chains); `uk-global-talent.html` audit card inserted after Peer Review tools section (`.lp-section--audit`, `.tool-cards.tool-cards--single`, no inline `style=`). Rubric currency confirmed: Tech Nation form permanently withdrawn 4 Aug 2025; GOV.UK Stage 1 packet (personal statement + 3 recommendation letters + evidence portfolio + Plan for the UK) is the current evidence set; emerging digital fields (quantum, blockchain, cybersecurity, design) not flagged as non-digital. First-person applicant voice is CORRECT for UKGT — must NOT be flagged. CS disambiguation rule preserved from generation prompts: industry SE without peer-reviewed publications → Tech Nation, not Royal Society. Posture bands: ENDORSEMENT-READY / ENDORSEMENT-READY WITH VULNERABILITIES / HIGH REFUSAL RISK / LIKELY INELIGIBLE (never numeric %). UK English mandatory throughout (organisation, recognise, programme, analyse). Brevo log prefixes: `[brevo-ukgt-audit]`, `[brevo-ukgt-audit-attr]`.

---

## Architecture — Multi-Page App

The frontend is a **multi-page app** with 12 core app HTML files + 6 EB tool pages + 4 O-1A pages + 6 UKGT tool pages + 1 admin page + 4 SEO landing pages + a `blog/` directory:

| File | Purpose | Auth required |
|---|---|---|
| `index.html` | Main marketing site — free tools + marketing cards for paid services (CV/SOP/Consult Start buttons redirect to standalone tool pages) | No |
| `signup.html` | Sign-up page | No |
| `login.html` | Login + forgot-password page | No |
| `reset-password.html` | Standalone password-reset page — handles Supabase recovery token (PKCE `?code=` query param and implicit `#access_token`/`PASSWORD_RECOVERY` hash); shows password + confirm form on PASSWORD_RECOVERY event; shows expired/invalid-link card with in-page email re-request (`resetPasswordForEmail` redirectTo `https://orabo.app/reset-password`, no `.html`); all logic in external `js/reset-password.js` (Cloudflare edge CSP blocks any inline script). Uses Supabase UMD + `supabaseClient` (never `const supabase`). | No |
| `dashboard.html` | Personal dashboard — sidebar tool launcher (15 entries: 1 journey home + 8 free + 6 Pro) + right-pane iframe tool content. Pro upgrade entry point. CV Review, SOP Writer, and Consultation are NOT here — they live on standalone pages (cv-tool.html, sop-tool.html, consult-booking.html). The three former Coming Soon placeholders (Budget Tracker, Progress Tracking, Priority Support) were removed on 2026-05-16 — the journey home now surfaces the personalisation those entries were intended to represent. | Yes — redirects to `login.html` if unauthenticated |
| `cv-tool.html` | Standalone CV Review tool — public, auto-opens CV modal on load | No — open to all; name + email captured in form (section 1) |
| `sop-tool.html` | Standalone SOP Writer tool — public, auto-opens SOP modal on load | No — open to all; name + email captured in form (section 1) |
| `consult-booking.html` | Standalone Consultation booking — public, auto-opens booking modal on load | No — open to all; name + email captured in booking form |
| `compare-cities.html` | Pro feature: compare cost of living across 4 cities | Pro localStorage gate |
| `all-countries-pay.html` | Pro feature: take-home pay across all 9 countries for all 35 roles | Pro localStorage gate |
| `doc-checklist.html` | Dashboard iframe: auto-opens document checklist wizard (free preview) | No (tool self-gates) |
| `readiness-report.html` | Dashboard iframe: auto-opens readiness quiz | No (tool self-gates) |
| `full-doc-checklist.html` | Dashboard iframe: auto-opens document checklist wizard (Pro card) | No (tool self-gates) |
| `readiness-score.html` | SEO landing page — "migration readiness quiz Africa" — drives to `/#readiness` | No |
| `visa-eligibility.html` | SEO landing page — "visa eligibility checker Africa" — drives to `/#eligibility` | No |
| `document-checklist.html` | SEO landing page — "immigration document checklist" — drives to `/#tools` | No |
| `eb-immigration.html` | SEO landing page — EB-1A & EB-2 NIW tools landing page. Hero: two Tier 1 CTA buttons to live `eb1a-tool.html` / `eb2-tool.html`. Footer: 'Not ready to file yet?' stay-updated `/api/newsletter/subscribe` capture (source: `eb_waitlist`). Waitlist JS in `js/eb-immigration.js`. | No |
| `eb1a-tool.html` | Standalone EB-1A Eligibility Assessment — no navbar/footer, no Supabase UMD; polls `window.eb1aTool` then calls `init()`; anonymous flow; Stripe returns here | No |
| `eb2-tool.html` | Standalone EB-2 NIW Eligibility Assessment — same pattern as `eb1a-tool.html` | No |
| `eb1a-letter-tool.html` | Standalone EB-1A Petition Support Letter (Tier 2) — polls `window.eb1aLetterTool`; 6 sections, 22 petition-content questions; $150; DOCX download | No |
| `eb2-letter-tool.html` | Standalone EB-2 NIW Petition Letter (Tier 2) — polls `window.eb2LetterTool`; 5 sections, 20 petition-content questions; $140; DOCX download | No |
| `eb1a-package-tool.html` | Standalone EB-1A Full Petition Package (Tier 3) — polls `window.eb1aPackageTool`; 7 sections (A–F + Your details), 25 questions; $250; 6-component DOCX download | No |
| `eb2-package-tool.html` | Standalone EB-2 NIW Full Petition Package (Tier 3) — polls `window.eb2PackageTool`; 6 sections (A–E + Your details), 23 questions; $240; 7-component DOCX download | No |
| `o1a-visa.html` | O-1A landing page — 3-tier tool card grid + audit card, 8-criteria explainer, petitioner requirements, EB upsell, 'Not ready to file yet?' stay-updated email capture (source: `o1a_waitlist`); waitlist JS in `js/o1a-visa-waitlist.js` (ES module, imports API_URL from config.js; fixed wrong host 2026-05-23) | No |
| `o1a-tool.html` | Standalone O-1A Eligibility Assessment (Tier 1) — polls `window.o1aTool`; 5 sections (Section 0 + A–D), 12 questions; $39; DOCX download; back on Section 0 → `/o1a-visa.html` | No |
| `o1a-letter-tool.html` | Standalone O-1A Petition Support Letter + Peer Consultation Letter (Tier 2) — polls `window.o1aLetterTool`; 6 sections, 22 questions; $150; DOCX download | No |
| `o1a-package-tool.html` | Standalone O-1A Full Petition Package (Tier 3) — polls `window.o1aPackageTool`; 7 sections, 25 questions; $250; 7-section DOCX download including Petitioner Support Brief | No |
| `uk-global-talent.html` | UK Global Talent landing page — all 6 tiers LIVE (Tech Nation ×3 + Peer Review ×3), tier cards, Exceptional Talent vs Promise explainer; 'Not ready to apply yet?' stay-updated email capture at `#waitlist` (source: `ukgt_waitlist`); waitlist JS in `js/uk-global-talent-waitlist.js` (ES module, imports API_URL from config.js, 2026-05-23) | No |
| `ukgt-tech-tool.html` | Standalone UKGT Tech Nation Tier 1 — polls `window.ukgtTechTool`; 5 sections (Section 0 + 4 content), 14 questions; $39; DOCX download; back on Section 0 → `/uk-global-talent.html` | No |
| `ukgt-tech-statement-tool.html` | Standalone UKGT Tech Nation Tier 2 — polls `window.ukgtTechStatementTool`; 6 sections (Section 0 + 5 content), 22 questions; $150; DOCX download (personal statement + 3 recommendation letter templates + Plan for the UK); back on Section 0 → `/uk-global-talent.html` | No |
| `ukgt-tech-package-tool.html` | Standalone UKGT Tech Nation Tier 3 — polls `window.ukgtTechPackageTool`; 7 sections (Section 0 + 6 content), 25 questions; $250; DOCX download (7-component full endorsement package); back on Section 0 → `/uk-global-talent.html` | No |
| `ukgt-research-tool.html` | Standalone UKGT Peer Review Tier 1 — polls `window.ukgtResearchTool`; 5 sections (Section 0 + A–D), 14 questions; $39; DOCX download; back on Section 0 → `/uk-global-talent.html` | No |
| `ukgt-research-statement-tool.html` | Standalone UKGT Peer Review Tier 2 — polls `window.ukgtResearchStatementTool`; 6 sections (Section 0 + A–E), 24 questions; $150; DOCX download (personal statement + UK eminent recommender letter + two peer letters + Plan for the UK); back on Section 0 → `/uk-global-talent.html` | No |
| `ukgt-research-package-tool.html` | Standalone UKGT Peer Review Tier 3 — polls `window.ukgtResearchPackageTool`; 7 sections (Section 0 + S1–S6), 27 questions; $250; DOCX download (7-component full endorsement package); back on Section 0 → `/uk-global-talent.html` | No |
| `o1a-audit-tool.html` | Standalone O-1A Petition Audit — reviewer tool (user uploads existing draft); polls `window.o1aAuditTool`; 6 screens (form / review / payment / loading / download / error); $59; DOCX download (`o1a-petition-audit-report.docx`); 3 MB raw file cap; PENDING live e2e validation | No |
| `eb1a-audit-tool.html` | Standalone EB-1A Petition Audit — reviewer tool; polls `window.eb1aAuditTool`; 6 screens; $59; DOCX download (`eb1a-petition-audit-report.docx`); 3 MB raw file cap; back button → `/eb-immigration.html` | No |
| `eb2-audit-tool.html` | Standalone EB-2 NIW Petition Audit — reviewer tool; polls `window.eb2AuditTool`; 6 screens; $59; DOCX download (`eb2-niw-petition-audit-report.docx`); 3 MB raw file cap; back button → `/eb-immigration.html` | No |
| `ukgt-audit-tool.html` | Standalone UKGT Endorsement Audit — reviewer tool; polls `window.ukgtAuditTool`; 6 screens (form/review/payment/loading/download/error); single SKU `ukgt_audit` $59; `route` selector (tech_nation / peer_review) on form screen; DOCX download (`ukgt-petition-audit-report.docx`); 3 MB raw file cap; back button → `/uk-global-talent.html`; `noindex, nofollow` | No |
| `pathway-match-tool.html` | Standalone Pathway Match Report — paid tool; polls `window.pathwayMatchTool`; 5 sections (Section 0 + A–D), 19 fields (13 required / 6 optional); $19; DOCX download (`pathway-match-report.docx`); back on Section 0 → `/pathway-compare.html`; multiselect q15 with 4-country cap; no preview screen | No |
| `petition-audit.html` | Petition Audit router/landing page — explains the audit product, routes to all four per-type tools (O-1A, EB-1A, EB-2 NIW, UK Global Talent); 2×2 `.audit-router-grid`; $59 per audit; no JS; static links only; mirrors `o1a-visa.html` template | No |
| `admin.html` | Admin panel — token-gated, not linked from any public page; direct URL only; loads only `css/admin.css` + `js/admin.js` | Token (`ADMIN_SECRET`) |
| `blog/index.html` | Blog index — 5 article cards | No |
| `blog/cost-of-living-lagos-london-toronto.html` | Article: Lagos vs London vs Toronto cost of living 2025 | No |
| `blog/express-entry-document-checklist.html` | Article: Complete Express Entry document checklist (Canada) | No |
| `blog/uk-skilled-worker-eligibility.html` | Article: UK Skilled Worker eligibility guide | No |
| `blog/eb2-niw-vs-eb1a.html` | Article: EB-2 NIW vs EB-1A comparison | No |
| `blog/nurse-salary-uk.html` | Article: UK nurse salary after tax | No |

`index.html` is always logged-out — no auth in navbar. Standalone tool pages (`cv-tool.html`, `sop-tool.html`, `consult-booking.html`) have no navbar/footer and require **no authentication** — they auto-open the tool modal via a DOMContentLoaded inline script. Name and email are captured as the first section of the tool form. Tool type passed via sessionStorage (`jc_cv_tool`, `jc_sop_tool`, `jc_consult_booking`) set by `redirect-paid-tools.js`. `compare-cities.html` / `all-countries-pay.html` gate via `localStorage.getItem('orabo_pro_status') === 'true'`; hide header with `?embed=true`. `doc-checklist.html`, `readiness-report.html`, `full-doc-checklist.html` are standalone embedded pages (no navbar/footer) — import tool module + `autoopen-*.js`; Stripe redirects use `window.top.location.href`.

---

## Frontend Structure

```
japaconnect/
├── index.html               # Main marketing site (public, free tools + marketing cards; paid tool buttons navigate to cv-tool.html / sop-tool.html / consult-booking.html)
├── signup.html              # Sign-up page (standalone, own inline script)
├── login.html               # Login + forgot-password page (standalone, own inline script)
├── reset-password.html      # Standalone password-reset page — loads Supabase UMD + js/reset-password.js (external, CSP-required); parses recovery token from ?code= (PKCE) or #access_token/type=recovery (implicit); shows password+confirm form on PASSWORD_RECOVERY event; shows expired/invalid-link card with in-page email re-request (redirectTo 'https://orabo.app/reset-password', no .html); fail-open try/finally; calls updateUser then redirects to login.html
├── dashboard.html           # Protected dashboard — sidebar tool launcher (15 entries: 1 journey home + 8 free + 6 Pro) + right-pane iframe tool content + Pro upgrade entry point. CV / SOP / Consultation NOT here; they live on cv-tool.html / sop-tool.html / consult-booking.html. All logic is a single inline <script> — no external module imports.
├── cv-tool.html             # Standalone CV tool — public; DOMContentLoaded inline script auto-opens modal; Stripe returns here; no auth
├── sop-tool.html            # Standalone SOP tool — public; DOMContentLoaded inline script auto-opens modal; Stripe returns here; no auth
├── consult-booking.html     # Standalone consultation — public; DOMContentLoaded auto-clicks .consult-btn; stripe.js shows calendlyOverlay on return; no auth
├── compare-cities.html      # Pro: compare CoL across 4 cities (standalone, Pro localStorage gate)
├── all-countries-pay.html   # Pro: pay comparison all countries (standalone, Pro localStorage gate)
├── doc-checklist.html       # Dashboard iframe: checklist wizard auto-opens; imports checklist.js + autoopen-checklist.js
├── readiness-report.html    # Dashboard iframe: quiz auto-opens; imports quiz.js + autoopen-quiz.js
├── full-doc-checklist.html  # Dashboard iframe: same as doc-checklist.html, shown to Pro users in dashboard
├── styles.css               # Global styles (imports all css/ partials)
├── script.js                # Legacy interactivity (pre-module)
├── site.webmanifest         # PWA web app manifest (name, icons, theme, display mode) — linked from index.html
├── sw.js                    # Service worker permanently removed — stub comment file only
├── icons/
│   ├── icon-192.png         # PWA icon 192×192 (navy background, globe + Orabo text)
│   └── icon-512.png         # PWA icon 512×512
├── css/
│   ├── base.css             # :root CSS variables, reset, .container (max-width 1180px), .section, embed-mode rules
│   ├── hero.css             # Hero section — grid-template-columns:1fr auto collapsed at 1024px; phone mockup hidden at 1024px; float animation
│   ├── features.css         # features-grid (3→2→1 col), steps-row (flex→column at 768px), trust-grid (4→2→1 col)
│   ├── visatracker.css      # vpt-layout: grid-template-columns 320px 1fr — responsive breakpoint is 900px, not 768px
│   ├── auth.css             # Nav login/signup button styles (.nav-login-btn, .nav-signup-btn)
│   ├── subscription.css     # Upgrade modal + .nav-go-pro
│   ├── quiz.css             # Readiness quiz + report modal; .country-select-error/.visible for validation; .quiz-email-capture styles + .hidden utility (Phase 5B)
│   ├── cvtool.css           # CV Review + SOP Writer tool styles (shared); CSS-based initial hide rules for both tool modals
│   ├── hamburger.css        # Mobile hamburger nav — full-screen overlay, X animation, body.menu-open scroll lock; all rules inside @media (max-width: 768px)
│   ├── eb-waitlist.css      # EB waitlist banner + blog teaser section styles for index.html; loaded before mobile.css in styles.css
│   ├── admin.css            # Admin panel styles — only loaded by admin.html, NOT imported into styles.css
│   ├── mobile.css           # Mobile responsive fixes — hero overflow, grid single-column, text scaling. Loaded last in styles.css.
│   ├── dashboard-page.css   # Dashboard page styles (linked directly from dashboard.html, NOT in styles.css)
│   ├── dashboard-preferences.css  # Phase 5 preferences modal styles — linked from dashboard.html; not in styles.css. Visibility via body.preferences-modal-open class toggle. Also styles .dj-pathway-edit-btn gear icon.
│   ├── ibtool-audit.css     # O-1A Petition Audit modal/screen/upload CSS (~100 lines); split from ibtool.css (was 575 lines — adding audit CSS would exceed 600-line cap); @import added to styles.css after ibtool.css
│   └── dashboard.css        # (legacy — dashboard is now dashboard.html)
├── js/
│   ├── auth.js              # Supabase session, profile cache, openAuthModal (redirects to login.html); defensive window.supabase check — safe to import on pages without Supabase UMD
│   ├── reset-password.js    # Reset-password page logic (external — inline scripts blocked by Cloudflare edge CSP). Parses recovery token from query (PKCE ?code=) and hash (implicit #access_token/type=recovery); shows password+confirm form on PASSWORD_RECOVERY event; shows expired-link card with resetPasswordForEmail re-request (redirectTo 'https://orabo.app/reset-password', no .html); fail-open try/finally; calls updateUser then redirects to login.html. Uses supabaseClient (never const supabase).
│   ├── embed.js             # Sets html.embed-mode class synchronously; hides navbar/footer in iframe
│   ├── stripe.js            # Stripe checkout + payment return handling; payment=success/cancelled skipped on dashboard.html; purchaseItem resolves email from bookingMeta.customer_email → booking_email → user.email; checkPaymentReturn IIFE preserves URL for payment_cancelled=1 for the 28 tool items
│   ├── subscription.js      # isPro() check (Supabase + localStorage fallback) + upgrade modal
│   ├── checklist.js         # Document checklist wizard engine (module — do not modify for payment-return/bypass/origin logic)
│   ├── quiz.js              # Readiness quiz engine + AI report (module — do not modify for payment-return logic)
│   ├── booking.js           # Consultation booking modal + Stripe; wrapped in IIFE with `!modal` guard; step 1 has `.tool-back-btn` "← Back to Home" inserted at top; step 2's `.prev-step` button replaced by `.tool-back-btn` "← Back" → goToStep(1)
│   ├── visatracker.js       # Processing times logic — 18 visas, list + detail + Chart.js trend
│   ├── visatracker-data.js  # Exported VISAS[], TREND_LABELS, TREND_DATA (split from visatracker.js)
│   ├── costliving.js        # Cost of living — 33 cities, grouped dropdown, single-card + compare modal (progressive city slots)
│   ├── paycalc.js           # Take-home pay — 35 roles, grouped dropdown, per-country result + compare modal; imports data from paycalc-data.js
│   ├── paycalc-data.js      # Exported ROLES[], ROLE_GROUPS, SALARY_DATA (split from paycalc.js)
│   ├── tool-review.js       # Shared ES module — exports renderReview(opts) used by all 14 tool modules; opts: { screenId, prefix, sections, answers, onBack, onContinue }; renders .tool-review-wrap with section/item/label/value layout; empty answers show "(not provided)"
│   ├── draft-store.js       # Shared ES module — localStorage draft persistence; exports saveDraft, loadDraft, clearDraft, hasDraft, listAllDrafts; key prefix orabo_draft_<toolKey>; DRAFT_VERSION = 1; 30-day TTL; all calls in try/catch for Safari private mode
│   ├── draft-banner.js      # Shared ES module — exports showResumeBanner({ container, toolLabel, savedAt, onResume, onStartFresh }); renders .draft-resume-banner as container.firstChild; relative time display; CSP-compliant (addEventListener only)
│   ├── cvtool.js            # CV Review + Cover Letter Generator — loaded on cv-tool.html only; imports stripe.js + config.js + tool-review.js + cv-file-extract.js (no auth.js); renders ALL_SECTIONS = CV_SECTIONS directly; screen visibility via classList.add('active'); review screen between last form section and payment; payment return reads URL param + sessionStorage (success→generate, cancel→review screen); exports window.cvTool = { init, start }
│   ├── cvtool-questions.js  # CV_SECTIONS[] — 6 sections: 'Your details' (user_name, user_email) + 5 original sections; Section 5 fileUpload accept=".txt,.pdf,.docx"
│   ├── cv-file-extract.js   # ES module — client-side PDF/DOCX/TXT extraction; imports pdfjs-dist from ./vendor/pdf.min.mjs; mammoth from window.mammoth (UMD global); exports extractTextFromFile(file) → { text, error }; errors: too_large / empty / unsupported / parse_failed
│   ├── soptool.js           # SOP Writer — loaded on sop-tool.html only; same pattern as cvtool.js; imports tool-review.js; review screen between last section and payment; ALL_SECTIONS = SOP_SECTIONS; exports window.sopTool = { init, start }
│   ├── soptool-questions.js # SOP_SECTIONS[] — 7 sections: 'Your details' (user_name, user_email) + 6 original sections
│   ├── redirect-paid-tools.js # Non-module: wires CV/SOP/Consult Start buttons on index.html — saves type to sessionStorage and navigates to standalone page
│   ├── hero-cta.js            # Non-module IIFE: intercepts #hero-eligibility-cta click on index.html — calls #openEligibilityBtn.click() for one-click modal trigger; falls through to href="#eligibility" anchor scroll if opener absent
│   ├── funnel-track.js        # ES module: first-party funnel analytics on index.html — tracks page_view, scroll_depth (25/50/75/100%), section_view (5 landmarks), tool_start (all tool triggers), eligibility_complete; anonymous UUID session_id in localStorage orabo_sid; fire-and-forget POSTs to /api/track; checkout_start not wired (no inline Stripe on index.html)
│   ├── autoopen-checklist.js # Triggers #openChecklistWizard.click() — used by doc-checklist.html + full-doc-checklist.html
│   ├── autoopen-quiz.js      # imports openQuiz from quiz.js and calls it — used by readiness-report.html
│   ├── payment-return-checklist.js # Non-module payment return handler for doc-checklist.html + full-doc-checklist.html
│   ├── payment-return-quiz.js      # Non-module payment return handler for readiness-report.html
│   ├── pro-bypass-checklist.js     # Non-module: Pro bypass for checklist — intercepts pay button, checks Supabase pro_status, calls API directly if Pro
│   ├── pro-bypass-quiz.js          # Non-module: Pro bypass for readiness report — same pattern as pro-bypass-checklist.js
│   ├── checklist-origin-saver.js   # Non-module: saves checklist country-of-origin to sessionStorage; validates before wizard Next1 (capture phase)
│   ├── quiz-origin-saver.js        # Non-module: saves quiz country-of-origin to sessionStorage; validates before quiz Next (capture phase)
│   ├── free-quiz-origin.js         # Non-module: country-of-origin capture-phase validation for the free readiness quiz on index.html
│   ├── quiz-email-capture.js       # Non-module: post-score email capture on index.html — currently a DISABLED STUB (// Disabled — pending rebuild). Original Phase 5B logic: MutationObserver on #quizResultsScreen; inject #quiz-email-capture; POST quiz_score+quiz_date to /api/newsletter/subscribe.
│   ├── quiz-results-saver.js       # Non-module IIFE: auto-save readiness quiz scores for logged-in users (Dashboard Rebuild Phase 3). Capture-phase listener on #quizOptions (.quiz-option buttons) tracks per-question scores into trackedScores { questionId: score }. MutationObserver on #quizResultsScreen style attribute calls attemptSave(). attemptSave(): if FLAG_KEY already set → return; if trackedScores has entries → saveQuizResults() immediately; else → setTimeout(saveQuizResults, 2000) for payment-return path. saveQuizResults(): primary path uses computeScoreFromClicks(trackedScores) for score + computeFromTracked() for dimensions (both require all 15); fallback (payment-return, no tracked answers) uses getQuizScoreFromDom() reading #scoreNumber after 2s (animation takes ~1.1s). Guard: if 15 answers tracked but computed score=0, refuses to save and logs loudly (score=0 is mathematically impossible — min is ~33%). DEPENDENCY coupling points (quiz.js line numbers as of 2026-05-16): score element #scoreNumber (quiz.js:286), scoring formula Math.round(raw/MAX_SCORE*100) (quiz.js:273-274), options container #quizOptions (quiz.js:217), button class .quiz-option (quiz.js:221), step label #quizStepLabel (quiz.js:205). OPTION_SCORES and MAX_SCORE (145) and CAT_MAX.preparation (65) must mirror quiz.js exactly — verified 2026-05-16 and corrected (language index 1: 3, timeline index 1: 10, field: [10,10,8,7,10,4], passport index 1: 7). Reads JWT via inlined readSessionFromStorage() (NOT window.supabaseClient). POSTs to /api/quiz/save-results. sessionStorage flag orabo_quiz_saved dedupes per session; cleanup-first wipe on page load; cleared on error for retry. Loaded after free-quiz-origin.js on index.html only. quiz.js untouched.
│   ├── quiz-retake-handler.js      # Non-module IIFE (Phase 5): loaded in index.html BEFORE main.js module. Cleanup-first wipes orabo_quiz_saved/jc_quiz_state/jc_quiz_origin on every load. Detects sessionStorage flag orabo_retaking_quiz='1' (set by dashboard.html handleRetakeQuiz()), consumes it, scrolls to #readiness, clicks #startQuizBtn after 200ms. quiz.js untouched.
│   ├── register-sw.js       # Service worker permanently removed — stub comment file; not loaded by any HTML page
│   ├── eb-index-waitlist.js # Non-module: wires EB waitlist email form on index.html — POSTs to /api/newsletter/subscribe with source 'eb_waitlist_homepage'; fail-open
│   ├── admin.js             # Non-module IIFE: full admin panel logic — auth, tab navigation, CRUD for all entities; API base URL hardcoded from config.js value
│   ├── eligibility.js       # Visa eligibility checker — 12 visas, 10 questions, 3 free uses (localStorage gate), Pro unlock via upgradeModal (direct call on index.html — not postMessage; postMessage path retained for any future iframe context)
│   ├── newsletter.js        # Newsletter subscribe form handling + subscribeToDigest() + waitlistDigestHook()
│   ├── analytics.js         # GA4 loader — non-module IIFE; Measurement ID G-W5GY92ZHYS ("Orabo Production"); loaded via <script defer> in every HTML page except admin.html; sets window.dataLayer + window.gtag; anonymize_ip:true; SameSite=None;Secure cookie flags
│   ├── config.js            # API_URL export
│   ├── utils.js             # escapeHTML and shared helpers
│   ├── ukgt-tech-assessment-questions.js  # UKGT_TECH_ASSESSMENT_SECTIONS[] — Tier 1; 5 sections (Section 0 + 4 content), 14 questions; prefix ukgt_tech_q<n>_<slug>
│   ├── ukgt-tech-tool.js            # UKGT Tech Nation Tier 1 — polls window.ukgtTechTool; DOCX download; back on Section 0 → /uk-global-talent.html; exports window.ukgtTechTool = { init, start }
│   ├── ukgt-tech-statement-questions.js   # UKGT_TECH_STATEMENT_SECTIONS[] — Tier 2; 6 sections (Section 0 + 5 content), 22 questions; Q5 track uses { value, label } objects; prefix ukgt_tech_statement_q<n>_<slug>
│   ├── ukgt-tech-statement-tool.js        # UKGT Tech Nation Tier 2 — polls window.ukgtTechStatementTool; _renderSelect handles { value, label } options; word-count guardrail; DOCX download; exports window.ukgtTechStatementTool = { init, start }
│   ├── ukgt-tech-package-questions.js     # UKGT_TECH_PACKAGE_SECTIONS[] — Tier 3; 7 sections (Section 0 + 6 content), 25 questions; Q5 track uses { value, label } objects; prefix ukgt_tech_package_q<n>_<slug>
│   ├── ukgt-tech-package-tool.js          # UKGT Tech Nation Tier 3 — polls window.ukgtTechPackageTool; word-count guardrail; DOCX download (7-component package); exports window.ukgtTechPackageTool = { init, start }
│   ├── ukgt-research-assessment-questions.js  # UKGT_RESEARCH_ASSESSMENT_SECTIONS[] — Peer Review Tier 1; 5 sections (Section 0 + A–D), 14 questions; prefix ukgt_research_q<n>_<slug>
│   ├── ukgt-research-tool.js              # UKGT Peer Review Tier 1 — polls window.ukgtResearchTool; plain text/markdown output; DOCX download; exports window.ukgtResearchTool = { init, start }
│   ├── ukgt-research-statement-questions.js   # UKGT_RESEARCH_STATEMENT_SECTIONS[] — Peer Review Tier 2; 6 sections (Section 0 + A–E), 24 questions; prefix ukgt_research_statement_q<n>_<slug>
│   ├── ukgt-research-statement-tool.js        # UKGT Peer Review Tier 2 — polls window.ukgtResearchStatementTool; HTML output; no preview screen; DOCX download (personal statement + UK eminent recommender letter + two peer letters + Plan for the UK); exports window.ukgtResearchStatementTool = { init, start }
│   ├── ukgt-research-package-questions.js    # UKGT_RESEARCH_PACKAGE_SECTIONS[] — Peer Review Tier 3; 7 sections (Section 0 + S1–S6), 27 questions; prefix ukgt_research_package_q<n>_<slug>
│   ├── ukgt-research-package-tool.js         # UKGT Peer Review Tier 3 — polls window.ukgtResearchPackageTool; HTML output; 7-component DOCX download (full endorsement package); exports window.ukgtResearchPackageTool = { init, start }
│   ├── autoopen-ukgt-research-package.js     # CSP-compliant polling IIFE — triggers window.ukgtResearchPackageTool.init() once available
│   ├── uk-global-talent-waitlist.js   # ES module — 'Not ready to apply yet?' form on uk-global-talent.html; imports API_URL from config.js; source 'ukgt_waitlist'
│   ├── o1a-visa-waitlist.js           # ES module — 'Not ready to file yet?' form on o1a-visa.html; imports API_URL from config.js; source 'o1a_waitlist'
│   ├── o1a-audit-tool.js              # O-1A Petition Audit — reviewer tool; upload zone (DOCX/PDF 3MB cap) + paste fallback; sessionStorage bridge; cancel-return DOM re-hydration from module state; post-payment ingest failure → support mailto (no re-checkout); QuotaExceededError guard aborts checkout if payload too large; exports window.o1aAuditTool = { init, start }; PENDING live e2e validation
│   ├── autoopen-o1a-audit.js          # CSP-compliant polling IIFE — triggers window.o1aAuditTool.init() once available
│   ├── pathway-match-questions.js     # PATHWAY_MATCH_SECTIONS[] — 5 sections (Section 0 + A–D), 19 fields (13 required / 6 optional); multiselect q15 target_countries with 4-country cap; prefix pathway_match_q<n>_<slug>
│   ├── pathway-match-tool.js          # Pathway Match Report tool — polls window.pathwayMatchTool; review screen; cancel-return restoration; DOCX download; Save & Resume Draft (TOOL_KEY 'pathway_match'); exports window.pathwayMatchTool = { init, start }
│   ├── autoopen-pathway-match.js      # CSP-compliant polling IIFE — triggers window.pathwayMatchTool.init() once available
│   ├── pathway-compare-data.js        # A1 data layer (DO NOT MODIFY) — exports COMPARE_PATHWAYS (13), COMPARE_DESTINATIONS (9), COMPARE_DIMENSIONS (16), COMPARE_MATRIX, getCell(pathwayKey, destinationLabel), isFeasible(pathwayKey, destinationLabel)
│   ├── pathway-compare-constants.js   # A1 constants (DO NOT MODIFY) — exports DEST_DATA (top_city, tax, CoL, credential, citizenship) + PATHWAY_DATA (full name, visa codes, eligibility gate, common rejection)
│   ├── pathway-compare-render.js      # A2 render module — exports renderTable(state), renderCTAStrip(state), renderOverviewCard(state); PATHWAY_LEVEL_DIMS = ['pathway_full_name','eligibility_gate','common_rejection'] partitions pathway-level fields from per-destination dims; PER_DEST_DIMS = COMPARE_DIMENSIONS filtered by that list; renderTable uses PER_DEST_DIMS.length for infeasible rowspan; renderCell() 3-state logic; formatDimension() per-dimension formatters; parseVerifyUrl() bare-domain extraction; renderInfeasibleCard(); fuzzyMatchPathway() exact-key lookup
│   └── pathway-compare.js             # A2 controller module — state machine, progressive destination slots, deep-link (?pathway=&dest=), URL sync via history.replaceState, switch-button event delegation, init on DOMContentLoaded; injects renderOverviewCard(state) into #pc-overview
├── pathway-compare.html     # Compare Destinations — free public tool; standard navbar + footer; pathway select + progressive dest slots; overview card (#pc-overview) above table shows pathway-level fields once; 13-dim per-destination comparison table; 3-state cells; frozen-label horizontal scroll; deep-link via ?pathway=&dest=; no auth, no backend
├── readiness-score.html     # SEO landing page — migration readiness quiz; drives to /#readiness; inline <style> + no JS
├── visa-eligibility.html    # SEO landing page — visa eligibility checker; drives to /#eligibility; inline <style> + no JS
├── document-checklist.html  # SEO landing page — document checklist; drives to /#tools; inline <style> + no JS
├── eb-immigration.html      # SEO landing page — EB-1A/EB-2 NIW tools landing; hero has two Tier 1 CTA buttons; footer 'Not ready to file yet?' waitlist posts to /api/newsletter/subscribe via js/eb-immigration.js
├── blog/
│   ├── index.html           # Blog index — links to 6 articles; uses ../styles.css
│   ├── cost-of-living-lagos-london-toronto.html  # Article: Lagos vs London vs Toronto cost of living 2025 comparison
│   ├── express-entry-document-checklist.html     # Article: Complete Express Entry document checklist (Canada)
│   ├── uk-skilled-worker-eligibility.html  # Article: UK Skilled Worker eligibility guide (2024/25 rules)
│   ├── eb2-niw-vs-eb1a.html                # Article: EB-2 NIW vs EB-1A comparison for African professionals
│   ├── nurse-salary-uk.html                # Article: UK nurse salary after tax + Nigeria comparison
│   └── us-immigration-ban-2026.html        # Article: US Immigration Ban 2026 — 39-country and 75-country lists and how they affect African migration
├── ukgt-tech-tool.html      # Standalone UKGT Tech Nation Tier 1 — no navbar/footer, no Supabase UMD; polls window.ukgtTechTool (100ms × 50) then init()
├── ukgt-tech-statement-tool.html  # Standalone UKGT Tech Nation Tier 2 — no navbar/footer, no Supabase UMD; polls window.ukgtTechStatementTool (100ms × 50) then init()
├── ukgt-tech-package-tool.html    # Standalone UKGT Tech Nation Tier 3 — no navbar/footer, no Supabase UMD; polls window.ukgtTechPackageTool (100ms × 50) then init()
├── ukgt-research-tool.html        # Standalone UKGT Peer Review Tier 1 — no navbar/footer, no Supabase UMD; polls window.ukgtResearchTool (100ms × 50) then init(); back on Section 0 → /uk-global-talent.html
├── ukgt-research-statement-tool.html  # Standalone UKGT Peer Review Tier 2 — no navbar/footer, no Supabase UMD; polls window.ukgtResearchStatementTool (100ms × 50) then init(); back on Section 0 → /uk-global-talent.html
├── ukgt-research-package-tool.html    # Standalone UKGT Peer Review Tier 3 — no navbar/footer, no Supabase UMD; polls window.ukgtResearchPackageTool (100ms × 50) then init(); back on Section 0 → /uk-global-talent.html
├── o1a-audit-tool.html        # Standalone O-1A Petition Audit — no navbar/footer, no Supabase UMD; 6 screens (form/review/payment/loading/download/error); polls window.o1aAuditTool (100ms × 50) then init(); PENDING live e2e validation
├── eb1a-audit-tool.html       # Standalone EB-1A Petition Audit — no navbar/footer, no Supabase UMD; 6 screens; polls window.eb1aAuditTool (100ms × 50) then init(); back → /eb-immigration.html
├── eb2-audit-tool.html        # Standalone EB-2 NIW Petition Audit — no navbar/footer, no Supabase UMD; 6 screens; polls window.eb2AuditTool (100ms × 50) then init(); back → /eb-immigration.html
├── ukgt-audit-tool.html       # Standalone UKGT Endorsement Audit — no navbar/footer, no Supabase UMD; 6 screens; polls window.ukgtAuditTool (100ms × 50) then init(); route selector (tech_nation/peer_review); single SKU ukgt_audit $59; back → /uk-global-talent.html
├── pathway-match-tool.html    # Standalone Pathway Match Report — no navbar/footer, no Supabase UMD; 5 screens (pm-form/review/payment/loading/download); polls window.pathwayMatchTool (100ms × 50) then init(); back on Section 0 → /pathway-compare.html
├── petition-audit.html        # Petition Audit landing/router page — lp-nav + lp-hero + lp-section; 2×2 audit-router-grid; routes to 4 per-type audit tools; no JS; static links only
├── admin.html               # Admin panel — not linked publicly; loads css/admin.css + js/admin.js; no Supabase UMD; no styles.css
├── sitemap.xml
├── robots.txt
├── _headers                 # Cloudflare Pages server-level HTTP response headers — sets the site-wide Content-Security-Policy; authoritative CSP source (replaces any per-page meta CSP tags, which have been removed)
├── stripe-api.js            # Backend Stripe route scaffold (reference only — not imported by frontend)
├── orabo-audit-report.html  # Static audit report (not a live app page)
└── CLAUDE.md
```

**Module size rule:** Every JS and CSS module must stay under 600 lines. `index.html` is exempt. Extract data or sub-sections into a sibling file when approaching the limit.

**Note:** `js/dashboard.js` still exists on disk but is no longer imported.

---

## Backend Structure (`japaconnect-backend`)

```
japaconnect-backend/
├── server.js            # Express app, route mounts
├── scheduler.js         # node-cron jobs with retry logic; includes hourly publish-notification cron (sends Brevo email when approved=true AND email_sent=false)
├── digest.js            # Weekly newsletter via Brevo
├── db/client.js         # pg Pool
├── routes/
│   ├── admin.js         # Admin CRUD routes — protected by requireAdmin (x-admin-token header); uses pool (Orabo Main) for content CRUD + Supabase JS client (Orabo Prod) for user management
│   ├── stripe.js        # Checkout, portal session, webhooks → Orabo Prod
│   ├── checklist.js     # AI document checklist (Claude + ai_cache) → Orabo Prod
│   ├── report.js        # AI readiness report (Claude + ai_cache) → Orabo Prod; sets res.setTimeout(180000) + req.setTimeout(180000) at route top
│   ├── cv.js            # CV Review + Cover Letter — AI generation + PDF/DOCX via docgen.js → Orabo Prod
│   ├── sop.js           # SOP Writer — AI generation + PDF/DOCX via docgen.js → Orabo Prod
│   ├── docgen.js        # generatePdf() + generateDocx() — both handle HTML and markdown input via isHtml() check (no DB)
│   ├── universities.js  # CRUD + pagination → Orabo Main
│   ├── scholarships.js  # CRUD + pagination → Orabo Main
│   ├── visas.js         # CRUD + pagination → Orabo Main
│   ├── opportunities.js # Community submissions → Orabo Main; duplicate check by normalized link (409 { error:'duplicate' } if found)
│   ├── ukgt-tech.js     # UKGT Tech Nation AI route — mounted at /api/ukgt-tech → Orabo Prod; imports prompts from ukgt-tech-prompts.js
│   ├── ukgt-tech-prompts.js  # Extracted system prompts — exports UKGT_TECH_SYSTEM_PROMPT (Tier 1) and UKGT_TECH_STATEMENT_SYSTEM_PROMPT (Tier 2) via module.exports (no DB)
│   ├── dashboard.js     # Dashboard APIs → Orabo Prod + Orabo Main; feature-flagged behind DASHBOARD_V2_ENABLED. Routes: GET /state, GET /recent-purchases, GET /matched-opportunities, POST /preferences (Phase 5). POST /preferences validates ALLOWED_DESTINATIONS/PATHWAYS/FIELDS, writes user_profiles.user_preferences, fires Brevo sync (DESTINATION, PREF_PATHWAY, PREF_FIELD)
│   ├── quiz.js          # POST /api/quiz/save-results → Orabo Prod (user_profiles.quiz_results). JWT-gated via resolveAuthUser, feature-flagged via DASHBOARD_V2_ENABLED, rate-limited 10/min per IP. Dashboard Rebuild Phase 3.
│   ├── audit.js         # Petition Audit — mounted at /api/audit with express.json({limit:'8mb'}) before global parser; dispatches by family ('o1a', 'eb1a', 'eb2' live; 'ukgt' future); verifyPayment gate; runAudit({family,text}) exported for offline calibration; 9-section HTML output → code-fence strip → generateDocx; NO ai_cache; AUDIT_HTML_DISCLAIMERS map (keyed by family) appended to every output
│   └── audit-prompts.js # Adversarial dual-persona prompts keyed by family — USCIS Adjudicating Officer (RFE-writing) + Senior Reviewing Immigration Attorney (remediation); officer calibration is the primary tuning risk
├── utils/
│   ├── constants.js     # ORIGIN_CURRENCY_MAP — 16 African countries mapped to local currency string (e.g. 'NGN (₦)')
│   └── document-ingest.js  # parseUploadedDocument (mammoth=DOCX, pdf-parse v2=PDF — pinned pdf-parse@^2.4.5, v2 uses new PDFParse({data}).getText() class API; paste=text), sanitizeForPrompt (strips <uploaded_petition> delimiter tags before prompt injection, case-insensitive), IngestError, MAX_WORDS=25000. New backend deps: mammoth, pdf-parse@^2.4.5.
└── scrapers/            # Cheerio/Playwright scrapers
```

---

## Feature Data Inventory

### Processing Times (`js/visatracker.js` + `js/visatracker-data.js`) — 18 visas
| Category | Visas |
|---|---|
| Study | F-1 (USA), Student Visa (UK), German Student, Australia 500, NZ Student, Portugal D4 |
| Work | UK Skilled Worker, UK Global Talent, O-1 (USA), EU Blue Card, Germany Job Seeker, EB-1A (USA), EB-2 NIW (USA) |
| PR | Express Entry (Canada), Canada PNP, Australia 190, Australia 189, NZ Skilled Migrant |

Each visa has: `id`, `label`, `country`, `flag`, `category`, `minWeeks`, `typicalWeeks` (optional), `maxWeeks`, `difficulty`, `difficultyColor`, `govUrl`, `note`, `milestones[]`. Trend data in `TREND_DATA` keyed by visa id (12 months, min/typical/max arrays). Live data from `/api/visa-tracker` (falls back to static).

### Cost of Living (`js/costliving.js`) — 33 cities
Grouped `<select>` dropdown. Data in `CITIES[]` and `COST_DATA{}` (monthly USD: rent, groceries, transport, utilities, dining). FX rates in `FX_RATES` (base USD). Comparison modal uses `compareSlots[]` (1–4 slots). `buildCityOptions(selectedId)` generates grouped `<optgroup>` options using `COUNTRY_ORDER` and `COUNTRY_FLAG_MAP` constants (also used by `buildCityDropdown()`). `compare-cities.html` uses same progressive slot pattern.

Countries: USA (5), Canada (4), UK (4), Ireland (2), Netherlands (2), Germany (3), Portugal (2), Australia (3), New Zealand (2), Nigeria (2), Kenya (1), Ghana (1), South Africa (2).

### Take-Home Pay Calculator (`js/paycalc.js` + `js/paycalc-data.js`) — 35 roles × 9 countries
`js/paycalc-data.js` exports `ROLES[]`, `ROLE_GROUPS`, `SALARY_DATA{}`. 18 countries across 3 groups: **Original Destinations** (USA, Canada, UK, Germany, Netherlands, Ireland, Australia, New Zealand, Portugal), **New Destinations** (UAE, France, Sweden, Switzerland, Spain), **African Origin** (Nigeria, Kenya, Ghana, South Africa — lets users see their current salary alongside destination salary). Sectors: Technology (9), Healthcare (6), Education (3), Business & Finance (8), Engineering (4), Trades (5). FX_RATES in `paycalc.js` extended with AED (3.67 fixed peg), SEK (10.30), CHF (0.88) — calculator-internal display only, not in user-facing currency dropdown. Country `<select>` restructured with `<optgroup>` sections via `buildCountryOptions()` in `paycalc.js`; `all-countries-pay.html` picks up all 18 countries automatically from its local `COUNTRY_DATA` object.

`all-countries-pay.html` imports from `./js/paycalc-data.js` via `<script type="module">`. `COUNTRY_DATA` holds 9 countries with `costOfLiving` values aligned to `paycalc.js`'s `COL_MONTHLY_USD`. All event wiring via `addEventListener`.

### Currency list (standard — all dropdowns)
**USD, GBP, EUR, CAD, NGN, KES, GHS, ZAR, AUD, NZD** (this exact order). FX rates (base USD): GBP 0.79, EUR 0.92, CAD 1.37, NGN 1580, KES 129, GHS 15.5, ZAR 18.5, AUD 1.55, NZD 1.63.

### CV Review + Cover Letter Generator (`js/cvtool.js`) — one-time purchase
Three output types: `cv_review` ($18), `cv_cover_letter` ($28), `cover_letter_only` ($12). Multi-step form — `ALL_SECTIONS = CV_SECTIONS` (6 sections; section 0 "Your details" captures `user_name` + `user_email`). Standalone page `cv-tool.html` polls for `window.cvTool` then calls `init()`. Stripe returns to `cv-tool.html?payment_success=1&item=cv_review`. API response: `{ success, result, files: { cv_docx?, cl_docx? }, firstName }` — Word download only, no PDF. Backend success_url for cv items is `cv-tool.html`. No auth required.

### SOP Writer (`js/soptool.js`) — one-time purchase
Three output types: `sop_scratch` ($35), `sop_review` ($28), `sop_rewrite` ($30). Multi-step form — `ALL_SECTIONS = SOP_SECTIONS` (7 sections; section 0 "Your details" captures `user_name` + `user_email`). Same standalone + poll pattern as cvtool. API response: `{ success, result, files: { sop_docx }, firstName }` — Word download only. Backend route `/api/sop`. Backend success_url for sop items is `sop-tool.html`. No auth required.

---

## CV/SOP Tool Architecture

Both tools use an identical pattern.

### HTML structure (standalone pages)
`cv-tool.html` and `sop-tool.html` are clean standalone pages — no navbar, no footer, no Supabase UMD script. The wrapper `#cv-tool-modal` / `#sop-tool-modal` contains five screen divs: `#cv-form-screen`, `#cv-review-screen`, `#cv-payment-screen`, `#cv-loading-screen`, `#cv-download-screen` (same with `sop-` prefix). All hidden by CSS in `css/cvtool.css`. The inline non-module `<script>` polls `window.cvTool` / `window.sopTool` (100 ms interval, max 50 attempts) then calls `init()`.

### Screen visibility
`showModal()` adds `.active` to `#cv-tool-modal` → CSS rule `#cv-tool-modal.active { display: block; }` shows it. `showScreen(id)` removes `.active` from all five screen divs then adds it to the target → CSS rule `#cv-form-screen.active { display: block; }` etc. Never use `style="display:..."` on these elements.

### Form rendering
`ALL_SECTIONS = CV_SECTIONS` (6 sections) / `ALL_SECTIONS = SOP_SECTIONS` (7 sections). The question files already include "Your details" (user_name, user_email) as section 0 — do NOT prepend a separate DETAILS_SECTION. `renderSection(index)` re-renders the entire `#cv-form-screen` on every navigation, including progress header, Back button, body questions, and Next button.

**Back button (`.tool-back-btn`):** rendered into the form body above the section title on every section. Section 0: label "← Back to Home" → `window.location.href = '/'`. Section 1+: label "← Back" → `saveCurrentAnswers(index)` then `renderSection(index - 1)`. There is no separate nav-area back button — do not add one back.

**Next button (`.tool-next-btn`):** accent blue pill (self-contained styles in `css/cvtool.css`). Label is "Next →" on all sections except the last, where it reads "Review your answers →" (advances to the review screen, not directly to payment). Do NOT use `btn-primary` for this button — bare `btn-primary` lacks the `.btn` base class in this context and renders unstyled.

Supported question types: `text`, `email`, `select`, `textarea`, `multiselect`, `conditionalSub` (sub-textarea shown when select hits trigger value), `conditional` (question omitted unless parent answer includes a keyword), `fileUpload` (.txt,.pdf,.docx → client-side extraction via `js/cv-file-extract.js` into the sibling textarea; see CV file extraction below), `large` (tall textarea).

**CV file extraction (`js/cv-file-extract.js`):** ES module imported by `cvtool.js`. Handles `.txt` (FileReader readAsText), `.pdf` (pdfjs-dist@4.10.38 ESM — vendored at `js/vendor/pdf.min.mjs`; worker at `js/vendor/pdf.worker.min.mjs` loaded via `workerSrc = new URL('./vendor/pdf.worker.min.mjs', import.meta.url).href`), `.docx` (mammoth@1.8.0 browser UMD — vendored at `js/vendor/mammoth.browser.min.js`, loaded as non-module `<script>` in cv-tool.html before the module scripts to set `window.mammoth`). Guards: 10 MB size limit (`too_large`); MIN_TEXT_CHARS=20 non-whitespace chars to detect scanned/image-only PDFs (`empty`); unsupported extension (`unsupported`); any thrown exception (`parse_failed`). Loading state: `readingMsg` span with class `cv-file-reading-msg`; `.active` added during extraction, removed after. File input disabled during extraction; `_extracting` flag prevents concurrent runs. CSP: `worker-src 'self'` in `_headers` covers the same-origin worker URL — no `blob:` needed; no `unsafe-eval` added; pdf.js v4 no-eval fallback activates automatically under strict CSP. mammoth non-module loading order guarantee: non-module `<script>` executes synchronously before any deferred module script, so `window.mammoth` is set before `cv-file-extract.js` evaluates. **Do not add `unsafe-eval` to script-src** if eval-related CSP warnings appear in the console — the pdf.js no-eval fallback is the correct fix and activates automatically.

### Payment return flow (sessionStorage bridge)
**Success path:**
1. User completes review screen → "Continue to payment →" → payment screen → "Select & Pay" → `sessionStorage.setItem('orabo_cv_answers', JSON.stringify(answers))` + `orabo_cv_output_type` → `purchaseItem(pkg.type, btn, { customer_email: answers.user_email })`
2. Stripe redirects to `cv-tool.html?payment_success=1&item=cv_review`
3. `init()` → `handlePaymentReturn()` checks two-part condition: `payment_success=1` in URL AND `orabo_cv_answers` in sessionStorage
4. Clears URL via `history.replaceState`, removes sessionStorage keys, shows loading screen
5. POSTs `{ outputType: item, formAnswers: parsedAnswers }` to `/api/cv` — **field name is `formAnswers`, not `answers`**
6. On success: `showDownloadScreen(data, userEmail)` renders Word download buttons from `data.files.cv_docx` / `data.files.cl_docx`

**Cancel path:**
1. User abandons Stripe checkout → Stripe redirects to `cv-tool.html?payment_cancelled=1&item=cv_review`
2. `init()` checks: if neither `payment_success=1` nor `payment_cancelled=1`, wipes sessionStorage (clean start). Otherwise preserves it.
3. `handlePaymentReturn()` detects `payment_cancelled=1` + `orabo_cv_answers` in sessionStorage → clears URL, restores answers, shows review screen
4. User can edit answers or proceed to payment again without re-filling the form

No userId passed for anonymous flow — backend skips purchases table check when userId absent. Download is Word only (DOCX) — no PDF. `generatePdf()` produces blank output for HTML input.

### API response shapes
- `/api/cv` → `{ success, result, files: { cv_docx?, cl_docx? }, firstName }` — `cv_docx` for cv_review, `cl_docx` for cover_letter_only, both for cv_cover_letter
- `/api/sop` → `{ success, result, files: { sop_docx }, firstName }`

---

## Document Checklist & Readiness Report Architecture

### Wizard pathway and destination selects
The checklist wizard (all three pages: `index.html`, `doc-checklist.html`, `full-doc-checklist.html`) uses `<select>` dropdowns for pathway (Stage 1) and destination (Stage 2) — **pathway cards and country pills were removed**. New element IDs:
- `#checklist-pathway-select` — Stage 1 pathway dropdown; 13 pathways (see below); value is a snake_case key (e.g. `work_visa`)
- `#checklist-destination-select` — Stage 2 destination dropdown; 9 standalone countries (USA, Canada, UK, Australia, NZ, Russia, China, Brazil, South Africa) + `<optgroup label="Western Europe">` with 19 countries matching `#opp_country` in the opportunities form
- `#pathway-select-error` / `#destination-select-error` — validation error divs (`.visible` class pattern, same as `.country-select-error`)

**wizardNext1 and wizardNext2 are no longer disabled in HTML.** Pathway and destination validation is handled inline inside `checklist.js`'s click handlers: if the select has no value, the appropriate error div gets `.visible` added and the handler returns.

**Pathway select values and counts:** `work_visa` (15), `study` (13), `permanent_residency` (18), `us_green_card` (20), `job_seeker_visa` (11), `eu_blue_card` (14), `family_partner` (13), `critical_skills_visa` (14), `investor_business_visa` (16), `digital_nomad_visa` (11), `global_talent_visa` (16), `self_employment_visa` (12), `working_holiday_visa` (10). The full option text (e.g. "Work Visa (Skilled Worker, H-1B, 482)") is stored as `checklistPathway` and sent to the backend API. The snake_case value is stored as `checklistPathwayKey` for `CHECKLIST_DOC_COUNTS` lookup and cancel-return restoration.

**`jc_checklist_state` shape:** `{ pathway: "<full label>", pathwayKey: "<snake_case>", country: "<country name>" }`. `oraboChecklistRestore(state)` sets `pathwaySel.value = state.pathwayKey` and `destSel.value = state.country`.

### Country-of-origin selection (mandatory)
- Checklist: `id="checklist-country-origin"` in `#wizardStage1` (only on `doc-checklist.html` and `full-doc-checklist.html` — not on `index.html`). First option is `<option value="" disabled selected>— Select your country —</option>` — no pre-selected country. `jc_checklist_origin` is saved as `sel.value` (empty string if not selected; the 'Nigeria' fallback was removed).
- Quiz: `id="report-country-origin"` in `#quizQuestionScreen`, validated before `#quizNext`

`checklist-origin-saver.js` and `quiz-origin-saver.js` use capture-phase `addEventListener('click', fn, true)`. If dropdown empty: `e.stopImmediatePropagation()` blocks module handler; `.country-select-error.visible` shows. Do NOT modify `checklist.js` or `quiz.js`.

### SessionStorage bridge
Before Stripe redirect: `checklist.js` saves `jc_checklist_state` (see shape above); `quiz.js` saves `jc_quiz_state`; origin savers save `jc_checklist_origin` / `jc_quiz_origin`. After return, `payment-return-*.js` reads and clears all keys, passes `country_of_origin` in API request body. Both scripts call `fetch()` directly inside `DOMContentLoaded` — no `supabaseClient.auth.getSession()` await before the request (that await was the root cause of the API call never firing). `userId` is read from `state.userId || null`; checklist.js/quiz.js do not currently save userId in state so it passes as null. AbortController timeouts: 100 s for checklist, 180 s for report. Message rotation interval: 8 s for checklist, 15 s for report. Console prefixes: `[checklist-return]` / `[quiz-return]`.

### API response format
`/api/checklist` and `/api/report` return `{ "success": true, "checklist/report": "<html>", "docx": "<base64 string>" }`. `docx` present on both cache hits and fresh generations.

### DOCX-only download
Buttons say "Download Checklist (Word)" / "Download Report (Word)" — DOCX only, no PDF. `payment-return-*.js` wires download button in capture phase inside `.then()`, calls `e.stopImmediatePropagation()` to block module's html2pdf handler. Uses `atob()` → `Uint8Array` → `Blob` → `URL.createObjectURL` → `<a>` click. Never add PDF — `generatePdf()` produces blank output for HTML input.

### Checklist knowledge base (backend — `utils/checklist-knowledge.js` + `utils/checklist-pathway-data.js`)
The checklist AI system is driven by two utility modules (neither is a route — they are imported by `routes/checklist.js`):

**`utils/checklist-pathway-data.js`** (442 lines) — pure data; exports `PATHWAY_KNOWLEDGE_BASE`:
- 13 entries keyed by snake_case pathway key (e.g. `work_visa`, `digital_nomad_visa`)
- Each entry: `{ display_name, what_it_is, eligibility_gate, core_documents[], destination_variants{}, common_pitfalls[] }`
- `eligibility_gate` fields timestamped `"Verified 2026-05-14"` against authoritative sources (USCIS, gov.uk, canada.ca, etc.)
- Destination variant keys use the exact frontend dropdown values (e.g. `'United States'` not `'USA'`, `'United Kingdom'` not `'UK'`)
- `working_holiday_visa` entry explicitly states no African passport qualifies (all bilateral agreements are with European/South American countries only)
- `critical_skills_visa` scoped to South Africa (SAQA evaluation required, R1,089 fee) and Ireland (Critical Skills Employment Permit, salary ≥€40,904/year from March 2026) only
- `investor_business_visa` lists African countries with US E-2 Treaty eligibility: Cameroon, Congo, Egypt, Ethiopia, Liberia, Morocco, Senegal, Togo, Tunisia
- `digital_nomad_visa` flags no-DNV destinations: USA, Canada, UK, Australia, NZ, Russia, China, Brazil, South Africa, Ireland

**`utils/checklist-knowledge.js`** (437 lines) — logic; exports `CHECKLIST_PROMPT_VERSION`, `INFEASIBLE_COMBINATIONS`, `buildKnowledgeBlock`, `buildInfeasibilityGuardrail`:
- `CHECKLIST_PROMPT_VERSION = 'v2.2-2026-05-21'` — bump this string whenever knowledge base content changes to invalidate all Supabase cache entries automatically
- `ORIGIN_AUTHENTICATION_CONTEXT` — 16 entries (15 African countries + DEFAULT):
  - `hague_apostille_member: true/false/null` — verified against HCCH status table 2026-05-14
  - Hague **members**: South Africa (1995), Morocco (2016), Senegal (2023), Rwanda (2024)
  - Hague **non-members**: Nigeria, Ghana, Kenya, Ethiopia, Tanzania, Uganda, Cameroon, Zimbabwe, Zambia, Côte d'Ivoire, Egypt (requires full consular legalisation chain for each destination)
  - `document_authentication` — complete legalisation chain per origin country
  - `credential_evaluation_notes` — WES (Canada/USA), NARIC/ENIC (UK), anabin (Germany/France), NOOSR (Australia)
  - `passport_quirks[]` — TB test requirements (India/Pakistan processing centres), yellow fever certificates, biometric appointment wait times
  - `common_destinations_quick_notes{}` — keyed by destination country name, e.g. Nigeria→UK: mandatory TB test at approved clinic in Lagos/Abuja
  - `verified: '2026-05-14'` on every entry
- `INFEASIBLE_COMBINATIONS` — 6 rules, each with `detect(pathwayKey, dest, origin)` function, `explanation`, `closest_alternative`:
  1. `working_holiday_visa` + any African passport → alternative: Job Seeker Visa or Skilled Worker Visa
  2. `us_green_card` + destination ≠ 'United States' → always destination-locked
  3. `eu_blue_card` + destination not in EU_BLUE_CARD_ELIGIBLE (Denmark and Ireland opted out; EEA-only countries excluded) → alternative: Skilled Worker for that country
  4. `critical_skills_visa` + destination not South Africa or Ireland → alternative: Skilled Worker for that country
  5. `digital_nomad_visa` + destination has no DNV programme → alternative: Work Visa for that country
  6. `investor_business_visa` + USA + African origin not in E-2 treaty list → alternative: EB-5 or O-1 Visa
- `buildInfeasibilityGuardrail()` — returns static 6-line protocol telling the AI to emit a ⚠ warning header then generate the alternative checklist
- `buildKnowledgeBlock(pathwayKey, destination, origin)` — builds Block 1 system prompt text: pathway knowledge entry + origin authentication context + 8-rule output format

**Backend routing in `routes/checklist.js`:**
- `PATHWAY_DISPLAY_TO_KEY` map (13 entries): translates the full option text sent from the frontend (e.g. `'Work Visa (Skilled Worker, H-1B, 482)'`) → internal snake_case key (e.g. `'work_visa'`). Returns 400 if the exact text doesn't match — this guards against stale frontend values.
- `VALID_DESTINATIONS` Set (28 values): uses full country names matching the frontend dropdown exactly (`'United States'`, `'United Kingdom'` — NOT `'USA'`/`'UK'`). Returns 400 for unrecognised destinations.
- Infeasible combinations skip both cache read AND cache write — they always generate fresh with the guardrail active in Block 2
- Cache key format: `checklist:${CHECKLIST_PROMPT_VERSION}:${pathwayKey}:${country}:${countryOfOrigin}` — includes version, pathway, destination, and origin for accurate per-user personalisation. Up to ~5,824 unique cache entries (13 × 28 × 16).

### Brand sanitisation
`routes/checklist.js` and `routes/report.js` run `sanitizeBrand()` on every Claude response (cache hits and fresh): replaces "Japa Readiness", "JapaConnect", "japaconnect", "japa connect" with "Orabo"/"Orabo Readiness". Sanitised content is written to `ai_cache`. `routes/checklist.js` also strips markdown code fences from the raw Claude response before `sanitizeBrand()` and before writing to `ai_cache`: `content = content.replace(/^```html\s*/i, '').replace(/```\s*$/i, '')`.

### Currency personalisation
`country_of_origin` in request body → backend resolves local currency from `ORIGIN_CURRENCY_MAP` → injects second non-cached system block (Block 2). Block 1 (knowledge base + infeasibility guardrail) is cached with `cache_control: { type: 'ephemeral' }`. Infeasible combination details are also injected into Block 2 — never into the cached Block 1.

### Dynamic scholarship and university generation (report only)
`routes/report.js` does NOT query `scholarships` or `universities` tables. Claude generates 6–8 scholarships and 6–8 universities from `VISA_KNOWLEDGE_BASE`. `max_tokens` for `/api/report` is 8000.

### Pro plan bypass
`js/pro-bypass-checklist.js` and `js/pro-bypass-quiz.js` intercept pay button in capture phase, check Supabase `pro_status`, call API directly if Pro. Non-module scripts loaded last. Do NOT add bypass logic to `checklist.js` or `quiz.js`.

**Pro bypass scripts read JWT from localStorage, NOT via getSession().** These scripts run inside iframes embedded in `dashboard.html`. Calling `supabaseClient.auth.getSession()` from the iframe hangs indefinitely because two GoTrueClient instances conflict (one in the parent, one in the iframe), surfacing as "Multiple GoTrueClient instances detected" in the console. Instead, use `readSessionFromStorage()` from `js/auth.js` (also inlined verbatim in each bypass IIFE), which reads the persisted Supabase session synchronously from localStorage (key `sb-<project-ref>-auth-token`). If the token is missing or expired, abort the bypass and fall through to the normal Stripe flow. Do NOT revert to `getSession()` in these scripts.

### Free quiz and docgen
Free quiz on `index.html`: country-of-origin validation in `js/free-quiz-origin.js` (non-module). `generateDocx()` checks `isHtml(content)` — do NOT pre-process checklist/report HTML to markdown before calling.

---

## Auth Flow

1. Sign Up → `signup.html` → `supabase.auth.signUp()` → verification email
2. Email link → `login.html?verified=true`
3. Login → `dashboard.html`
4. `dashboard.html` calls `supabase.auth.getUser()` — redirects to `login.html` if no session
5. `openAuthModal()` in `auth.js` redirects to `login.html`

---

## Integrations

### Supabase — Dual-DB Architecture

Orabo runs on two Supabase projects under the Oraboss organisation, deliberately split by role.

#### 1. Orabo Prod (operational / transactional)
- **Host:** `rrkkbnoqubfmhrnxgngp.supabase.co`
- **Region:** us-west-2 (West US Oregon)
- **Backend env vars:** `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY`
- **Backend client:** Supabase JS client (`createClient`) instantiated inline in each route that needs it — `routes/admin.js`, `routes/checklist.js`, `routes/cv.js`, `routes/sop.js`, `routes/eb1a.js`, `routes/eb2.js`, `routes/o1a.js`, `routes/report.js`, `routes/stripe.js`, `routes/ukgt-tech.js`, `routes/quiz.js`, `routes/audit.js`, `utils/verify-payment.js`
- **Tables:** `user_profiles`, `purchases`, `one_off_purchases`, `ai_cache`, `processed_webhook_events`
- **Holds:** user accounts, payments, AI cache, webhook idempotency ledger
- Auth lives here (email/password). Email verification redirect: `https://orabo.app/login.html?verified=true`. Password reset redirect: `https://orabo.app/reset-password` (dedicated `reset-password.html` page; matches the Supabase Redirect URLs allow-list entry — note NO `.html` in the `redirectTo` string passed to `resetPasswordForEmail`).
- `user_profiles` columns: `id`, `pro_status`, `pro_expires_at`, `stripe_customer_id`, `stripe_subscription_id`, `quiz_results` (jsonb), `user_preferences` (jsonb), `journey_state` (jsonb). JSONB columns added 2026-05-16 in Phase 1 of dashboard rebuild — see backend repo `migrations/2026-05-16-dashboard-rebuild-p1.sql`.
- `one_off_purchases` columns: `id` (uuid pk), `name` (text), `email` (text), `tool` (text), `stripe_session_id` (text, nullable), `created_at` (timestamptz) — audit log for anonymous CV/SOP purchases

#### 2. Orabo Main (content / CMS / analytics)
- **Host:** `fcogjoxcfmrcuvtreivm.supabase.co`
- **Region:** us-east-1 (East US Virginia)
- **Backend env vars:** `DB_HOST` + `DB_PORT` + `DB_NAME` + `DB_USER` + `DB_PASSWORD` (direct PostgreSQL connection to Supabase's managed Postgres; `DB_HOST` is `db.fcogjoxcfmrcuvtreivm.supabase.co`)
- **Backend client:** raw `pg.Pool` instantiated in `db/client.js`; imported as `pool` by all content routes
- **Tables:** `opportunities`, `scholarships`, `universities`, `visas`, `stories`, `visits`, `scrape_logs`, `post_earn_rewards`, `visa_processing_snapshots`
- **Holds:** scraper-fed content powering public listing pages (University Finder, Scholarships & Funding, Visa Tracker), community-submitted opportunities, visitor analytics, post-and-earn reward ledger
- Routes that hit this DB: `routes/opportunities.js`, `routes/scholarships.js`, `routes/universities.js`, `routes/visas.js`, `routes/stories.js`, `routes/visits.js`, `routes/visa-tracker.js`. `routes/admin.js` hits **both** DBs — uses `pool` for content CRUD and the Supabase JS client for user management.
- `opportunities` extra columns added by migration: `status` (text, default 'pending'), `submitter_phone` (text), `rejection_reason` (text)
- `post_earn_rewards` columns: `id` (uuid pk), `email` (text unique), `phone` (text), `name` (text), `approved_count` (int, default 0), `total_paid` (int, default 0), `reward_paid` (bool, default false), `reward_paid_at` (timestamptz), `created_at`, `updated_at` — upserted on each opportunity approval; counter resets to 0 after each payout

#### Cross-cutting rules
- Never mix clients in a single route (except `admin.js` which intentionally spans both). If a new route needs data from both DBs, fetch from each then combine in memory — do not attempt a cross-DB JOIN.
- New tables go where their role belongs: user/auth/payment/AI-state → Orabo Prod; content/CMS/scraper output/analytics → Orabo Main.
- Migrations target one project at a time. Always confirm the project ref in the Supabase URL bar matches the intended target before running any ALTER TABLE, DROP, or destructive SQL.
- Both projects are on Supabase Pro with daily automated physical backups. No manual download is required before migrations.

### Stripe
- Pro subscription price ID: `price_1TO3hu2Nqh05kvDpKAmtEYjs` — stored in `STRIPE_PRO_PRICE_ID` Railway env var; backend reads `process.env.STRIPE_PRO_PRICE_ID` — never from frontend request body
- One-time items: `readiness_report` ($15), `document_checklist` ($9), `verified_badge` ($5), `consultation_express` ($55), `consultation_full` ($90), `consultation_doc_review` ($35), `cv_review` ($18), `cv_cover_letter` ($28), `cover_letter_only` ($12), `sop_scratch` ($35), `sop_review` ($28), `sop_rewrite` ($30), `eb1a_assessment` ($39), `eb2_assessment` ($35), `eb1a_letter` ($150), `eb2_letter` ($140), `eb1a_package` ($250), `eb2_package` ($240), `o1a_assessment` ($39), `o1a_letter` ($150), `o1a_package` ($250), `ukgt_tech_assessment` ($39), `ukgt_tech_statement` ($150), `ukgt_tech_package` ($250), `ukgt_research_assessment` ($39), `ukgt_research_statement` ($150), `ukgt_research_package` ($250), `o1a_audit` ($59), `eb1a_audit` ($59), `eb2_audit` ($59), `ukgt_audit` ($59), `pathway_match_report` ($19). Success URL: `pathway-match-tool.html?payment_success=1&item=pathway_match_report`. `TOOL_CANCEL_PAGES` + `js/stripe.js` bypass list: **32 keys** (added `ukgt_audit` in Phase Petition Audit Phase 5).
- **Subscription return URLs:** `dashboard.html?payment=success` / `dashboard.html?payment=cancelled`
- **One-time return URLs (cv/sop):** `cv-tool.html?payment_success=1&item=...` / `sop-tool.html?payment_success=1&item=...`
- **One-time return URLs (consultation):** `consult-booking.html?payment_success=1&item=consultation_*` — stripe.js `checkPaymentReturn` IIFE calls `_showCalendlyOverlay()`
- **One-time return URLs (other):** `index.html?payment_success=1&item=...` / `index.html?payment=cancelled`
- **Cancel return URLs (tool items):** `cv_review`, `cv_cover_letter`, `cover_letter_only` → `cv-tool.html?payment_cancelled=1&item=<key>`; `sop_scratch`, `sop_review`, `sop_rewrite` → `sop-tool.html?payment_cancelled=1&item=<key>`; `eb1a_assessment` → `eb1a-tool.html?payment_cancelled=1&item=eb1a_assessment`; `eb2_assessment` → `eb2-tool.html?payment_cancelled=1&item=eb2_assessment`; `eb1a_letter` → `eb1a-letter-tool.html?payment_cancelled=1&item=eb1a_letter`; `eb2_letter` → `eb2-letter-tool.html?payment_cancelled=1&item=eb2_letter`; `eb1a_package` → `eb1a-package-tool.html?payment_cancelled=1&item=eb1a_package`; `eb2_package` → `eb2-package-tool.html?payment_cancelled=1&item=eb2_package`; `o1a_assessment` → `o1a-tool.html?payment_cancelled=1&item=o1a_assessment`; `o1a_letter` → `o1a-letter-tool.html?payment_cancelled=1&item=o1a_letter`; `o1a_package` → `o1a-package-tool.html?payment_cancelled=1&item=o1a_package`; `ukgt_tech_assessment` → `ukgt-tech-tool.html?payment_cancelled=1&item=ukgt_tech_assessment`; `ukgt_tech_statement` → `ukgt-tech-statement-tool.html?payment_cancelled=1&item=ukgt_tech_statement`; `ukgt_tech_package` → `ukgt-tech-package-tool.html?payment_cancelled=1&item=ukgt_tech_package`; `ukgt_research_assessment` → `ukgt-research-tool.html?payment_cancelled=1&item=ukgt_research_assessment`; `ukgt_research_statement` → `ukgt-research-statement-tool.html?payment_cancelled=1&item=ukgt_research_statement`; `ukgt_research_package` → `ukgt-research-package-tool.html?payment_cancelled=1&item=ukgt_research_package`; `document_checklist` → `doc-checklist.html?payment_cancelled=1&item=document_checklist`; `full_doc_checklist` → `full-doc-checklist.html?payment_cancelled=1&item=full_doc_checklist`; `readiness_report` → `readiness-report.html?payment_cancelled=1&item=readiness_report`; `consultation_express` / `consultation_full` / `consultation_doc_review` → `consult-booking.html?payment_cancelled=1&item=<key>`; `o1a_audit` → `o1a-audit-tool.html?payment_cancelled=1&item=o1a_audit`; `eb1a_audit` → `eb1a-audit-tool.html?payment_cancelled=1&item=eb1a_audit`; `eb2_audit` → `eb2-audit-tool.html?payment_cancelled=1&item=eb2_audit`; `ukgt_audit` → `ukgt-audit-tool.html?payment_cancelled=1&item=ukgt_audit`; `pathway_match_report` → `pathway-match-tool.html?payment_cancelled=1&item=pathway_match_report`. Dashboard-originated items still cancel to `dashboard.html?payment=cancelled`; all others to `/?payment=cancelled`. Managed via `TOOL_CANCEL_PAGES` map (32 keys total) in `routes/stripe.js`.
- **Standalone page return URLs:** `readiness_report` → `readiness-report.html?payment_success=1&item=readiness_report`; `document_checklist` → `doc-checklist.html?payment_success=1&item=document_checklist`; `full_doc_checklist` → `full-doc-checklist.html?payment_success=1&item=full_doc_checklist`. Handled by `payment-return-*.js`, not `stripe.js`.
- Webhook events: `checkout.session.completed`, `customer.subscription.deleted`, `invoice.payment_succeeded`, `invoice.payment_failed`, `charge.refunded`
- `pro_expires_at` from Stripe's actual `current_period_end` — never hardcode duration
- Required Railway env vars: `STRIPE_SECRET_KEY`, `STRIPE_PRO_PRICE_ID`, `STRIPE_WEBHOOK_SECRET`, `AI_ROUTES_REQUIRE_STRIPE_SESSION`, `DASHBOARD_V2_ENABLED`

### Brevo
- Weekly digest: Saturday 08:00 UTC (`digest.js` → `scheduler.js`)
- Subscribe: `POST /api/newsletter/subscribe` — accepts `email` (required), `destination` (optional), `name`, `source`, `quiz_score`, `quiz_date`. Only adds to list when `destination` provided. When `quiz_score` present, sets `QUIZ_SCORE`, `QUIZ_DATE`, `QUIZ_LEAD='true'` on contact. Callers without `destination` (EB waitlist, quiz capture) now succeed.
- Frontend: `js/newsletter.js` — `subscribeToDigest()` + `waitlistDigestHook()`
- Transactional emails: consultation confirmation (via `routes/stripe.js`); checklist/report delivery fire-and-forget after generation + cache hits; CV/SOP delivery fire-and-forget from `routes/cv.js` and `routes/sop.js` — email includes specific output type label + upsell paragraph (CV→SOP Writer for `cv_review`/`cover_letter_only`; SOP→Consultation for all SOP types)
- CV/SOP purchase attribute update (fire-and-forget): after each delivery, `PURCHASE_DATE` (ISO date) + `LAST_TOOL` (outputType) set on the Brevo contact via `POST /v3/contacts` with `updateEnabled: true`
- `userId` passed in request body from `supabaseClient.auth.getSession()` for checklist/report; CV/SOP use `user_email` from formAnswers (no auth required)
- Required Brevo custom attributes (create in dashboard if not present): `DESTINATION` (text), `QUIZ_SCORE` (number), `QUIZ_DATE` (text), `QUIZ_LEAD` (text), `PURCHASE_DATE` (date), `LAST_TOOL` (text), `LAST_PREVIEW_TOOL` (text), `LAST_PREVIEW_DATE` (date), `WORTHIT_TARGET` (text), `WORTHIT_VERDICT` (text)
- Env vars: `BREVO_API_KEY`, `BREVO_LIST_ID`, `BREVO_SENDER_EMAIL`, `BREVO_SENDER_NAME`
- **Email template logo:** all Brevo templates reference `https://orabo.app/orabo-icon-256x256.png` as a hotlinked PNG (`width="80" height="80"` required for Outlook). Never use `orabo-icon.svg` in email HTML — SVGs do not render in Outlook or many other clients. Inline styles are permitted in email HTML — the CSP rule applies to the web app only.

### Calendly
- Account: `calendly.com/oraboapp`
- Express (30 min): `https://calendly.com/oraboapp/30min`
- Full (1 hr): `https://calendly.com/oraboapp/full-consultation`
- Doc Review (45 min): `https://calendly.com/oraboapp/document-review`
- Flow: pay → `consult-booking.html?payment_success=1&item=consultation_*` → `stripe.js` IIFE shows `#calendlyOverlay`
- `#calendlyOverlay` in `consult-booking.html`, styled in `css/consultation.css`, controlled by `_showCalendlyOverlay(url)` in `stripe.js`
- `#bookingModal` is 2 steps only (About You → Review & Pay) — never re-add date/time picker

### EmailJS
- Service ID: `service_vcvthda`; Admin template: `template_3oaoxaa`; User auto-reply: `template_yva0uva`
- Admin template (`template_3oaoxaa`) fires on contact form submissions to notify the Orabo team. The user auto-reply (`template_yva0uva`) may still be wired to other forms (e.g. contact form) — grep before assuming it is removable elsewhere.

### Claude API
- Model: `claude-sonnet-4-6`; all use prompt caching on knowledge base system block
- `/api/checklist`: max_tokens 8000; two-block system prompt — Block 1 (infeasibility guardrail + pathway knowledge + origin auth context, cached via `cache_control: ephemeral`) + Block 2 (local currency + optional infeasibility marker, not cached); knowledge base in `utils/checklist-knowledge.js` + `utils/checklist-pathway-data.js`; infeasible combinations skip cache entirely; cache key includes `CHECKLIST_PROMPT_VERSION` — bump it in `checklist-knowledge.js` when rules change
- `/api/report`: max_tokens 8000; two-block system prompt — first (knowledge base, cached) + second (per-request currency, not cached)
- `/api/cv` and `/api/sop`: max_tokens 6000; two-block system prompt — first cached (gatekeeper persona + universal rules including no-fabrication rule for CV), second uncached per-request (target-country format/SOP rules + SOP track scaffold from `utils/document-norms.js`); no `ai_cache` reads or writes — every submission is generated fresh; no userId required
- `/api/checklist` checks Supabase `ai_cache` before calling Claude (feasible combinations only); stores sanitised results after; infeasible combinations never cached
- `/api/report` checks `ai_cache`; stores sanitised results after
- **When changing checklist immigration rules**, bump `CHECKLIST_PROMPT_VERSION` in `utils/checklist-knowledge.js` — this changes all cache keys and forces fresh generations for all combinations

---

## Dashboard Architecture (`dashboard.html`)

| Function | Purpose |
|---|---|
| `initDashboard()` | Auth check → profile fetch → `updateDashboardForPlan()` → `handleReturnFromStripe()` |
| `updateDashboardForPlan(plan)` | Single source of truth for all plan-state visual changes. Call on load AND after payment. |
| `handleReturnFromStripe()` | Detects `?payment=success/cancelled`, waits 1.5 s, re-fetches profile, calls `updateDashboardForPlan()`. No page reload. |
| `initiateStripeCheckout()` | POSTs `{ userId, email, mode: 'subscription' }` to `/api/create-checkout-session` — no `priceId` |
| `showProModal()` / `hideProModal()` | Opens/closes `#pro-modal` |
| `showToast(message, type)` | `type`: `'success'`, `'error'`, or `'info'` |
| `openPreferencesModal(prefill)` | Opens `#prefs-modal`; if `prefill` (user_preferences object) provided, pre-fills selects and date. Adds `preferences-modal-open` to `body`. |
| `closePreferencesModal()` | Removes `preferences-modal-open` from `body`. |
| `handlePreferencesSubmit(e)` | Form submit handler — validates required fields, reads JWT via localStorage scan, POSTs to `/api/dashboard/preferences`, calls `loadJourneyState()` on success. |
| `handleRetakeQuiz()` | Sets `sessionStorage.orabo_retaking_quiz='1'` then navigates top frame to `/#readiness`; `quiz-retake-handler.js` on index.html detects the flag and auto-starts the quiz. |
| `wireJourneyCtas()` | Wires static `data-cta` buttons + adds delegated click listener on `#dash-journey-panel` for `data-action="open-prefs-edit"` and `data-action="retake-quiz"` (Phase 5). |
| `loadJourneyState()` | Alias for `window.loadJourneyState` (set by dashboard-journey.js); call after preferences save to refresh all 5 widgets. |
| `refreshUsageStats(plan)` | Phase 6 dispatcher — if plan='pro' fetches `/api/dashboard/usage-stats` and renders Pro widgets (`#value-this-month-widget`, `#monthly-recap-widget`); if 'free' renders upgrade CTA only and skips the fetch. Called on page load (from DOMContentLoaded in dashboard-journey.js) and automatically via the `updateDashboardForPlan` wrap for mid-session upgrades. |
| `renderValueThisMonthWidget(data)` | Phase 6 — Pro only. Populates `#value-this-month-widget`. Shows 30d hours saved, 30d value, and 3-item tool list. Empty state on null/zero data. |
| `renderMonthlyRecapWidget(data, state)` | Phase 6 — Pro only. Populates `#monthly-recap-widget`. Shows lifetime hours saved, total purchases, and one nudge line computed from `_oraboLastJourneyState`. |
| `renderValueUpgradeCTAWidget()` | Phase 6 — Free users only. Populates `#value-upgrade-cta-widget`. Shows upgrade pitch + "Upgrade to Pro" button that calls `showProModal()`. |

- **Phase 6 widget visibility pattern:** Pro widgets (`#value-this-month-widget`, `#monthly-recap-widget`) carry `data-plan="pro"` in HTML — hidden by CSS rule `[data-plan="pro"]:not(.unlocked) { display: none }` in `dashboard-journey.css`. `refreshUsageStats('pro')` adds `.unlocked` to show them. Free-user CTA widget (`#value-upgrade-cta-widget`) has no `data-plan`; hidden by `refreshUsageStats('pro')` adding `.dj-widget--hidden` class.
- **`updateDashboardForPlan` wrap (Phase 6):** `dashboard-journey.js` wraps the inline-defined `window.updateDashboardForPlan` (not modifies it) to fire `refreshUsageStats(plan)` after each plan change. This means `handleReturnFromStripe()` triggering `updateDashboardForPlan('pro')` automatically surfaces the Pro widgets without any additional wiring. The wrap includes a `console.warn` if the function isn't found at wrap time.
- **`GET /api/dashboard/usage-stats` (Phase 6):** JWT-gated, `DASHBOARD_V2_ENABLED` flag. Queries `one_off_purchases` by `email` (not `user_id`). Returns `{ hours_saved_30d, value_purchased_30d, hours_saved_lifetime, value_usd_lifetime, tools_used, total_purchases }`. No caching — lifetime totals computed fresh from already-loaded rows. `utils/tool-value-map.js` is the source of truth for `hours_saved` and `value_usd` per tool; `value_usd` must stay in sync with ITEM_PRICES in `routes/stripe.js` (stripe.js is canonical).

- **Preferences modal pattern:** `#prefs-modal` visibility is controlled by CSS class `body.preferences-modal-open .prefs-modal { display: flex }` in `css/dashboard-preferences.css` — never inline styles. `PREF_PATHWAY` and `PREF_FIELD` are Brevo text attributes (create in Brevo dashboard if not present). `DESTINATION` is an existing Brevo attribute reused.
- **Retake quiz sessionStorage note:** `handleRetakeQuiz()` MUST use `window.location.href`, not an iframe click. sessionStorage is per-browsing-context — a flag set in the top frame IS readable by index.html loaded via `window.location.href` in the same tab, but is NOT readable by an iframe (iframes have their own sessionStorage).
- **`window._oraboLastJourneyState`:** set by `dashboard-journey.js` after each successful `/api/dashboard/state` fetch. Used by `openPreferencesModal` for prefill when editing existing preferences.
- **Iframe close:** dashboard listens for `'tool-closed'` postMessage → calls `closeTool()`. `payment-return-*.js` wires close to `handleToolClose()` — posts `'tool-closed'` (iframe) or redirects to `dashboard.html` (standalone).
- **Pro upgrade from iframes:** post `'initiate-pro-upgrade'` to `window.top`; dashboard calls `initiateStripeCheckout()`. `wireUpgradeModal` in `stripe.js` follows same pattern.
- **Pro tool cards:** `class="pro-tool"` + `data-plan="pro"`. Tooltip via `[data-plan="pro"]::after`; hidden when `.unlocked` present.
- **`#upgrade-btn`:** hidden by default; shown by `updateDashboardForPlan('free')`, hidden by `updateDashboardForPlan('pro')`.
- **Pro upgrade modal (#pro-modal)** is hidden by default via `#pro-modal { display: none }` in `css/dashboard-page.css` and shown by adding `.pro-modal-visible`. The modal does NOT auto-open on sign-in or page load — it is invoked only by explicit user action: click on the top-nav Upgrade button (#upgrade-btn), click on the "Upgrade to Pro" button inside #value-upgrade-cta-widget, click on a locked Pro tool card, click on the sidebar plan label upgrade link, or 'initiate-pro-upgrade' postMessage from an iframe. The previous `.pro-modal-hidden` class was removed 2026-05-18 — it used a class rule (specificity 10) that was always overridden by the `#pro-modal { display: flex }` ID rule (specificity 100), leaving the modal permanently visible for all users. The modal is dismissed via #pro-modal-close (X), #pro-modal-dismiss (Maybe later), backdrop click, or Escape key — all four wired in `js/dashboard-inline.js`.
- **Pro modal scroll-lock caveat:** `showProModal()` / `hideProModal()` set `document.body.style.overflow` directly (inline style mutation). This works while `style-src` retains `'unsafe-inline'` (required by Chart.js). If `style-src` is ever tightened, this mutation will be silently blocked and the page will scroll behind the modal. Refactor to a `body.modal-open { overflow: hidden }` class + classList toggle before tightening `style-src`.
- **Scroll-lock is single-modal-scope:** each modal's close handler unconditionally sets `body.style.overflow = ''`. If a future feature introduces nested or stacked modals, closing the inner modal releases scroll-lock while the outer is still open. Track open-modal count with a counter or a `Set` of open modal IDs, and only restore `overflow` when the count returns to zero. Do not add ad-hoc scroll-lock to any new modal without addressing this.

---

## Coding Rules

- Always commit and push to `origin main` after every code change — no need to ask
- **Pro price is $12/mo** — never advertise any other price for the Pro subscription on any surface (marketing pages, button labels, error recovery text, FAQ, email templates, etc.)
- **Document Checklist and Readiness Report outputs are DOCX-only** — never advertise "PDF" for these tools in marketing copy, buttons, emails, or FAQ. `generatePdf()` produces blank output for HTML input; this is a known, intentional constraint that is not changing.
- Pure HTML/CSS/JS for the frontend — no framework
- Keep global styles in `styles.css`; feature-specific styles in `css/<feature>.css`
- Feature JS for `index.html` goes in `js/<feature>.js` (ES modules with `import/export`)
- `signup.html`, `login.html`, and `dashboard.html` use external non-module script files (`js/signup.js`, `js/login.js`, `js/dashboard-inline.js`) — these were extracted from inline blocks on 2026-05-18 to comply with the Cloudflare Pages edge CSP (`script-src` has no `'unsafe-inline'`). Do not convert these to ES modules and do not re-inline them.
- **`reset-password.html` MUST keep ALL logic in `js/reset-password.js`.** The Cloudflare edge CSP (`script-src 'self' ...`, no `'unsafe-inline'`) blocks inline scripts site-wide, silently rendering the page as a blank shell with no error visible to users. This was the original failure mode before `js/reset-password.js` was extracted. Never inline any script on this page. Uses Supabase UMD — always `supabaseClient`, never `const supabase`.
- On `signup.html`, `login.html`, and `dashboard.html` — always use `supabaseClient` (not `supabase`); `const supabase` conflicts with the UMD global `var supabase` and crashes silently
- City/role selection UIs use grouped `<select>` dropdowns (not pill buttons); extend existing data arrays — do not revert to pills
- **Cost of Living comparison city selection:** uses `compareSlots[]` state (not `compareCities`). `buildCityOptions(selectedId)` generates grouped options. Slots 2–4 progressive; slot 0 permanent. Do NOT revert to pills.
- **paycalc-data.js pattern:** `ROLES[]`, `ROLE_GROUPS`, `SALARY_DATA{}` in `js/paycalc-data.js` only. Never re-declare in `paycalc.js` or `all-countries-pay.html`. Adding a role: add to all three exports with data for all 18 countries. Adding a country: add a `SALARY_DATA` key for all 35 roles, a `COUNTRIES` entry in `paycalc.js` (with `fxRate`, `taxRate`, `cityId`), a `COL_MONTHLY_USD` entry in `paycalc.js`, a `COUNTRY_DATA` entry in `all-countries-pay.html`, and add its ID to the correct `COUNTRY_GROUPS` array in `paycalc.js`.
- **all-countries-pay.html module script:** tool logic in `<script type="module">`; Pro gate in separate non-module `<script>` before it. Module does NOT duplicate Pro check — `return` at module top level is a SyntaxError.
- Follow existing BEM-like CSS class naming conventions
- No TypeScript — plain JavaScript only
- Do not add comments unless logic is non-obvious
- Every public-facing HTML page must include a complete Open Graph block (`og:title`, `og:description`, `og:url`, `og:image` with `og:image:secure_url`, `og:image:type`, `og:image:width=1200`, `og:image:height=630`, `og:image:alt`) referencing `https://orabo.app/og-image.jpg`, plus `twitter:card=summary_large_image` and `twitter:image`. Reference implementation: `uk-global-talent.html`.
- Mobile-first responsive design — check existing `@media` breakpoints before adding new ones
- Use existing CSS variables (`--navy`, `--accent`, `--gold`, etc.) — do not introduce new colour values
- **When adding a new database table:** provide `CREATE TABLE` SQL for Supabase SQL editor BEFORE pushing dependent code
- **RLS is enabled on all user-data tables.** Anon key has: read-only access to `opportunities WHERE approved = true` and `stories WHERE approved = true`; no access to anything else via direct Supabase REST. User JWT has read/write access only to their own row in `user_profiles` and read-only access to their own `purchases` rows. All other writes (one_off_purchases, post_earn_rewards, ai_cache, processed_webhook_events, opportunities, stories) go through the Express backend with the service role key, which bypasses RLS. Never add a new user-data table without also adding RLS policies in the same migration. Policy SQL canonical reference: `migrations/2026-05-13-rls-pass-2.sql` (backend repo).
- **Stripe subscription checkout:** never pass `priceId` from frontend — backend reads `process.env.STRIPE_PRO_PRICE_ID`; frontend sends `{ userId, email, mode: 'subscription' }`
- **Dashboard plan state:** always route changes through `updateDashboardForPlan(plan)` — never scatter plan-state updates elsewhere
- **`DASHBOARD_V2_ENABLED` env var** gates all `/api/dashboard/*` routes. When false or unset, every dashboard read route returns 404. Used as a kill switch during the dashboard rebuild phases. Flag is removed in Phase 7 once the rebuild is sealed.
- **CSP is enforced via `_headers` (Cloudflare Pages) — not meta tags.** The `_headers` file in the repo root is the single authoritative Content-Security-Policy. All per-page `<meta http-equiv="Content-Security-Policy">` tags have been removed (they conflicted with the server-level header — browsers enforce both simultaneously, applying the most restrictive of each directive). When adding a new external domain dependency, update `_headers` only. Never re-add meta CSP tags to HTML files.
- **CSS specificity hide rule:** when hiding an element by toggling a class, the class rule's specificity must beat any default ID-level rule on that element. Prefer `#elementId { display: none }` as the default state plus `#elementId.visible-class { display: ... }` as the override (ID specificity on both sides). Avoid `.hidden-class { display: none }` against an `#elementId { display: flex }` default — the ID will always win and the hide will silently fail. This is the failure mode that caused the 2026-05-18 pro modal bug.
- **CSP inline styles:** `style-src` has `'unsafe-inline'` (required for Chart.js and dynamically injected styles) — the server-level CSP in `_headers` allows it. Never use `style="display:none"` on elements that need to start hidden via JS; use CSS classes instead (tool modals hidden via `css/cvtool.css`).
- **CSP inline scripts:** `script-src` has no `'unsafe-inline'` — never use `onclick=""` attributes; always wire via `addEventListener` in module init IIFEs
- **Cloudflare Pages edge CSP `frame-src` MUST include `'self'`.** Dashboard embeds same-origin pages (doc-checklist.html, readiness-report.html, full-doc-checklist.html, compare-cities.html, all-countries-pay.html, and the right-panel tool viewer in dashboard.html) via same-origin iframes. Without `'self'`, every load is blocked and Chrome renders `chrome-error://chromewebdata/` in the iframe slot. This was the 2026-05-18 dashboard-tool blank-iframe bug. If a new third-party iframe source is ever added, append its origin to `frame-src` — do not replace `'self'`.
- **Standalone tool page boot scripts MUST be external files (CSP).** Every standalone tool page that auto-opens a modal via a polling boot script loads that script from `js/autoopen-<tool>.js` — never inline. The 18 pages currently in this pattern: `cv-tool.html`, `sop-tool.html`, `consult-booking.html`, `eb1a-tool.html`, `eb2-tool.html`, `eb1a-letter-tool.html`, `eb2-letter-tool.html`, `eb1a-package-tool.html`, `eb2-package-tool.html`, `o1a-tool.html`, `o1a-letter-tool.html`, `o1a-package-tool.html`, `ukgt-tech-tool.html`, `ukgt-tech-statement-tool.html`, `ukgt-tech-package-tool.html`, `ukgt-research-tool.html`, `ukgt-research-statement-tool.html`, `ukgt-research-package-tool.html`. The 2 dashboard-iframe pages (`doc-checklist.html` / `full-doc-checklist.html` and `readiness-report.html`) follow the same pattern with `js/autoopen-checklist.js` / `js/autoopen-quiz.js`. Production CSP (`_headers` line 2, `script-src 'self' ...` with no `'unsafe-inline'`, no hash, no nonce) blocks inline `<script>` silently — Cloudflare enforces it at the edge, and `python3 -m http.server` does NOT apply `_headers`, so inline blocks pass local smoke and fail in production. When adding a new standalone tool page: create `js/autoopen-<tool>.js` as a plain non-module IIFE (canonical reference: `js/autoopen-cv.js` for the `DOMContentLoaded` pattern, `js/autoopen-eb1a.js` for the bare polling pattern, `js/autoopen-consult.js` for the complex side-effect pattern), and reference it from the HTML as `<script src="js/autoopen-<tool>.js"></script>` at end of `<body>` after all module imports. NEVER inline. NEVER use `type="module"` unless the boot script needs `import` syntax. Pre-launch verification: hard-reload the new page in incognito Chrome at `https://orabo.app/<page>` with DevTools open — zero `Refused to execute inline script` errors in Console, `autoopen-<tool>.js` returns 200/304 in Network. Local smoke alone is insufficient for this class of bug.
- **CSP changes fail silently.** Cloudflare Pages edge CSP in `_headers` (and any future CSP rule changes elsewhere) does not surface failures as JS errors, network errors, or visible exceptions. Symptoms include: inline event handlers that do nothing on click, iframes rendering `chrome-error://chromewebdata/` (broken-image icon), inline scripts that never run, inline styles that get ignored. Local `python3 -m http.server` does NOT apply `_headers`, so CSP regressions are invisible in development. Every commit that touches `_headers`, any CSP `<meta>` tag, or any directive value MUST be followed by an incognito production test on https://orabo.app with DevTools Console open after the Cloudflare deploy completes (~60–90 s). Click through every affected surface (dashboard iframes, auth pages, paid tool modals, payment flows) and confirm zero `Refused to...` or `... violates the following Content Security Policy directive` lines. Three CSP regressions in production on 2026-05-18 (dead login/signup buttons, dead Pro modal close, blank dashboard iframes) shipped exactly because this step was skipped. Do not declare a CSP-touching commit done until incognito production verification passes.
- **New tool modal pattern:** follow cvtool/soptool pattern — CSS `.active` class for visibility (never `style="display:..."`), `showModal()` adds `.active` to modal wrapper, `showScreen(id)` toggles `.active` on child screens, sessionStorage bridge, two-part URL condition (`payment_success=1` + sessionStorage key) in `init()`
- **Dashboard iframe pages:** standalone HTML (no navbar/footer) + `autoopen-<tool>.js` + `url:` entry in dashboard TOOLS array. Never use `anchor:` for auto-opening tools.
- **Iframe close button pattern:** include `payment-return-*.js` wiring close to `handleToolClose()` — posts `'tool-closed'` (iframe) or redirects to `dashboard.html` (standalone)
- **userId in AI generation requests:** AI generation routes derive user identity from the `Authorization: Bearer <jwt>` header, never from `req.body.userId`. `utils/verify-payment.js` calls `supabase.auth.getUser(jwt)` with the service-role client to resolve `authUserId` server-side. The anonymous Stripe-session-paid flow (CV, SOP, EB, O-1A tools) works without a JWT — the Stripe path in `verify-payment.js` is independent. Pro-bypass scripts (`js/pro-bypass-checklist.js`, `js/pro-bypass-quiz.js`) read the JWT via `readSessionFromStorage()` (synchronous localStorage read — see rule below), send `access_token` in the `Authorization: Bearer` header, and abort the bypass if no session is available (letting the normal Stripe flow proceed). Do NOT pass `userId` to `/api/cv` or `/api/sop` — these are anonymous flows; recipient email uses `formAnswers.user_email`. `payment-return-checklist.js` / `payment-return-quiz.js` do NOT send any userId; email resolution is handled inside `verify-payment.js` from the trusted source (Stripe session or JWT).
- **Pro bypass scripts read JWT from localStorage, NOT via getSession().** `js/pro-bypass-checklist.js` and `js/pro-bypass-quiz.js` run inside iframes embedded in `dashboard.html`. Calling `supabaseClient.auth.getSession()` from the iframe hangs indefinitely because two GoTrueClient instances conflict (one in the parent, one in the iframe), surfacing as "Multiple GoTrueClient instances detected" in the console. Instead, use `readSessionFromStorage()` from `js/auth.js` (also inlined verbatim at the top of each bypass IIFE), which reads the persisted Supabase session synchronously from localStorage (key `sb-<project-ref>-auth-token`). If the token is missing or expired, abort the bypass and fall through to the normal Stripe flow. Do NOT revert to `getSession()` in these scripts.
- **Stripe redirects in iframes:** `purchaseItem` and `subscribeToPro` in `stripe.js` use `(window !== window.top ? window.top : window).location.href` — preserve this
- **Pro upgrade from embedded pages:** post `'initiate-pro-upgrade'` to `window.top` — never call Stripe directly from an iframe. Applies to `compare-cities.html`, `all-countries-pay.html`, `wireUpgradeModal` in `stripe.js`, `eligibility.js`.
- **Upgrade button label:** always "Upgrade to Pro" — never "Unlock with Pro" or other variants
- **Backend CORS:** `server.js` uses explicit `ALLOWED_ORIGINS` array (`https://orabo.app`, `https://www.orabo.app`) — do not revert to `FRONTEND_URL` env var
- **Consultation booking flow:** `#bookingModal` and `#calendlyOverlay` in `consult-booking.html`. Modal is 2 steps only — never re-add date/time picker. `_showCalendlyOverlay(url)` in `stripe.js` shows correct event link.
- **booking.js guard pattern:** wraps all code in IIFE with `if (!modal) return` — safe to import on any page without `#bookingModal`
- **booking.js back buttons:** step 1 gets a `.tool-back-btn` "← Back to Home" inserted programmatically at the top of `#step1`; step 2's existing `.prev-step` button is removed and replaced with a `.tool-back-btn` "← Back" → `goToStep(1)`. Both wired via `addEventListener` inside the IIFE — never `onclick=""`.
- **`.tool-back-btn` in cv/sop tool forms:** plain accent-coloured text-link (`color: var(--accent)`, no border, no background, `font-size: 0.9rem`) defined in `css/cvtool.css`. Rendered above the section title by `renderSection()` on every section. Section 0: "← Back to Home" → `/`. Section 1+: "← Back" → saves current answers + `renderSection(index - 1)`. No nav-area back button exists — do not add one.
- **`.tool-next-btn` in cv/sop tool forms:** self-contained accent blue pill defined in `css/cvtool.css` (`background: var(--accent)`, `color: #fff`, `border-radius: 8px`, `padding: 0.75rem 2rem`, `font-weight: 600`). Used by `renderSection()` Next/Continue button in both `cvtool.js` and `soptool.js`. Do NOT revert to `btn-primary` for this button.
- **`.tool-landing-card .btn-primary` scoped rule:** defined in `css/cvtool.css` — makes bare `btn-primary` buttons (no `.btn` base class) self-contained with full pill styling (gradient background, 50px radius, shadow) matching `.btn.btn-primary`. Non-featured CV/SOP pricing cards on `index.html` use bare `btn-primary` — this rule is required. Do not remove it.
- **one_off_purchases audit log:** `routes/cv.js`, `routes/sop.js`, `routes/eb1a.js`, and `routes/eb2.js` fire-and-forget insert into `one_off_purchases` after every successful generation using `formAnswers.user_name` and `formAnswers.user_email`. `stripe_session_id` is null for now.
- **CV and SOP no-fabrication rule:** the CV system prompt (`routes/cv.js`) forbids inventing metrics, team sizes, revenue figures, percentages, awards, certifications, dates, or job responsibilities the user did not describe. Where a metric would strengthen a bullet but the user did not provide the figure, the AI inserts `[INSERT METRIC — e.g. team size, $ figure, % improvement]` and flags every placeholder in the Top 5 Priority Actions. SOP retains its existing Rule 1 (no invented experiences). Both rules must survive any future prompt edit — do not remove them.
- **SOP track branching:** `routes/sop.js` reads `formAnswers.sopTrack`, normalises display strings to internal keys via `normalizeTrack()`, and selects an adjudicator persona + output scaffold from `SOP_TRACK_SCAFFOLDS` in `utils/document-norms.js`. Chevening produces four labelled essays (Leadership and Influence, Networking, Studying in the UK, Career Plan), not one document. MCF structures the essay around the give-back arc. Other scholarship tracks share a default scaffold with adjudicator-persona variation only. If `sopTrack` is missing or unrecognised, `normalizeTrack()` falls back to `masters_admissions` silently. The `destinationCountry` dropdown in both CV and SOP forms contains nine individual countries (USA, Canada, United Kingdom, Germany, Netherlands, Ireland, Australia, New Zealand, Portugal, Other) — not the old grouped "Europe" option. Backend country lookup: `CV_COUNTRY_RULES[formAnswers.destinationCountry] || CV_COUNTRY_RULES.DEFAULT` and `SOP_COUNTRY_RULES[formAnswers.destinationCountry] || SOP_COUNTRY_RULES.DEFAULT`.
- **EB tool routes (routes/eb1a.js, routes/eb2.js):** NO ai_cache reads — every EB generation is fresh (Tier 1, Tier 2, and Tier 3). max_tokens 8000 for Tier 1 and Tier 2; max_tokens 10000 for Tier 3 packages. Legal disclaimer included in Tier 1 AI output (appended) and in Tier 2/3 system prompt HTML structure. No `userId` check — anonymous flow only. Route branches on `outputType`: `eb1a_assessment` → Tier 1 (plain text/markdown output); `eb1a_letter` → Tier 2 (HTML output, code-fence stripping); `eb1a_package` → Tier 3 (HTML output, code-fence stripping, res.setTimeout(180000)). Same branching pattern for eb2. Brevo delivery email fire-and-forget after generation. Stripe item keys: `eb1a_assessment` ($39), `eb2_assessment` ($35), `eb1a_letter` ($150), `eb2_letter` ($140), `eb1a_package` ($250), `eb2_package` ($240). Success URLs: `eb1a-tool.html`, `eb2-tool.html`, `eb1a-letter-tool.html`, `eb2-letter-tool.html`, `eb1a-package-tool.html`, `eb2-package-tool.html`. Console prefixes: `[eb1a]`, `[eb2]`, `[brevo-eb1a-letter]`, `[brevo-eb2-letter]`, `[brevo-eb1a-letter-attr]`, `[brevo-eb2-letter-attr]`, `[brevo-eb1a-package]`, `[brevo-eb2-package]`, `[brevo-eb1a-package-attr]`, `[brevo-eb2-package-attr]`.
- **EB tool frontend (js/eb1atool.js, js/eb2tool.js):** cvtool pattern. sessionStorage keys: `orabo_eb1a_answers`/`orabo_eb1a_output_type` and `orabo_eb2_answers`/`orabo_eb2_output_type`. `init()` preserves sessionStorage when `payment_success=1` or `payment_cancelled=1` is in the URL; wipes otherwise (clean start). Loading messages rotate every 10s via `startMessageRotation()`. Single payment card per tool (no grid). Tooltip via `.ib-hint-icon` + `.ib-hint-tooltip` CSS-only hover — never onclick. Question type `'large'` renders textarea with `.large` class. Element ID prefix: `ib-q-` and `ib-err-`. Exports `window.eb1aTool` / `window.eb2Tool = { init, start }`. EB-1A answer keys include `evidence_inventory` and `letter_writers` (Section E, optional); EB-2 NIW answer key includes `evidence_and_letters` (Section D, optional). Backend defaults both to empty string if absent — a blank answer must not block submission or crash the assessment. SCREENS constant includes review screen (`eb1a-review-screen` / `eb2-review-screen`). Imports `renderReview` from `./tool-review.js`.
- **EB Tier 1 tools (`eb1a-tool.html`, `eb2-tool.html`):** identical pattern to CV/SOP — sessionStorage bridge with `orabo_eb1a_*` / `orabo_eb2_*` keys, two-part URL condition (`payment_success=1` + sessionStorage answers) in `handlePaymentReturn()`, anonymous flow with no `userId`, `formAnswers` field name in API request, DOCX-only download, no caching, attorney disclaimer appended. `js/stripe.js` `checkPaymentReturn` IIFE bypasses the generic banner for all 15 tool item keys (both `payment_success=1` and `payment_cancelled=1`) so the URL is preserved for the module's `init()` to read. Cancel return (`payment_cancelled=1`) restores answers from sessionStorage and shows review screen.
- **EB Tier 2 tools (`eb1a-letter-tool.html`, `eb2-letter-tool.html`):** same standalone page pattern as Tier 1. Polls `window.eb1aLetterTool` / `window.eb2LetterTool` (100 ms × 50 attempts) then calls `init()`. sessionStorage keys: `orabo_eb1a_letter_answers` / `orabo_eb1a_letter_output_type` and `orabo_eb2_letter_answers` / `orabo_eb2_letter_output_type`. Download file keys: `data.files.eb1a_letter_docx` and `data.files.eb2_letter_docx`. AI system prompts produce HTML output; backend strips code fences before DOCX generation. Question id prefix: `eb1a_letter_q<n>_<slug>` and `eb2_letter_q<n>_<slug>`. Mandatory email validation at section 0. No caching, no userId, DOCX-only download. SCREENS constant includes `eb1a-letter-review-screen` / `eb2-letter-review-screen`. Cancel return (`payment_cancelled=1`) restores answers from sessionStorage and shows review screen — same flow as Tier 1. **EB-2 NIW Tier 2 only — R-01 Filing Risk Advisory:** `EB2_LETTER_SYSTEM_PROMPT` contains a conditional advisory block that fires when ALL THREE of (a) `eb2_letter_q15_us_collaborations`, (b) `eb2_letter_q12_publications_projects`, and (c) `eb2_letter_q14_expert_letters` indicate no evidence; when triggered it inserts `<h3>Filing Risk Advisory</h3>` between Prong 2 and Prong 3. When editing `EB2_LETTER_SYSTEM_PROMPT`, preserve this block and do NOT weaken or remove it — it is the primary risk-disclosure mechanism for Prong 2-deficient petitions. The Layer 2 guard (`console.warn('[eb2-letter] prong2-advisory-missing', ...)`) in `handleEb2Letter()` is log-only and must also be preserved; it runs on pre-footer `aiOutput` and detects cases where `[INSERT` placeholders are present but the advisory is absent.
- **EB Tier 3 tools (`eb1a-package-tool.html`, `eb2-package-tool.html`):** same standalone page pattern as Tier 1 and Tier 2. Polls `window.eb1aPackageTool` / `window.eb2PackageTool` (100 ms × 50 attempts) then calls `init()`. sessionStorage keys: `orabo_eb1a_package_answers` / `orabo_eb1a_package_output_type` and `orabo_eb2_package_answers` / `orabo_eb2_package_output_type`. Download file keys: `data.files.eb1a_package_docx` and `data.files.eb2_package_docx`. Download filenames: `eb1a-full-petition-package.docx` / `eb2-niw-full-petition-package.docx`. Question id prefix: `eb1a_package_q<n>_<slug>` and `eb2_package_q<n>_<slug>` (isolated from Tier 2 prefix to avoid sessionStorage key collisions). EB-1A package: 7-section array (Section 0 + Sections A–F); 25 questions; last 3 questions (Section F) are optional inventory/timeline fields. EB-2 NIW package: 6-section array (Section 0 + Sections A–E); 23 questions; last 3 questions (Section E) are optional. SCREENS constant includes `eb1a-package-review-screen` / `eb2-package-review-screen`. `res.setTimeout(180000)` on backend handlers. `max_tokens: 10000`. No caching, no userId, DOCX-only download. Cancel return restores answers and shows review screen — same flow as Tier 1 and Tier 2.
- **O-1A route (`routes/o1a.js`):** mounted at `/api/o1a`. Branches on `outputType`: `o1a_assessment` → Tier 1 (plain text/markdown, 8-criterion scorecard, non-immigrant framing, petitioner pathway assessment from Q3 answer); `o1a_letter` → Tier 2 (HTML output, two `<h1>` sections: Petition Support Letter + mandatory Peer Consultation Letter); `o1a_package` → Tier 3 (HTML, seven `<h1>` sections including unique Petitioner Support Brief). NO ai_cache reads — all O-1A generations are fresh. Petitioner-voice Rule 1 in Tier 2/3 system prompts bans "I have", "my work", "my contributions" and all first-person pronouns when referring to the beneficiary; third-person petitioner voice required throughout. Peer organisation library (IEEE, AMA, YPO, sport governing bodies, etc.) included in Tier 2/3 system prompt to ensure credible peer consultation letter. `[INSERT: <description>]` placeholders for missing specifics (no fabrication). `max_tokens: 8000` for Tier 1 and Tier 2; `max_tokens: 10000` for Tier 3. `res.setTimeout(180000)` on all handlers. Brevo delivery + attribute update fire-and-forget after every generation. `one_off_purchases` audit row written. File keys: `o1a_assessment_docx` (Tier 1), `o1a_letter_docx` (Tier 2), `o1a_package_docx` (Tier 3). Console prefixes: `[o1a]`, `[brevo-o1a]`, `[brevo-o1a-attr]`, `[brevo-o1a-letter]`, `[brevo-o1a-letter-attr]`, `[brevo-o1a-package]`, `[brevo-o1a-package-attr]`.
- **O-1A frontend tools (`o1a-tool.html`, `o1a-letter-tool.html`, `o1a-package-tool.html`):** identical pattern to EB Tier 1/2/3 tools. Section 0 Back button on all 3 O-1A tools routes to `/o1a-visa.html` (not `/`). sessionStorage keys: `orabo_o1a_answers`/`orabo_o1a_output_type` (Tier 1), `orabo_o1a_letter_answers`/`orabo_o1a_letter_output_type` (Tier 2), `orabo_o1a_package_answers`/`orabo_o1a_package_output_type` (Tier 3). Question ID prefixes isolated across tiers: `o1a_q<n>_<slug>`, `o1a_letter_q<n>_<slug>`, `o1a_package_q<n>_<slug>`. SCREENS constant on each module includes the review screen. Download filenames: `o1a-assessment.docx`, `o1a-petition-letter.docx`, `o1a-full-petition-package.docx`. Mandatory email validation at section 0. No caching, no userId, DOCX-only download. Cancel return restores answers from sessionStorage and shows review screen.
- **UKGT Tech Nation route (`routes/ukgt-tech.js`):** mounted at `/api/ukgt-tech`. Branches on `outputType`: `ukgt_tech_assessment` → Tier 1 (plain text/markdown, Tech Nation criteria scorecard); `ukgt_tech_statement` → Tier 2 (HTML, personal statement + 3 recommendation letter templates + Plan for the UK one-pager); `ukgt_tech_package` → Tier 3 (HTML, 7-component full endorsement package). System prompts in `routes/ukgt-tech-prompts.js` (`UKGT_TECH_SYSTEM_PROMPT` + `UKGT_TECH_STATEMENT_SYSTEM_PROMPT` + `UKGT_TECH_PACKAGE_SYSTEM_PROMPT`). `sanitizeBrand()` defined inline. UK English mandatory in all prompts — organisation/programme/behaviour/recognise/analyse/specialise etc. `max_tokens: 8000` for Tier 1 and Tier 2; `max_tokens: 10000` for Tier 3. `res.setTimeout(180000)` + `req.setTimeout(180000)`. NO ai_cache reads. verifyPayment + gate before Claude call. `formAnswers` field name in POST body. Word-count guardrail on Tier 2 and Tier 3: `_countPersonalStatementWords()` extracts personal statement section, strips HTML tags, counts words; if >1,200, one retry with stricter instruction; accepts result either way; logs `[ukgt-tech-statement] word-count retry` / `[ukgt-tech-package] word-count retry`. Code-fence stripping before `generateDocx()` in Tier 2 and Tier 3. Brevo delivery + attribute update fire-and-forget. `[brevo-ukgt-tech]`/`[brevo-ukgt-tech-attr]` (Tier 1), `[brevo-ukgt-tech-statement]`/`[brevo-ukgt-tech-statement-attr]` (Tier 2), `[brevo-ukgt-tech-package]`/`[brevo-ukgt-tech-package-attr]` (Tier 3) log prefixes. `one_off_purchases` audit row written. File keys: `ukgt_tech_assessment_docx` (Tier 1), `ukgt_tech_statement_docx` (Tier 2), `ukgt_tech_package_docx` (Tier 3). Download filenames: `ukgt-tech-nation-eligibility-assessment.docx` (Tier 1), `ukgt-tech-personal-statement-package.docx` (Tier 2), `ukgt-tech-full-endorsement-package.docx` (Tier 3). Tier 1 email upsell links to `/ukgt-tech-statement-tool.html`; Tier 2 email upsell links to `/ukgt-tech-package-tool.html`.
- **UKGT Tech Nation frontend tools (`ukgt-tech-tool.html`, `ukgt-tech-statement-tool.html`, `ukgt-tech-package-tool.html`):** identical pattern to O-1A tools. All 3 poll their respective `window.*Tool` (100 ms × 50 attempts). Section 0 back button → `/uk-global-talent.html` on all 3. Tier 1 (`js/ukgt-tech-tool.js`): MODAL_ID `ukgt-tech-tool-modal`; sessionStorage keys `orabo_ukgt_tech_answers`/`orabo_ukgt_tech_output_type`; ITEM_TYPE `ukgt_tech_assessment`; question ID prefix `ukgt_tech_q<n>_<slug>`; email validation on `ukgt_tech_q0_user_email`; SCREENS includes `ukgt-tech-review-screen`. Tier 2 (`js/ukgt-tech-statement-tool.js`): MODAL_ID `ukgt-tech-statement-tool-modal`; sessionStorage keys `orabo_ukgt_tech_statement_answers`/`orabo_ukgt_tech_statement_output_type`; ITEM_TYPE `ukgt_tech_statement`; question ID prefix `ukgt_tech_statement_q<n>_<slug>` (isolated from Tier 1); email validation on `ukgt_tech_statement_q0_user_email`; Q5 track uses `{ value, label }` objects — `_renderSelect` handles both `{ value, label }` objects and plain strings; SCREENS includes `ukgt-tech-statement-review-screen`. Tier 3 (`js/ukgt-tech-package-tool.js`): MODAL_ID `ukgt-tech-package-tool-modal`; sessionStorage keys `orabo_ukgt_tech_package_answers`/`orabo_ukgt_tech_package_output_type`; ITEM_TYPE `ukgt_tech_package`; question ID prefix `ukgt_tech_package_q<n>_<slug>` (isolated from Tier 1 and Tier 2); email validation on `ukgt_tech_package_q0_user_email`; Q5 track uses `{ value, label }` objects; SCREENS includes `ukgt-tech-package-review-screen`. All 3: mandatory email validation at section 0; no caching; no userId; DOCX-only download; cancel return restores answers and shows review screen.
- **css/ibtool.css:** EB + O-1A + UKGT tool modal/screen visibility (`.active` pattern) for Tier 1, Tier 2, and Tier 3 ID sets, including review screen IDs (`eb1a-review-screen`, `eb2-review-screen`, `eb1a-letter-review-screen`, `eb2-letter-review-screen`, `eb1a-package-review-screen`, `eb2-package-review-screen`, `o1a-review-screen`, `o1a-letter-review-screen`, `o1a-package-review-screen`, `ukgt-tech-review-screen`, `ukgt-tech-statement-review-screen`, `ukgt-tech-package-review-screen`). `.tool-spinner` + `.tool-loading-inner` + `.tool-loading-msg` for loading screen. `.ib-hint-icon` + `.ib-hint-tooltip` tooltip. `.ib-payment-card` + `.ib-payment-features` + `.ib-payment-price` + `.ib-disclaimer` for payment card. When adding a new EB, O-1A, or UKGT tool page, extend the modal/screen ID lists in ibtool.css. Imported in `styles.css` before `mobile.css`.
- **Review screen pattern (all 14 tool modules):** A `*-review-screen` div sits between the last form section and the payment screen in each tool's HTML (`cv-review-screen`, `sop-review-screen`, `eb1a-review-screen`, `eb2-review-screen`, `eb1a-letter-review-screen`, `eb2-letter-review-screen`, `eb1a-package-review-screen`, `eb2-package-review-screen`, `o1a-review-screen`, `o1a-letter-review-screen`, `o1a-package-review-screen`, `ukgt-tech-review-screen`, `ukgt-tech-statement-review-screen`, `ukgt-tech-package-review-screen`). Rendered by `renderReview()` from `js/tool-review.js` via a thin wrapper in each module. Last form section Next button label: "Review your answers →" (not "Continue to payment →"). Payment screen back button targets the review screen (not the last form section). Review screen back button returns to last form section; continue button advances to payment screen.
- **js/tool-review.js (shared):** ES module exporting `renderReview({ screenId, prefix, sections, answers, onBack, onContinue })`. Builds `.tool-review-wrap` > sections > `.tool-review-item` (`.tool-review-label` + `.tool-review-value`). Empty/null/undefined answers display as `"(not provided)"` with `.tool-review-value.empty`. Action row: `.tool-review-actions` flex container with back (`.tool-back-btn`) and continue (`.tool-next-btn`) buttons. All buttons wired via `addEventListener` — never `onclick=""`.
- **css/cvtool.css review layout classes:** `.tool-review-wrap`, `.tool-review-section`, `.tool-review-item`, `.tool-review-label`, `.tool-review-value` (+ `.empty` modifier for not-provided text), `.tool-review-actions`. These are shared by CV and SOP. EB tools inherit the same layout via `ibtool.css` (which references the same class names — classes live in `cvtool.css`, which is imported globally via `styles.css`).
- **Stripe cancel URL for tool items:** `routes/stripe.js` contains a `TOOL_CANCEL_PAGES` map for the 27 tool item keys (21 CV/SOP/EB/O-1A/UKGT + 6 free tools + consultation). Cancel URL format: `${FRONTEND_URL}/${toolPage}?payment_cancelled=1&item=${itemType}`. Do NOT use `/?payment=cancelled` for these items — that URL has no sessionStorage and loses the user's form answers. Dashboard items still cancel to `dashboard.html?payment=cancelled`; all others to `/?payment=cancelled`.
- **payment_cancelled=1 bypass in stripe.js:** the `checkPaymentReturn` IIFE in `js/stripe.js` preserves the URL (does nothing) when `payment_cancelled=1` is present and the item is one of the 27 tool keys. This allows the tool module's `init()` to read and handle the parameter. Do not clear the URL or show a generic banner for these items.
- **Bypass list count in `js/stripe.js`:** report as an absolute count (e.g. "27 keys"), not a cumulative delta ("extended from N to N+1"). Every future tool ship grep-counts `js/stripe.js` against the canonical Stripe item key list to verify, rather than incrementing a number from memory. This convention was adopted 2026-05-18 after a plan miscount surfaced during UKGT Peer Review T3 verification.
- **booking.js cancel return pattern:** `js/booking.js` saves `jc_booking_state` (plan, price, duration, name, email, country, destination, purpose, itemType) to sessionStorage before calling `purchaseItem()`. `js/payment-return-booking.js` (non-module, loaded by `consult-booking.html`) detects `payment_cancelled=1`, sets `window._bookingCancelReturn = true`, clears URL, then in DOMContentLoaded restores form fields and advances modal to Step 2. The inline script in `consult-booking.html` checks `window._bookingCancelReturn` and skips the default auto-open on cancel return.
- **Document Checklist back buttons:** `js/checklist.js` injects `.tool-back-btn` on Stage 2 and Stage 3 only (Stage 1 has NO back button — only the X close, to avoid blank-page navigation on the bare standalone page). Stage 2 → `checklistWizardGoTo(1)`; Stage 3 → `checklistWizardGoTo(2)`. `window.oraboChecklistRestore(state)` is a public helper that restores pathway/country state and calls `checklistWizardGoTo(3)` — used by `payment-return-checklist.js` on cancel return. Do NOT modify checklist.js for payment-return logic beyond these UI hooks.
- **Readiness Quiz back buttons:** `js/quiz.js` `renderQuestion(index)` removes any existing `#quizTopBack` and re-creates a `button#quizTopBack.tool-back-btn` at the top of `#quizQuestionScreen` only when `index > 0` (Question 1 has NO back button — only the X close). Q2–Q15 click → `currentQ--; renderQuestion(currentQ)`. `showResults()` injects a `button#quizResultsBack.tool-back-btn` as first child of `#quizResultsScreen`; clicking it hides results, shows `#quizQuestionScreen`, and re-renders Question 15 with the saved answer. `window.oraboQuizRestoreScore(state)` restores `quizAnswers` and calls `showResults()` — used by `payment-return-quiz.js` on cancel return. Do NOT modify quiz.js for payment-return logic beyond these UI hooks.
- **Readiness quiz options (`js/quiz.js`):** Q7 (id: `destination`) has 6 options — US, Canada, UK, Europe, Australia, New Zealand (all `score:10`; `value` field is PascalCase short string; emoji flag inline in `label`). Q11 (id: `field`) has 6 options — Healthcare, Tech, Finance, Education, Trades, Other (`Other` is always last, `score:4`; Trades is 5th, `score:10`). When adding new options to either question, mirror the existing data shape (`{label, score, value}`) and casing convention; `MAX_SCORE` is auto-computed from option scores so no separate constant needs updating; `destination_value` from Q7 is forwarded to the report API as the destination string — ensure new `value` fields are valid destination names recognisable by the backend.
- **init() sessionStorage preservation rule (all 14 tools):** on page load, `init()` checks for both `payment_success=1` AND `payment_cancelled=1` before deciding whether to wipe sessionStorage. Wipe only when NEITHER is present (clean start). This ensures cancel returns can restore the review screen from the saved answers.
- **Save & Resume Draft pattern (all 14 paid tools):** `js/draft-store.js` + `js/draft-banner.js` are the two shared modules. Every paid tool module imports both, defines `TOOL_KEY` (snake_case, e.g. `'eb1a_letter'`) and `TOOL_LABEL` (display name). `saveCurrentAnswers(index)` ends with `saveDraft(TOOL_KEY, { answers, sectionIndex: index, outputType: ITEM_TYPE || null })`. `showDownloadScreen()` starts with `clearDraft(TOOL_KEY)`. `init()` shows `showResumeBanner(...)` inside the `if (!isReturn)` / `if (!didReturn)` block, after `renderSection(0)`, gated on no payment URL params. For CV/SOP (no `isSuccess`/`isCancelled` vars): compute `_isPaymentUrl` from `URLSearchParams` BEFORE calling `handlePaymentReturn()` (since it clears the URL). For EB/O1A/UKGT: gate on `!isSuccess && !isCancelled`. Do NOT apply this pattern to Document Checklist, Readiness Quiz, or Consultation Booking.
- **Migration Worth It route (`routes/migration-worth-it.js`):** mounted at `/api/migration-worth-it` with NO server-level rate limiter — the route carries its own `worthitLimiter` (5/IP/24h, env-overridable). No `verifyPayment()`, no `ai_cache`. Claude model is `claude-haiku-4-5-20251001` (`max_tokens: 250`, `res.setTimeout(30000)`) — the only Haiku use in Orabo. Commentary failure is non-fatal (fallback string). Tool recommendation is a deterministic lookup: USA+HIGH_ACH→EB-1A, USA+TECH→O-1A, USA+other→EB-2, UK+TECH→UKGT Tech Assessment, TRADES→Document Checklist, all others→CV Review. Brevo fire-and-forget sets `WORTHIT_TARGET` + `WORTHIT_VERDICT` with `[brevo-worthit]` prefix. `utils/worthit-data.js` holds all data constants (SALARY_DATA is already in USD — no FX conversion needed for target salary). Rate limit 429 response: `{ error: 'rate_limited', retryAfter: 86400 }`.
- **Migration Worth It frontend (`migration-worth-it.html` + `js/worthit.js`):** standalone page — no auth, no Supabase UMD, no navbar/footer. `js/worthit.js` is an ES module importing ROLES/ROLE_GROUPS from `./paycalc-data.js` and API_URL from `./config.js`. On page load: decodes `?result=<base64>` param via TextEncoder/TextDecoder and renders result without form submission (shareable URL). On form submit: POSTs to `/api/migration-worth-it`, renders result, pushes shareable URL via `history.pushState`. Share buttons (X, WhatsApp, clipboard) are all CSP-safe `addEventListener`. No `.hidden` class management is needed — `#worthit-result` starts hidden via `css/worthit.css` `.hidden { display: none }`.
- **Migration Worth It `WORTHIT_TARGET` + `WORTHIT_VERDICT` Brevo attributes:** add these as Text custom attributes in the Brevo dashboard if not yet present. They record which country and verdict the contact received.
- **Compare Destinations (pathway-compare.html):** free public tool at `/pathway-compare.html` — no auth, no payment, no backend. Consumes the A1 data layer (`js/pathway-compare-data.js` + matrix chunk files + `js/pathway-compare-constants.js`). **Pathway-level / per-destination split:** `PATHWAY_LEVEL_DIMS = ['pathway_full_name', 'eligibility_gate', 'common_rejection']` in `js/pathway-compare-render.js` partitions fields that are identical across all destinations; `PER_DEST_DIMS` is `COMPARE_DIMENSIONS.filter(d => !PATHWAY_LEVEL_DIMS.includes(d.id))` (currently 13 dims). Pathway-level fields render once in `renderOverviewCard(state)` injected into `#pc-overview` (above the table), sourced from the first feasible destination cell — no overview card when all selected destinations are infeasible. `renderTable()` iterates `PER_DEST_DIMS` only; infeasible rowspan uses `PER_DEST_DIMS.length` (NOT `COMPARE_DIMENSIONS.length`). Styles in `css/pathway-compare-overview.css` (NEW split — `pathway-compare.css` was at 593 lines); `@import './css/pathway-compare-overview.css'` added to `styles.css` after `pathway-compare.css`. Three-state cell rendering: real value (State 1) → formatter; null+_note+parseable URL → verify link (State 3a); null+_note+no URL → italic note text (State 3b); null+no note → em-dash (State 2). `parseVerifyUrl()` handles bare-domain `'verify domain.tld/path — description'` format. `alternative` field in infeasible cells is always a pathway key — `fuzzyMatchPathway()` does exact key match. State machine via CSS classes on `#pc-results`: `pc-state--no-pathway`, `pc-state--no-dest`, `pc-state--table` — never inline `style=`. Progressive destination slots: slot 1 permanent, slots 2–4 appear when previous slot is filled. Deep-link: `?pathway=<key>&dest=<label>,<label>` via `history.replaceState`. All events wired via `addEventListener` — CSP-safe. Switch buttons (`data-pathway-key`) handled by event delegation on `#pc-results`. `pc-` CSS prefix. `css/pathway-compare.css` imported between `ibtool-audit.css` and `pathway-compare-overview.css`; `pathway-compare-overview.css` imported between `pathway-compare.css` and `mobile.css`.
- **Compare Destinations Phase B (SEO validation batch, 2026-05-21):** `scripts/generate-compare-seo.mjs` — Node ESM generator. Run: `node scripts/generate-compare-seo.mjs`. Slug convention: `compare/{pathway-slug}-{destA-slug}-vs-{destB-slug}.html` (e.g. `compare/work-visa-united-kingdom-vs-canada.html`). Feasible-pair-only rule: generator skips any pair where `isFeasible(pathway, dest)` returns false for either destination (logs `[skip]` warning). Batch scope: 12 pages — `work_visa` (UK↔Canada, UK↔Australia + reverses), `study` (UK↔Canada, USA↔UK + reverses), `permanent_residency` (Canada↔Australia, Canada↔UK + reverses). Manifest: `compare/index.html` lists all generated pages. Sitemap: idempotent guard (`xml.includes('/compare/')` check) prevents duplicate entries on re-run. Anti-thin-content: intro uses pathway-specific phrase pool (2 variants selected by `destA < destB` order); FAQ Q&As derived from actual cell data with pathway-type variations (study: work-hours + post-study work; others: spouse work rights + PR timeline); cross-links vary per page; A→B and B→A pages use opposite leading phrase. JSON-LD: `Article` + `FAQPage` schemas as `<script type="application/ld+json">` (no inline `<style>` — CSP-safe). Navbar: `class="scrolled"` hardcoded in `buildNavbar()`; `buildPage()` and `buildManifest()` both carry a page-level `<style>` block (identical to `pathway-compare.html`'s block) that forces `#navbar` solid unconditionally — required because `script.js`'s scroll handler removes `.scrolled` at `scrollY ≤ 60`, including during page load / scroll-restoration, reverting the nav to transparent+white on the light `--bg` behind the fixed nav. CSS: new SEO classes (`.pc-content-section`, `.pc-seo-intro`, `.pc-faq-section`, `.pc-faq-item`, `.pc-faq-q`, `.pc-faq-a`, `.pc-also-compare`, `.pc-freshness-note`, `.pc-compare-more`) added to `css/pathway-compare-overview.css`. Phase C deferred: `/pathway-match-tool.html` CTA is NOT linked from batch pages — regenerate to add it when Phase C ships. **Quality pass (2026-05-21):** (1) `#backToTop` link has `aria-label="Back to top"` (Lighthouse a11y); (2) `withArticle(dest)` helper in generator prefixes "the" for "United Kingdom"/"United States" mid-sentence in `buildIntro()` + `buildFAQItems()` (grammar fix — other countries unchanged); (3) `.pc-compare-more-link` + `.pc-also-compare a` colour changed from `var(--accent)` to `var(--navy)` in `css/pathway-compare-overview.css` (contrast ~9.8:1); (4) `script.js` EmailJS init + waitlist block wrapped in `if (typeof emailjs !== 'undefined') { … }` with null guard on `waitlistForm` — prevents `ReferenceError` on Compare/SEO pages that omit the EmailJS library.
- **Compare Destinations `us_green_card` content (2026-05-21):** Three errors corrected in `js/pathway-compare-constants.js` `PATHWAY_DATA.us_green_card`: (1) DV lottery — Nigeria was NOT eligible for DV-2026 (exceeded 50k-native threshold); most other African countries are eligible — verify the current year's exclusion list. (2) EB-2 ROW Nigeria — EB-1 and EB-2 are **Current (C)** for Rest-of-World as of May 2026 Visa Bulletin; multi-decade backlogs affect India and China, not Nigeria. (3) EB-2 NIW category error — NIW removes PERM, not the visa queue; NIW petitioners share the EB-2 priority date. **Re-synced with backend v2.3 (2026-05-21):** (4) E-2 treaty list in `investor_business_visa×US` updated to include both Republic of Congo (Brazzaville) and Democratic Republic of Congo (Kinshasa); (5) UK credential field changed from `'UK ENIC / NARIC'` to `'UK ENIC'` (renamed March 2021); (6) Netherlands credential field changed from `'EP-Nuffic (Netherlands)'` to `'Nuffic'`. Compare Destinations data is now fully aligned with backend `CHECKLIST_PROMPT_VERSION v2.3`; 24h soak clock starts 2026-05-21. **Cross-repo flag:** the same three errors exist in the backend `utils/checklist-pathway-data.js` `us_green_card` entry — a separate backend fix + `CHECKLIST_PROMPT_VERSION` bump is required to flush `ai_cache`. **Navbar solid state (2026-05-21):** `pathway-compare.html` now carries a page-level `<style>` block that forces `#navbar` to always render solid (white background, navy text) regardless of scroll position — the hero section sits 72px below the fixed nav due to `pc-main { padding-top: 72px }`, exposing the light `--bg` body colour behind the nav and making the default white wordmark invisible. `#navbar` also gets `class="scrolled"` for the initial render; `#backToTop` element added to prevent null errors in `script.js` scroll handler.
- **Worth-it commentary must steer to the recommended Orabo tool.** The 4th sentence of every commentary references the Orabo tool the recommendation engine selected — by name. The system prompt's NEVER list forbids recommending external websites, government portals, immigration lawyers, consultants, or any non-Orabo resource. The recommendation engine is the single source of truth; the commentary and the CTA card must reinforce each other, never compete. When extending the recommendation engine, also confirm the new tool's label + description read naturally in a commentary closing sentence.
- **Preview route skips verifyPayment:** `/api/preview` is the free funnel entry — never add `verifyPayment()` to it. Claude is called directly after input validation and rate-limit check. No `ai_cache` reads or writes — every preview is fresh (small output, no value in caching).
- **Preview guardrails must survive prompt edits:** every preview prompt file (`routes/preview-prompts-*.js`) contains a `CRITICAL GUARDRAILS` block that enforces 4-block output and bans petition-letter prose. Do not remove or soften this block when editing prompts.
- **Preview cached in draft, not ai_cache:** after a successful preview, `lastPreview` + `lastPreviewEmail` are written to the localStorage draft (DRAFT_VERSION = 2). On re-visit, `showPreviewScreen_()` reads `draft.lastPreview` and passes it as `cachedPreview` — skipping the Claude call entirely. Version mismatch (v1 drafts) is handled by the existing `clearDraft` + `return null` path in `loadDraft`.
- **localStorage draft keys:** `orabo_draft_cv`, `orabo_draft_sop`, `orabo_draft_eb1a`, `orabo_draft_eb2`, `orabo_draft_eb1a_letter`, `orabo_draft_eb2_letter`, `orabo_draft_eb1a_package`, `orabo_draft_eb2_package`, `orabo_draft_o1a`, `orabo_draft_o1a_letter`, `orabo_draft_o1a_package`, `orabo_draft_ukgt_tech`, `orabo_draft_ukgt_tech_statement`, `orabo_draft_ukgt_tech_package`, `orabo_draft_ukgt_research`, `orabo_draft_ukgt_research_statement`, `orabo_draft_ukgt_research_package`, `orabo_draft_pathway_match`. These are distinct from the sessionStorage payment-return keys (`orabo_*_answers`, `orabo_*_output_type`) — do not confuse the two systems. Draft keys persist across tab closes and browser restarts; sessionStorage keys are tab-scoped.
- **CV/SOP purchase verification:** `verify-payment.js` derives `authUserId` from the JWT in the Authorization header; if a JWT is present and the user has an unconsumed `purchases` row for the requested item type, it is consumed atomically. For the anonymous cv-tool.html / sop-tool.html flow (no JWT, has Stripe session), the Stripe-session path is used instead. Do not add a hard userId requirement to `/api/cv` or `/api/sop` — both support anonymous Stripe-paid flow.
- **purchaseItem() guest checkout:** `stripe.js purchaseItem()` resolves email as `bookingMeta.customer_email || bookingMeta.booking_email || user?.email || ''`. CV/SOP tools pass `{ customer_email: answers.user_email }` as the third argument. Consultation (booking.js) passes `{ booking_email }`. Backend `userId` is optional for payment mode; required for subscription mode.
- **index.html paid tool buttons navigate to standalone pages:** via `js/redirect-paid-tools.js` (non-module). `cvtool.js`/`soptool.js` NOT loaded on `index.html`.
- **Standalone tool pages (cv-tool.html, sop-tool.html, consult-booking.html):** no auth, no navbar, no footer. cv-tool.html and sop-tool.html: no Supabase UMD — non-module inline `<script>` polls `window.cvTool`/`window.sopTool` (100 ms × 50 attempts) then calls `init()`. `init()` checks for payment return first; if not a return, calls `showModal()` + `renderSection(0)`. consult-booking.html keeps Supabase UMD (needed by booking.js → stripe.js → auth.js chain) and DOMContentLoaded auto-clicks `.consult-btn` after 300 ms.
- **auth.js is safe to import without Supabase UMD:** `const { createClient } = window.supabase || {}` — if Supabase UMD is not loaded, `supabaseClient` is null, auth state listener and init IIFE are skipped, `getCurrentUser()` returns null. This is required for cvtool.js/soptool.js which import stripe.js which imports auth.js on pages that do not load Supabase.
- **Return URL security:** `login.html`/`signup.html` validate `?return=` with `/^[a-z0-9\-]+\.html$/` — only `.html` filenames; no full URLs or query strings
- **stripe.js onDashboard guard:** IIFE skips `payment=success/cancelled` when `#dash-main` is present (dashboard handles those params itself)
- **Stripe webhook `checkout.session.completed`:** uses `.update().eq('id', userId)` (not upsert) on `user_profiles`. `current_period_end` read via `sub.current_period_end ?? sub.items?.data?.[0]?.current_period_end` (Stripe API `2026-03-25.dahlia` moved this field)
- **Country-of-origin validation:** `quiz-origin-saver.js`/`checklist-origin-saver.js` use capture-phase `addEventListener('click', fn, true)`; `e.stopImmediatePropagation()` if dropdown empty; `.country-select-error.visible` shows. Do NOT add to `quiz.js`/`checklist.js`.
- **DOCX-only downloads for checklist/report pages:** capture-phase listener inside `.then()`; `e.stopImmediatePropagation()` blocks html2pdf. Never add PDF — `generatePdf()` produces blank output for HTML.
- **Brand sanitisation in AI routes:** call `sanitizeBrand()` on every Claude response in `routes/checklist.js` and `routes/report.js` — both cache hits and fresh. Write sanitised content to `ai_cache`.
- **Dynamic Claude scholarship/university generation:** do NOT add DB queries for scholarships/universities to `routes/report.js` — Claude generates from `VISA_KNOWLEDGE_BASE`
- **Checklist knowledge base versioning:** when changing any immigration rule in `utils/checklist-pathway-data.js` or `utils/checklist-knowledge.js`, bump `CHECKLIST_PROMPT_VERSION` in `checklist-knowledge.js` (e.g. `'v2.1-2026-05-18'` → `'v2.2-2026-05-21'`). This changes all Supabase cache keys, forcing fresh AI generations for every combination. Old entries in `ai_cache` become unreachable harmlessly — no cleanup needed. **Current version: `v2.3-2026-05-21`** (bumped for accuracy sweep — Congo DRC E-2 treaty, UK ENIC rename, Nuffic rename).
- **Checklist infeasible combinations:** detected in `INFEASIBLE_COMBINATIONS` in `utils/checklist-knowledge.js`. Never cache infeasible results — they always generate fresh with the ⚠ guardrail header. Never modify `verifyPayment()` in `utils/verify-payment.js` — it must remain intact before any Claude call. To add a new infeasible combination, add a `{ detect(pathwayKey, dest, origin), explanation, closest_alternative, action }` entry to `INFEASIBLE_COMBINATIONS` and bump `CHECKLIST_PROMPT_VERSION`.
- **Opportunities submission form:** 8 required fields (`opp-field-error` inline per field): Link, Title, Organization, Country, End Date, Type, Brief Description, Your Email. Country `<select>`: USA, Canada, UK, Australia, New Zealand as flat options + `<optgroup label="Western Europe">` (19 countries) + "Other" as the final option for submitters outside listed regions. Type options include Undergraduate Scholarship and Scholarship Award. Frontend sends `{ title, organization, country, end_date, type, link, description, email }`; backend maps `organization`→`org`, `end_date`→`deadline`/`expires_at`. Duplicate check: `LOWER(TRIM(link))`; returns `{ error: 'duplicate' }` HTTP 409. `input[type="date"]` styled in `css/opportunities.css` under `.story-modal`; text/select inputs styled via global `.form-group input, .form-group select` in `css/layout.css`. Rate limit on POST: 5 submissions per IP per 10-minute window (env-overridable via `OPP_RATE_LIMIT_WINDOW_MIN` + `OPP_RATE_LIMIT_MAX`), route-scoped only — GET requests are not rate-limited. Backend sends `Retry-After` header + structured `{ error: 'rate_limited', retryAfter }` body on 429. Frontend reads `Retry-After` for the live cooldown countdown. `app.set('trust proxy', 1)` is required on `server.js` so Railway's proxy IP doesn't collapse all submitters into a single rate-limit bucket.
- **Community Feed filter dropdown:** The pill-button filter row was replaced with a `<select id="oppFilterSelect" class="opportunity-filter-select">` dropdown in `index.html`. Filter order: All · Undergraduate · Masters · PhD Funding · Fellowship · Scholarship · Research Grant · Postdoc · Jobs · Work Permit. Filter option `value` attributes and their exact type-mappings in `js/opportunities.js` `filterOpportunities()`: `all` → no filter; `undergraduate` → `type === 'Undergraduate Scholarship'` only; `Masters Scholarship` → `type === 'Masters Scholarship'` only (exact via default branch); `PhD Funding` → exact; `Fellowship` → exact; `scholarship` → `type === 'Scholarship Award'` only; `Research Grant` → exact; `Postdoc` → exact; `Job Opportunity` → exact; `Work Permit Program` → exact. The three scholarship-related filters (Undergraduate, Masters, Scholarship) are **mutually exclusive** — each maps to exactly one stored type and never overlaps. Do NOT change `scholarship` to a substring/contains match — that would cause cross-filter contamination. Styles in `css/opportunities.css` (`.opp-filter-row`, `.opportunity-filter-select`). `.filter-btn` styles in `css/cards.css` are retained — they are used by the visa tracker and university finder.
- **Opportunities publish notification:** `scheduler.js` runs hourly; queries `approved=true AND email_sent=false AND email != ''`; sends Brevo email; sets `email_sent=true`. The `email_sent` column was added via SQL editor — do not include in CREATE TABLE.
- **User-supplied URLs:** any URL sourced from a public submission form (opportunities, stories, universities, scholarships, user profiles) must be passed through `safeURL()` before rendering as an `<a href>`, `setAttribute('href', ...)`, or `window.open()` argument. `safeURL()` is defined inside the IIFE in `js/admin.js` and rejects everything except `http:` and `https:` schemes, returning `'#'` for invalid input. `esc()` escapes HTML metachars but does NOT sanitise the URL scheme — both are required. `js/opportunities.js` (public side) already applies the same pattern via its own `safeURL` helper.
- **Always update CLAUDE.md** after any major feature addition, architecture change, or new integration
- **CLAUDE.md mirror sync — App-authenticated with fail-loud SHA verification.** Mirror (`orabo-claudemd-mirror`) auto-syncs from `japaconnect` via `mirror-claudemd.yml`, authenticated by a **GitHub App installation token** (App scoped `contents: write` on the mirror only; secrets `MIRROR_APP_ID` + `MIRROR_APP_PRIVATE_KEY` in `japaconnect`). The workflow **fails RED** if the mirror does not end byte-identical to source (SHA256 verified via the GitHub Contents API, not the raw CDN — CDN lags ~5 min and gives false negatives immediately after push). Manual re-sync without a CLAUDE.md edit: Actions → `mirror-claudemd.yml` → Run workflow (workflow_dispatch). **Standing backstop unchanged:** after any CLAUDE.md push, `curl https://raw.githubusercontent.com/Oraboss/orabo-claudemd-mirror/main/CLAUDE.md | grep` for a unique string from the change — grep wins over a green Action. **History:** The 2026-05-13→05-22 incident (mirror frozen 9 days from a silently-expired `MIRROR_PAT`) and a second silent failure on 2026-05-23 are why the PAT was replaced with a GitHub App token: PATs expire and silently break the sync; App installation tokens are minted fresh per run (~1 h TTL) and cannot expire between runs. `MIRROR_PAT` has been retired — do not re-add it.
- **Data freshness governance:** Every static data file that displays numbers to users must export a `DATA_FRESHNESS` block `{ source, verified, cadence_days, tier }`. Tiers: `fx` (30d) — `js/costliving-data.js` FX rates; `fees` (90d) — `js/paycalc-data.js` salary/tax; `costliving` (90d) — `js/costliving.js` COST_DATA; `eligibility` (365d) — `js/visatracker-data.js` + `js/pathway-compare-data.js`. Run `node scripts/check-data-freshness.mjs` to check freshness and FX band (exits 1 if STALE or OUT-OF-BAND). `js/costliving-data.js` is a pure-data file (no browser imports) that holds `FX_RATES`, `FX_BASELINE`, and `DATA_FRESHNESS`; `costliving.js` imports from it. On monthly FX re-verify: update both `FX_RATES` and `FX_BASELINE` in `costliving-data.js` together, bump `DATA_FRESHNESS.verified`. See `FRESHNESS.md` for the full governance table and re-verify workflow.
- **Source citations (`js/sources.js`):** Every numeric figure displayed by a tool must have a matching `SOURCES[key]` entry across the three data files (`js/sources-paycalc.js`, `js/sources-costliving.js`, `js/sources-visatracker.js`). No entry without an opened authoritative URL, an ISO `verified` date, and a `license_note` field (`'public_domain'`, `'fair_use_attribution'`, `'orabo_estimate'`). If a number cannot be traced to a citable source, mark it `license_note: 'orabo_estimate'` with a `methodology` field — never invent a source. `sources-modal.js` accesses `window.OraboSources` lazily at click time (set by `sources.js` module evaluation) — do NOT read it at IIFE init time to avoid module-deferred-execution race. When adding a new tool with numeric outputs, add its source entries and render a `.source-tag[data-source-key]` button. Part 2 (AI document citation footers) shipped 2026-05-18 — see Phase Sources Part 2 entry in Current State above.
- **Footer content must never include provenance or debug metadata.** The `## Sources & Authorities` footer appended to AI deliverables must contain only legal citations and regulatory references visible to the customer — never authoring metadata, methodology comments, bot-detection notes, or internal process annotations (e.g. `canonical-pattern-confirmed`, `content not web-fetched due to Cloudflare bot detection`, `Last verified: …`). This is the inverse of the 3-layer footer-defence lesson (which ensures a *required* block always appears): this rule ensures a *forbidden* fragment never appears. **Guard pattern:** any new document-generation route that appends a footer must add a log-only absence check in `routes/docgen.js generateDocx()` or inline in the route handler. The existing guard (`LEAK_TOKENS = /Cloudflare|web-fetched|canonical-pattern|Last verified/i`) in `generateDocx()` covers the four originally leaked tokens; extend the regex when new forbidden tokens are identified. The guard must be log-only (`console.error` / `console.warn`) — never runtime-strip, because stripping would mask a future reintroduction silently. When editing any `## Sources & Authorities` section across the 8 backend source files, verify the edited text does not match `LEAK_TOKENS` before committing.
- **Email delivery for checklist + report uses trusted email from `verify-payment.js`**, then falls back to `user_email` from request body. Frontend reads from `localStorage.orabo_user_email` (set by dashboard.html on auth, cleared on sign-out). Resolution priority: `trustedEmail` (from Stripe session `customer_email` or JWT Supabase lookup inside `verify-payment.js`) → `user_email` body field → skip send + log `[brevo-checklist] no recipient email available`. Both routes return `email_queued: boolean` in the response; frontend email-notice is gated on `json.email_queued`.
- **Every Brevo fire-and-forget call must have `.then(success log)` + `.catch(err => detailed error log)` with a `[brevo-<context>]` prefix.** No silent swallowing — Railway logs are the only way to spot delivery regressions. Contexts: `checklist`, `report`, `cv`, `cv-attr`, `sop`, `sop-attr`, `eb1a`, `eb1a-attr`, `eb2`, `eb2-attr`, `eb1a-letter`, `eb1a-letter-attr`, `eb2-letter`, `eb2-letter-attr`, `eb1a-package`, `eb1a-package-attr`, `eb2-package`, `eb2-package-attr`, `o1a`, `o1a-attr`, `o1a-letter`, `o1a-letter-attr`, `o1a-package`, `o1a-package-attr`, `ukgt-tech`, `ukgt-tech-attr`, `ukgt-tech-statement`, `ukgt-tech-statement-attr`, `ukgt-tech-package`, `ukgt-tech-package-attr`, `ukgt-research`, `ukgt-research-attr`, `ukgt-research-statement`, `ukgt-research-statement-attr`, `ukgt-research-package`, `ukgt-research-package-attr`, `preview`, `worthit`, `consultation`, `waitlist-welcome`, `rejection`, `story-approved`, `story-rejected`, `payout`, `opp-ack`, `publish`.
- **Waitlist welcome email** is fired by `routes/newsletter.js` when `destination` is present OR `source === 'waitlist'`, AND the source is not in the suppressed-sources list, AND the Brevo contact-create returned HTTP 201 (new contact). Suppressed sources: `eb_waitlist`, `eb_waitlist_homepage`, `quiz_score_capture`, `opportunity_submission`. Repeat subscribers (existing contacts, HTTP 204) never receive a second welcome email. Gate decisions log to Railway as `[brevo-waitlist-welcome] gate: { destination, source, isNewContact, shouldFireWelcome }`. Contact creation uses `POST /v3/contacts` with `updateEnabled: true` directly. `sendBrevo` in `routes/admin.js` accepts `context` as its first argument — all callers must pass it.
- **Opportunity submission email flow:** `routes/opportunities.js` fires a fire-and-forget acknowledgement email (`[brevo-opp-ack]`) after every successful row insert — includes title, type, country, link details and a phone-conditional Post & Earn paragraph (renders only when `submitter_phone` is non-empty). `scheduler.js` hourly cron fires the "your opportunity is now live" email (`[brevo-publish]`) when `approved=true AND email_sent=false AND email != ''`; the query LEFT JOINs `post_earn_rewards` to include `approved_count` in the email. The newsletter subscribe call in `js/opportunities.js` passes `source: 'opportunity_submission'` (suppressing the welcome template) and maps the opportunity country to a valid `VALID_DESTINATIONS` value for the digest list (`UK`→`United Kingdom`, European countries→`Europe`); destination is omitted when no mapping exists so the contact is still created in Brevo without a list-add.
- **Mobile hamburger nav:** all styles in `css/hamburger.css` inside `@media (max-width: 768px)`. Toggle IIFE in `script.js`. Hamburger uses `.open` class. Do not add desktop styles to `hamburger.css`; do not duplicate toggle logic.
- **Mobile overflow fixes (`css/mobile.css`):** loaded last in `styles.css`. All rules inside `@media (max-width: 768px)`. Owns `html, body { overflow-x: hidden }`, hero container fixes, hero text scaling, stats bar wrapping, grid single-column. Do NOT touch other CSS files for mobile overflow.
- **Service worker:** permanently removed — `sw.js` and `js/register-sw.js` are stub comment files; the `<script src="js/register-sw.js">` tag is absent from `index.html`. Do not re-add a service worker.
- **PWA icons:** `icons/icon-192.png` and `icons/icon-512.png` are binary copies of `orabo-icon-256x256.png` and `orabo-icon-512x512.png` respectively. Source files live in the repo root. Replace both icons folder files whenever the source logo changes. `site.webmanifest` references these exact paths.
- **Email template logo:** all Brevo email templates reference `https://orabo.app/orabo-icon-256x256.png` as a hotlinked PNG (80×80 rendered, `width`/`height` attributes required for Outlook). Never reference `orabo-icon.svg` in emails — SVGs do not render in Outlook or many other clients. Inline styles ARE permitted in email HTML — the CSP rule applies to the web app only.
- **SEO landing pages pattern (`readiness-score.html`, `visa-eligibility.html`, `document-checklist.html`, `eb-immigration.html`):** minimal nav (logo left + "Back to Orabo" right), no footer (just copyright line), load `styles.css` only, all page-specific layout in inline `<style>` in `<head>`, mobile responsive below 768px. Use `.lp-nav`, `.lp-hero`, `.lp-section`, `.lp-cta-band`, `.lp-footer` class convention. CTAs link to app sections (e.g. `/#readiness`, `/#eligibility`, `/#tools`) — not to standalone tool pages. **CSP rule applies equally to SEO landing pages:** the Cloudflare edge `script-src 'self'` bans ALL inline `<script>` blocks site-wide — not just tool boot scripts. Any interactive JS (form handlers, analytics wiring, etc.) on an SEO landing page MUST live in an external file. `eb-immigration.html` uses `js/eb-immigration.js` (non-module IIFE, loaded at end of `<body>`); `readiness-score.html`, `visa-eligibility.html`, `document-checklist.html` have no interactive JS and no external script required. Do NOT add inline `<script>` blocks to any page.
- **`eb-immigration.html` footer waitlist:** handled by `js/eb-immigration.js` (non-module IIFE, external file — externalised 2026-05-21 to fix CSP block). IIFE wires click handler on footer form only (`eb-email-2`/`eb-submit-btn-2`/`form-success-2` — hero email capture removed 2026-05-23, replaced with two Tier 1 CTA buttons). POSTs `{ email, source: 'eb_waitlist' }` to `/api/newsletter/subscribe`. Shows success on both resolve and reject (fail-open — **known limitation**: `.catch` shows success even on endpoint error; flagged for a future fix). No auth required.
- **Blog articles (`blog/*.html`):** load `../styles.css` (relative), use `.article-body`, `.callout`, `.data-table`, `.cta-block`, `.blog-nav` classes. Back link points to `/blog/`. Target 900–1,200 words. End each article with a `.cta-block` linking to the relevant Orabo tool.
- **`blog/index.html`:** card grid using `.blog-card` anchors; `blog-tag`, `blog-card h2`, `blog-meta` classes. Add a new card here whenever a new blog article is created.
- **Homepage blog teaser section (`index.html`):** 3-column `.blog-teaser-grid` of `.blog-teaser-card` anchors above the EB waitlist banner. Styles in `css/eb-waitlist.css`. Update cards here whenever a new article is added to the blog.
- **Homepage EB waitlist banner (`index.html`):** dark navy `.eb-waitlist-section` above the footer. Email form `#eb-index-email` / `#eb-index-btn` / `#eb-index-success` wired by `js/eb-index-waitlist.js` — POSTs `{ email, source: 'eb_waitlist_homepage' }` to `/api/newsletter/subscribe`; fail-open (shows success on both resolve and reject). Styles in `css/eb-waitlist.css`.
- **Nav Tools dropdown links (index.html):** Blog and EB Green Card Tools entries live after the SOP Writer `<li>`, separated by a `.nav-dropdown-divider` `<div>`. EB entry uses `.nav-dropdown-coming-soon` + `.nav-coming-badge` "Soon" badge. Styles for badge in `css/nav.css`.
- **Admin panel (`admin.html`):** accessed by direct URL only — not linked from any public page. Does NOT load Supabase UMD or `styles.css`. Auth via `x-admin-token` header checked against `process.env.ADMIN_SECRET`. Token stored in `sessionStorage`. All admin API routes live under `/api/admin/` and require the token. `css/admin.css` is NOT imported into `styles.css`. `js/admin.js` is a non-module IIFE.
- **Admin opportunity approval:** sets BOTH `approved = TRUE` AND `status = 'approved'` — keeps existing scheduler cron working. Does NOT send the "post is live" email (scheduler handles it). Upserts into `post_earn_rewards` (approved_count +1, phone from submitter_phone).
- **Post & Earn payout:** `POST /api/admin/post-earn/:email/mark-paid` resets `approved_count = 0`, sets `reward_paid_at = NOW()`, increments `total_paid`, keeps `reward_paid = FALSE` so they can earn again. Sends Brevo airtime notification. Railway env vars: `POST_EARN_THRESHOLD` (default 20), `POST_EARN_REWARD_PER_POST` (default 300), `POST_EARN_PAYOUT` (default 6000).
- **Opportunities `submitter_phone` field:** collected via `id="opp_phone"` in index.html modal (optional). Passed in POST body as `phone`. Stored in `submitter_phone` column. Displayed in admin panel Phone column.
- **CV/SOP delivery emails (Phase 5A):** `_sendCvEmail` and `_sendSopEmail` in `routes/cv.js` / `routes/sop.js` include the specific output type label in subject and body. Upsell paragraph: `cv_review` and `cover_letter_only` upsell SOP Writer; all SOP types upsell Consultation. Both fire a separate fire-and-forget `POST /v3/contacts` to Brevo setting `PURCHASE_DATE` (ISO date string `YYYY-MM-DD`) + `LAST_TOOL` (outputType). `SOP_LABELS` map in `sop.js` maps `sop_scratch`→`Statement of Purpose (from scratch)`, `sop_review`→`SOP Review`, `sop_rewrite`→`SOP Rewrite`.
- **Newsletter subscribe backward compatibility:** `/api/newsletter/subscribe` no longer requires `destination`. `destination` is validated only when provided; contact is added to the list only when `destination` is present. Source-only callers (EB waitlist, quiz capture, any future non-destination subscriber) work without `destination`.
- **Fire-and-forget writes from public pages (Dashboard Rebuild Phase 3+):** Any new save-on-action script (like `quiz-results-saver.js`) must: (1) skip silently if no session — no console error visible to users; (2) never block or visually alter the UI it observes; (3) use a sessionStorage flag to dedupe saves within a session; (4) clear the flag on error so retry-on-retake works; (5) never modify the source module it observes (`quiz.js`, `cvtool.js`, etc.); (6) use a MutationObserver or capture-phase listener to attach — never hook directly into source module internals. When the source module uses `style.display` for visibility (like `quiz.js`), observe the `style` attribute; when it uses `.active` class (like cvtool/soptool), observe the `class` attribute. On index.html, scripts that need a JWT must use `readSessionFromStorage()` inlined verbatim from `js/auth.js`, not `window.supabaseClient.auth.getSession()`. The home page does not load the Supabase UMD client. This pattern matches `pro-bypass-checklist.js` and `pro-bypass-quiz.js`. The same rule applies to any new script added to `index.html` that requires the user's session.
- **Saver-style scripts that mirror quiz.js (or any other sacred module) MUST document the exact line numbers and selectors they depend on at the top of the file, in a DEPENDENCY comment block.** When the underlying module changes, the dependent script's coupling-points note is updated in the same commit, OR a smoke test is run to confirm the dependency still holds. Smoke test: take the full quiz as a signed-in user, observe the /api/quiz/save-results POST in the Network tab, confirm `score` in the request payload matches the score shown on screen (not 0). A genuine score of 0 is mathematically impossible (minimum ~33% with all lowest options) — any score=0 save is a selector or formula drift bug, not real user data.
- **Quiz email capture pattern (Phase 5B):** `js/quiz-email-capture.js` is a non-module IIFE. DOMContentLoaded must wipe `orabo_quiz_email_captured` + `orabo_quiz_score_capture` from sessionStorage BEFORE reading any state (cleanup-first rule). MutationObserver watches `#quizResultsScreen` for `style` attribute changes; injects `#quiz-email-capture` above `#lockedReportBox` when results become visible. Skips injection if sessionStorage key already set. Submit POSTs `{ email, source: 'quiz_score_capture', quiz_score, quiz_date }` to Railway URL directly (not relative — Cloudflare Pages has no server proxy). Fail-open on both resolve and reject. "No thanks" adds `.hidden` to the block and sets `orabo_quiz_email_captured = 'skipped'`. Load AFTER `quiz.js` and `free-quiz-origin.js` in `index.html`.
- **`.hidden` utility class:** defined in `css/quiz.css` — `display: none`. Shared by quiz email capture elements. Do not define again elsewhere.
- **EmailJS legacy:** `emailjs.send` calls remain in `js/booking.js:137` and `js/opportunities.js:251` — both using `template_3oaoxaa` (admin notification only). `template_yva0uva` (user auto-reply) has been removed from the codebase entirely (removed 2026-05-11 — was causing duplicate welcome emails via Gmail relay alongside Brevo). EmailJS is scheduled for full removal in a future migration session — **do NOT add any new EmailJS calls anywhere in the codebase**. Use Brevo for all new email functionality.
- **Payment verification on AI routes:** All 7 AI generation routes call `verifyPayment()` from `utils/verify-payment.js` before invoking Claude. Recipients for Brevo delivery are derived from the trusted source (Stripe session `customer_email`, or Supabase auth lookup via `userId`), never from `formAnswers.user_email`. Kill switch is `AI_ROUTES_REQUIRE_STRIPE_SESSION` on Railway. Frontend tool modules must forward `stripe_session_id` from URL params in the POST body — never omit. Any new AI-generation route added in the future (additional visa suites, etc.) MUST call `verifyPayment()` before any Claude API call.
- **Visa Eligibility tool back/next buttons:** `js/eligibility.js` follows the same `.tool-back-btn` / `.tool-next-btn` pattern as CV/SOP/EB/O-1A tools — back button rendered on Q2–Q10 only (Q1 has only the X close), wired via `addEventListener`, label `← Back`. Next button is the standard accent-blue pill from `css/cvtool.css`. Final question's Next label is `See my matches →`. Progress counter reads from `questions.length` — never hardcoded. A `#eligibilityResultsBack` `.tool-back-btn` is also injected as the first child of the results screen by `renderResults()` — clicking it hides results, re-renders Q10 with the saved answer preserved, and does NOT modify the localStorage free-use counter (`usesIncrementedThisSession` flag guards against double-increment on back+resubmit; "Check Again" resets the flag for a genuine new attempt). Mirrors the `#quizResultsBack` pattern in `js/quiz.js`.
- **Visa Eligibility upgrade modal z-index:** the upgrade modal must stack ABOVE the eligibility results modal — its z-index (10010) is set above the `.quiz-overlay` z-index (10000) in `css/subscription.css`. Do not close the eligibility results when opening the upgrade modal — keep both stacked so the user can return to their matches after the upgrade flow.
- **Petition Audit reviewer pattern (`routes/audit.js` + `routes/audit-prompts.js` + `utils/document-ingest.js`):** `routes/audit.js` is a **shared reviewer route** that dispatches by `family` — do NOT add petition-review personas to the O-1A generate prompts (`routes/o1a-prompts.js`). The officer+counsel personas live exclusively in `audit-prompts.js` keyed by family. Ingestion uses `utils/document-ingest.js` (`parseUploadedDocument` + `sanitizeForPrompt`) — the untrusted petition text is always wrapped in `<uploaded_petition>...</uploaded_petition>` delimiters and the system prompt instructs Claude to ignore any embedded instructions inside the tags; `sanitizeForPrompt` strips the delimiter tags from the text before they reach the prompt so the user cannot inject extra closing tags to escape the delimited block. Officer calibration is the primary tuning risk: the officer must be adversarial but anchored — not everything INSUFFICIENT (too harsh → useless), not everything STRONG (too lenient → worthless). Overall Posture is always a named band, never a numeric %. When porting to a new family (ukgt), add a new key to `audit-prompts.js` and extend the `AUDIT_HTML_DISCLAIMERS` map in `audit.js` — do not create a separate route file. **EB-1A audit voice rule:** EB-1A is self-petitioned; first-person beneficiary voice is CORRECT and must NOT be flagged in `EB1A_AUDIT_BLOCK1` or the output contract Structural Compliance section. **EB-2 audit threshold rule:** Dhanasar requires ALL THREE prongs; `EB2_AUDIT_BLOCK1` must state this is not a 3-of-N count; the Threshold Check section must say "failure on any single prong is a threshold failure." **EB-1A calibration status (2026-05-21):** NEEDS-TUNING (officer too harsh on letter-format inputs; letter_A STRONG profile → HIGH RFE RISK; tuning pass deferred). **EB-2 calibration status (2026-05-21):** CALIBRATED (6/6). **DEFERRED:** pre-payment file-readability precheck (post-payment ingest failure for encrypted/scanned PDFs is currently handled by a support mailto link; a free `/api/audit/precheck` or client-side readability probe would catch this before Stripe redirect — deferred beyond Phase 1B). **EB-1A tuning pass deferred:** lever is the CALIBRATION REQUIREMENT block in `EB1A_AUDIT_BLOCK1` — calibrate toward leniency on single letter-format inputs by emphasising that a strong letter with ≥3 arguable criteria is not "HIGH RFE RISK."
- **Pathway Match route (`routes/pathway-match.js` + `routes/pathway-match-prompts.js`):** mounted at `/api/pathway-match` with `checklistLimiter`. Single `outputType` branch: `pathway_match_report` only — gate returns 400 for any other value. Two-block system prompt: Block 1 = `PATHWAY_MATCH_SYSTEM_PROMPT` (cached, built at require-time from v2.3 knowledge modules), Block 2 = per-request formAnswers JSON + date + local currency + TOOL_MANIFEST. `max_tokens: 6000`. NO ai_cache reads/writes — every report is generated fresh. `verifyPayment` + `gate` (HTTP 403 on failure). `stripe_session_id: null` in one_off_purchases insert (intentional — verify-payment.js already inserts the row with real session_id). Brevo log prefixes: `[brevo-pathway-match]`, `[brevo-pathway-match-attr]`. Response file key: `pathway_match_docx`. Download filename: `pathway-match-report.docx`. `TOOL_MANIFEST` is a local constant in `pathway-match-prompts.js` — do NOT import from stripe.js (circular dep). Keep in sync manually with `ITEM_PRICES` when prices change. **Accuracy rails in Block 1** must never be weakened: (1) INFEASIBLE_COMBINATIONS absolute; (2) DV lottery Nigeria exclusion; (3) EB-2 ROW no multi-decade backlogs, NIW removes PERM not the priority date queue; (4) success bands high/moderate/low only; (5) profile gaps with `[PROFILE GAP]`; (6) attorney consultation note in Sections 3 and 7; (7) Section 7 ≥1 real matching tool.
- **Pathway Match frontend tool (`pathway-match-tool.html` + `js/pathway-match-tool.js`):** identical standalone pattern to the UKGT/EB/O-1A tools. Section 0 back button → `/pathway-compare.html` (NOT `/`). Polls `window.pathwayMatchTool` via `js/autoopen-pathway-match.js` (CSP-compliant external IIFE — follows the same pattern as `js/autoopen-ukgt-research-package.js`). sessionStorage keys: `orabo_pathway_match_answers` / `orabo_pathway_match_output_type`. TOOL_KEY `pathway_match`, localStorage draft key `orabo_draft_pathway_match`. NO preview screen — $19 is below the $140 threshold. Multiselect `q15_target_countries`: 4-country cap — disable unchecked boxes when 4 checked; stored as pipe-delimited `|` string; validation requires ≥1. POST body field name: `formAnswers` (not `answers`). Cancel return restores answers from sessionStorage and shows review screen + `.payment-cancel-notice`. SCREENS: `['pm-form-screen','pm-review-screen','pm-payment-screen','pm-loading-screen','pm-download-screen']`. `css/pathway-match.css` holds the screen visibility rules; `.payment-cancel-notice` is defined in `css/quiz.css` — NOT redefined in `css/pathway-match.css`.
- **Dual-DB architecture:** Operational/transactional data (users, payments, auth, AI cache, audit logs) lives in Orabo Prod (`rrkkbnoqubfmhrnxgngp.supabase.co`, us-west-2), accessed via the Supabase JS client (`SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY`). Content/CMS data (universities, scholarships, opportunities, visas, stories, visits) lives in Orabo Main (`fcogjoxcfmrcuvtreivm.supabase.co`, us-east-1), accessed via the raw `pg.Pool` in `db/client.js` (`DB_HOST`, `DB_PORT`, `DB_NAME`, `DB_USER`, `DB_PASSWORD`). When adding a new table, decide which role it serves and place it in the correct project. Never JOIN across DBs — fetch from each and combine in memory. When running migrations, always verify the project ref in the Supabase URL bar before clicking Run.

---

## Design System

**Colours:**
- `--navy: #1a3c6e`, `--navy-dark: #0f2548`, `--navy-light: #2a5298`
- `--accent: #4a90d9`, `--gold: #f5a623`
- `--bg: #f4f7fb`, `--bg-alt: #eaf0f9`, `--text-muted: #5a6b85`

**Fonts:** Inter (body), Playfair Display (decorative)

**Key utility classes:** `.btn-primary`, `.btn-gold`, `.btn-outline`, `.section-tag`, `.gradient-text`, `.filter-btn`

**Nav logo:** `.nav-logo` (flex, align-items center, gap 8px) wraps `<img src="/orabo-icon.svg">` + `<span class="nav-brand-name">Orabo</span>`. `.nav-brand-name`: Inter 700, 1.2rem, `#fff`, letter-spacing -0.01em. Both classes defined in `css/base.css`.

**Footer brand:** `.footer-brand` uses `<img src="/orabo-icon.svg" alt="Orabo">` + `<span>Orabo</span>` inside `.nav-logo` anchor — same SVG and sizing (36×36px) as the navbar logo, no globe emoji. No separate footer-logo class — inherits `.nav-logo img` styles from `css/nav.css`.

**Nav buttons (index.html):** `.nav-login-btn` (ghost, white border), `.nav-signup-btn` (gold fill), `.nav-go-pro` (gold pill) — all use `!important` in `css/auth.css`.

**Landing-page hero button role system:** Three EB/O-1A/UKGT landing pages (`eb-immigration.html`, `o1a-visa.html`, `uk-global-talent.html`) follow a shared role convention for hero CTAs:
- **`.btn-hero-primary`** — gold (`var(--gold)`) pill; the primary money-CTA in the hero section only. Defined locally in each page's `<style>` block (not in a shared stylesheet — see tech-debt note below). EB hero adopted this in the 2026-05-23 gold-family pass; o1a and ukgt already used it.
- **`.btn.btn-primary`** — standard blue/navy action; used for all non-hero buttons (eligibility-section cards, tier CTAs, etc.).
- **Ghost/outline** — secondary or de-emphasized action.
Never use `.btn.btn-primary` in a landing-page hero where a money-CTA is intended — use `.btn-hero-primary` so all three heroes remain visually consistent.

**Parent organisation attribution:** `orabo.app` declares `oraboss.com` as its parent organisation in footer text and JSON-LD schema. The Oraboss attribution line ("A product of Oraboss — building intelligent products at the intersection of Africa and the global economy.") appears above the copyright line in the footers of `index.html`, `blog/index.html`, `eb-immigration.html`, `o1a-visa.html`, `uk-global-talent.html`, `readiness-score.html`, `visa-eligibility.html`, and `document-checklist.html` (plus any blog articles that have their own footer). `dashboard.html` has no footer. The link uses `target="_blank" rel="noopener"` and hovers to `var(--accent)`. The `parentOrganization` and `sameAs` fields are merged into the existing `WebApplication` JSON-LD block in `index.html <head>` (not a separate schema block). The `.footer-parent-org` and `.oraboss-link` CSS rules live inline in the `<style>` block of each landing page; on `index.html` they live in `css/footer.css`; on `blog/index.html` they live in the page's inline `<style>` block.

---

## Repositories

- **Frontend:** https://github.com/Oraboss/japaconnect — deployed at `https://orabo.app`
- **Backend:** https://github.com/Oraboss/japaconnect-backend — deployed on Railway
- **Branch (both):** main
- **Git user:** Abiodun Bello

---

## Pending / Parked Items

Priority tiers: **Blocking** → **High** → **Medium** → **Low**

### High — verification owed (code shipped, not yet confirmed live)

- **Live $19 Pathway Match production smoke** (scheduled 2026-05-22) — full funnel gate: real card → Stripe `cs_live_` session → `one_off_purchases` row written with non-null `stripe_session_id` → 7-section report renders in DOCX → Brevo delivery email arrives. ~$0.56 non-refundable Stripe fees (canonical smoke cost). Do not mark Pathway Match Phase C complete until this passes.
- **`eb-immigration.html` live verification** (reworked 2026-05-23) — confirm on live page: hero shows two CTA buttons (EB-1A $39 + EB-2 NIW $35), no email field; footer shows 'Not ready to file yet?' heading + 'Join Waitlist' button; submitting footer email POSTs to `/api/newsletter/subscribe {email, source:'eb_waitlist'}` with no CSP error; zero `Refused to execute inline script` in console.

### High — sitewide silent-CSP audit (new, flagged 2026-05-21)

- The 2026-05-18 inline-script-extraction pass **missed `eb-immigration.html`**, leaving its waitlist dead for 3 days. Other pages from that pass may have been missed too. **Action:** static scan of every HTML file for inline `<script>` blocks with bodies (no `src` attribute) that the Cloudflare edge `script-src 'self'` CSP (no `unsafe-inline`) would block. Prioritise lead-capture surfaces: SEO landing pages (`readiness-score.html`, `visa-eligibility.html`, `document-checklist.html`, `eb-immigration.html`), marketing pages (`o1a-visa.html`, `uk-global-talent.html`), and blog articles. Failure mode is invisible to users (form appears to succeed, no POST fires), so this requires a deliberate audit — not incidental discovery.
- **Standing check to adopt:** any page with a form or interactive JS gets a one-time DevTools console pass for `Refused to execute inline script` before it is considered shipped.

### Medium — footer template a11y (sitewide)

- `buildFooter()` / shared footer has two a11y issues capping Lighthouse Accessibility at ~93 across SEO pages, `pathway-compare.html`, and all pages using the footer:
  1. **`color-contrast`** — `--text-muted` footer greys (`#b07f2e` / `#6f7c91` / `#637188`) on `--navy-dark` (`#0f2548`) fail the 4.5:1 WCAG AA ratio. Fix: introduce a footer-context token, e.g. `.footer-text-muted { color: rgba(255,255,255,0.6) }`, without touching the global `--text-muted` variable used elsewhere.
  2. **`heading-order`** — footer `<h5>` column headings follow `<h1>`→`<h3>` page headings, skipping H4. Fix: replace footer column headings with `<p class="footer-col-heading">` or a styled `<div>` (not a heading element).
  - After fixing `buildFooter()`, regenerate the SEO comparison batch (`node scripts/generate-compare-seo.mjs`). Target: Accessibility ≥ 95.

### Low — follow-up (flagged, not a regression)

- **Tech debt (PARKED — do on next new public-page build, do NOT do standalone) — navbar transparent-at-top mechanism:** The navbar defaults to transparent + white text at `scrollY=0` because `script.js`'s scroll handler removes `.scrolled` at the top; pages needing a solid top-nav each carry an inline `<style>` `!important` override (`pathway-compare.html` + the 13 `/compare/` pages). Consolidation would make solid the default in `css/nav.css` (with an opt-in modifier for any genuine dark-hero page) and initialise the scroll state on `DOMContentLoaded`, so new light-background pages inherit a legible nav without the per-page override. **Why parked:** payoff is minor/occasional; the change is high blast-radius (every public page incl. signup/login/dashboard inline-nav paths); current state is safe and isolated. **TRIGGER — pull off the shelf when EITHER:** (a) building a new public page with a light area behind the top nav (you'd otherwise paste the override — do the consolidation then, while already in the nav code with a concrete page to verify against), OR (b) a user reports an invisible/flashing navbar on any page (the `scrollY=0` flicker may intermittently hit non-override pages) — that converts this from maintainability to an active bug and it jumps the queue.

- **`js/eb-immigration.js` `.catch` swallows endpoint errors** — the catch handler currently shows the success message ("You're on the list!") even when the backend returns an error or the network is down, silently losing the lead. Pre-existing behaviour preserved verbatim during the 2026-05-21 CSP fix. Address only if Railway logs show recurring `/api/newsletter/subscribe` errors; when fixing, give the `.catch` branch a real inline error state (e.g. "Something went wrong — try again").

- **Tech debt (PARKED — low priority):** `.btn-hero-primary` is defined locally on 3 pages (`o1a-visa.html`, `uk-global-talent.html`, `eb-immigration.html`) and has **already drifted**: o1a uses `padding: 14px 28px`; uk-global-talent uses `padding: 16px 32px` plus `border: none; cursor: pointer; font-family: inherit` — so the ukgt hero buttons render larger than o1a's/EB's today (a live, subtle visual inconsistency). Durable fix: pick ONE canonical definition (decide tighter `14px 28px` vs roomier `16px 32px`), promote it to `css/buttons.css`, delete the 3 local copies — this collapses the duplication AND resolves the size drift in one move. TRIGGER: do this on the next styling pass touching these landing pages, or when the size mismatch is noticed in the wild. (eb-immigration adopted o1a's `14px 28px` version in the 2026-05-23 gold-family pass.)
